# Donor Adaptation — what has been tried, what it cost, where to look

**The goal:** run somebody else's pretrained LLM on our architecture (`engine.c`), target **~10B at
50 tok/s** (good) / **100 tok/s** (excellent).
**Last updated: 2026-09-12 (**E43 **UNITS HALF DONE, SPEED HALF BLOCKED -- no verdict, no rate** (ledger 53): the head is `V x D`, it sits OUTSIDE the layer loop, it is charged whole on every token and it is **61.5% of the floor** at ten billion -- bigger than all the attention put together, and never probed; `V = 32768` is a choice, not a law. The finished half is the uncomfortable one: **the goal is written in tokens/second and a token is not a fixed amount of text.** Measured on the frozen 51,870 bytes, a token is worth **2.647 B at V=2048 against 4.124 at V=131072 -- a 1.56x range at an IDENTICAL parameter count**, so "50 tok/s" under-specifies the target by up to 56% in delivered text. `G-E43B`, `G-E43C` (exact, to the last digit) and `G-E43D` (ORDINAL by design, E42's repair) all FIRE. **`G-E43A` went VOID**: the box was verified at 5.8% busy and reading +2.0% before launch, then filled to 45.0% DURING the run; reps 1-2 were clean and **are not used**, because picking the reps that pass is the error that voided E42. Worse and mine: arms are timed in ascending `V` within a rep, so **a drifting box manufactures "small V is faster" by itself** -- E40 had the antidote and used it, run 1 did not. Addendum A (`3342175`) registers ONE re-run, both orders, or no speed half ever. **Zero BPB: E43 cannot move the T4 ask, only say which `V` to train H1 at.**)** --- (precedente: **Last updated: 2026-09-12 (**E42 **NESSUN VERDETTO REGISTRATO: il controllo nullo e andato VOID e applico la regola registrata per intero** ("nessun braccio sotto conta"), quindi i numeri qui sotto sono una MISURA e non un verdetto, e il gate NON e stato rifatto passare. **`G-E42C` era MAL SPECIFICATO DA ME, ed e aritmetica**: confrontava la MEDIA su tre semi con un VALORE a un seme con tolleranza `0,010`, mentre la deviazione del braccio stesso e `0,099` -- dieci volte la tolleranza; un controllo che la dispersione ordinaria fa scattare per forza non e un controllo, e avevo fissato quella tolleranza prima che la dispersione su quell'asse fosse mai stata misurata, che e esattamente cio che E42 doveva misurare. **L'ipotesi che il gate NOMINA ("l'harness sta fittando l'eval") e confutata per via diretta**: due celle pubblicate si riproducono allo stesso seme -- `D0C` `3,597107527` contro il `3,597108` di E38 (`-4,73e-07`) e `RAND` `4,535311605` contro il `4,535311855` dell'addendum B di E41 (`-2,50e-07`); un harness che fitta l'eval non riproduce i numeri di altre sessioni a sette cifre. Non basta a farlo scattare. Gli altri tre gate scattano (`G-E42B` inerzia a `0,00e+00` su tutti e quattro; `G-E42D`: `PRED` e 256 gruppi da 35 in ogni layer e disaccorda da `COACT` sul 93,7% dei neuroni nel layer peggiore). **LA MISURA: il criterio che E41 aveva indicato, costruito per bene, e PEGGIO di entrambi gli incumbent a OGNI seme** -- `PRED` **3,812991** contro `D0C` 3,614540 e `COACT` 3,509543, cioe `+0,198` e `+0,303`. **E ha funzionato MECCANICAMENTE**: la deviazione fra semi di `PRED` e 3,8x piu stretta di quella di `COACT`, il router e davvero diventato meno dipendente dalla fetta -- **la costruzione e riuscita e il modello e peggiorato.** Ipotesi REGISTRATA COME IPOTESI e non come claim: la prevedibilita si paga in COPERTURA (neuroni predetti dalla stessa direzione portano informazione simile, quindi 16 gruppi coerenti coprono meno direzioni d'uscita di 16 diversi); testabile su CPU col rango efficace delle righe di `down_proj` tenute, E42 NON lo misura. **IL NUMERO CHE SOPRAVVIVE ALLA SONDA: la deviazione fra semi della colonna fitted e `0,027`-`0,143` BPB cambiando SOLO il seme di calibrazione del router** -- la costante strumentale che mancava al programma, e risolve E41 da dietro: il margine con cui l'addendum B di E41 aveva "vinto" (`-0,013308`) e **0,09 sigma**; `COACT-D0C` su tre semi e `-0,104997 +- 0,129309`, **IRRISOLTO a 2 sigma per la seconda volta, esattamente come avevo registrato contro me stesso**; `RAND-D0C` e `+0,842` = 5,9 sigma. **Sopravvive SOLO la distinzione grossolana: una partizione strutturata batte una casuale di 0,84 BPB, e l'ordine FRA le strutturate sta dentro il rumore.** In piu il `COACT` di E41 non era nemmeno lo stesso oggetto: ricostruito su 32 sequenze invece di 8 legge `3,565004` allo stesso seme contro `3,583800`, cioe il budget di costruzione della partizione lo muove di `-0,018796`, **piu del margine 0,013308 con cui aveva vinto**. Predizioni **0 HIT / 1 split / 4 MISS**: l'unica cosa giusta e quella registrata contro me stesso. **EFFETTO SUL PROGRAMMA: NESSUNO, deliberatamente** -- E42 non ha verdetto registrato, quindi non chiude la porta che E41 aveva lasciato socchiusa e non rafforza niente; **la richiesta T4 resta poggiata su E37, E38, E40, esattamente dov'era prima che E41 ed E42 girassero.** Quello che le due sonde insieme hanno comprato e uno STRUMENTO: nessun confronto fra partizioni sotto ~0,3 BPB e risolvibile a tre semi su questo harness) --- (precedente: 2026-09-12 (**E41 addendum B -- **RITIRO IL VERDETTO DI E41 PER MIA STESSA REGOLA REGISTRATA: la cella e `VERDICT-UNRESOLVABLE` e E41 NON rafforza la richiesta T4.** Il blocco qui sotto (scritto e committato a `d136f98` APPOSTA prima che l'addendum stampasse un numero) concludeva `PARTITION-IS-NOT-THE-LEVER` e contava E41 come QUARTA misura della strada chiusa: **sbagliato, le misure restano TRE (E37 conversione, E38 selettore, E40 forma dell'attenzione).** L'addendum B, registrato a `fadeb42` prima che il runner esistesse, ha rifittato tutti e sei i router al budget con cui la barra di banda era stata DAVVERO misurata (32 sequenze/seed 42424 invece delle 8 che il mio brief aveva registrato) e **l'ordine E CAMBIATO**: `COACT` **3,583800** passa primo e **scavalca la barra 3,597108 di -0,013308** -> per la regola registrata nel brief la cella e UNRESOLVABLE, le due tabelle stanno entrambe e nessuna promuove l'altra. **`G-E41D`, noto-positivo, SCATTA a SETTE cifre**: `D0C` rifittato a 32/42424 legge `3,597108` contro il `3,597108` pubblicato di E38, diff `-4,73e-07` -- **il budget di calibrazione era TUTTA la discrepanza di `+0,207238`, e niente altro**, quindi la tabella di B e quella misurata sullo strumento noto-buono. **LA COLONNA PIU INFORMATIVA E il GUADAGNO da 4x di calibrazione, che NON e uniforme e NON e rumore**: `RAND` non guadagna nulla e anzi PERDE 0,0048 (una partizione casuale non ha struttura che un router lineare possa imparare -- esattamente cio che un braccio nullo deve fare), `STRIPE` 0,055, `CONC` 0,089, i due bracci derivati da D0c 0,207 ciascuno, **`COACT` 0,303, il massimo dei sei** -- cioe l'asse della PREVEDIBILITA letto direttamente, e **la mia predizione 4 registrata diceva proprio che `COACT` avrebbe guadagnato di piu per quel motivo: e successo, e poi ha scavalcato `D0C`, che la stessa predizione diceva non sarebbe successo.** **DISPERSIONE: insufficiente, ed e per questo che la cella e irrisolta e non semplicemente rovesciata.** A 8 sequenze `PERM fitted` riproduceva `D0C fitted` a sedici cifre; a 32 no (`+3,070e-04`), unico stimatore di rumore sul percorso fitted, e il margine di `COACT` e 43x quello -- ma quello limita il rumore da RINOMINA, mentre l'asse che ha rovesciato l'ordine e la SCELTA DELLA FETTA DI CALIBRAZIONE, su cui questa sonda ha due punti, un seme ciascuno, zero repliche. **COSA SOPRAVVIVE A ENTRAMBI GLI STRUMENTI** (e non dipende da chi vince): al 6,25% il migliore dei sei raggruppamenti perde fra l'**85,3%** (B) e il **92,0%** (run 1) della finestra denso->caso e manca la barra di usabilita di **2,58 BPB**; all'1,17% tutte e dodici le celle stanno sopra il caso; oracolo e router attenibile ordinano i sei in modo DIVERSO **e l'ordine attenibile e a sua volta instabile nel budget del router**, quindi entrambi i modi economici di prezzare un raggruppamento ingannano; **e cio che paga e la PREVEDIBILITA, non la massa.** Predizioni sull'intera sonda: **1 HIT / 4 split / 5 MISS su dieci.** Dovuto promosso in cima: una partizione ottimizzata per la PREVEDIBILITA (`COACT` e un proxy 1-D grezzo e gia batte il controllo sullo strumento buono) e **la dispersione della colonna fitted sulla fetta di calibrazione, mai replicata**) --- (precedente: 2026-09-12 (**E41 `PARTITION-IS-NOT-THE-LEVER`: **SEI modi di raggruppare gli STESSI 8.960 neuroni nei 256 gruppi del carve, e al ritmo che E40 misura comprato da 50 tok/s a dieci miliardi (6,25%, `k=16` di 256) il miglior selettore ATTENIBILE e la famiglia di etichette che usavamo gia**: `D0C` legge **3,804346** BPB e NESSUNA partizione la batte (barra di banda 3,597108, barra di usabilita 1,005039 = il braccio `QO512+V52` di E22 che fece 126/160). A `k=3` (1,17%) **tutte e dodici le celle sono SOPRA il caso** (4,069819): la scelta perfetta sul migliore dei sei raggruppamenti e +0,0561 peggio di una monetina. **IL RISULTATO CHE VALE PIU DEL VERDETTO: L'ORDINE SI ROVESCIA FRA ORACOLO E ROUTER.** `COACT` (neuroni che si accendono insieme) ha il MIGLIOR soffitto dei sei (oracolo 3,095548, -0,354 su `D0C`) e il PEGGIOR divario di realizzazione (0,791455 contro 0,354880): ordine per oracolo `COACT<CONC<D0C<STRIPE<RAND`, per fitted `D0C<CONC<COACT<STRIPE<RAND`, rho 0,60. **Chi prezza i raggruppamenti col loro oracolo -- la misura economica -- sceglie `COACT` e spedisce il terzo migliore**: E14 sez.3 e il `ROUTER-IS-NOT-THE-CONSTRAINT` di E37 su un TERZO asse. E lo swing del raggruppamento DECADE col ritmo: stesso selettore, `D0C` contro partizione casuale, oracolo **+0,453019 a k=16** e **+0,031031 a k=3** -- l'attivazione cala 5,3x, il valore di raggruppare bene cala 14,6x; **il valore della partizione ha il picco dove ce l'ha la selezione, al 25%, e il ritmo che la scatola si permette sta MOLTO sotto il ginocchio.** **IL CONTROLLO PIANTATO E ANDATO VOID e NON e stato rifatto passare**: `G-E41C` chiedeva che rinominare i 256 gruppi (`PERM` = `D0C` relabellato) muovesse l'oracolo di ESATTAMENTE zero -- a `k=3` si (`+0,000e+00`), a `k=16` no (`-1,221e-03`). **L'addendum A lo ha diagnosticato e la MIA prima ipotesi era SBAGLIATA ed e registrata come sbagliata**: la massa di gruppo e BIT-IDENTICA sotto rinomina (diff max `0,000e+00` su 344.064 righe), la causa sono **DUE righe su 344.064 con massa float32 esattamente UGUALE a cavallo della soglia** -- `topk` rompe il pari per indice, permutare i nomi lo ribalta, e due scelte ribaltate (layer 1 e 11) diventano tutto l'`1,22e-3`. Riparazione VERIFICATA (accumulo in float64: 0 pari, 0 ribaltamenti) e **`e38_oracle_ceiling.py` NON toccato** perche i suoi numeri devono continuare a riprodursi. Pavimento di jitter `1,2e-3`: 290-660x sotto ogni differenza d'oracolo riportata, 9,5x sotto il confronto piu stretto. **TRE numeri pubblicati di E38 si riproducono GRATIS** (oracolo k=3 `4,131817`, oracolo k=16 `3,449466`, swing di raggruppamento a k=3 `+0,031031`). **SECONDO DIFETTO, MIO**: la sez.2 del brief ha registrato la calibrazione a 8 sequenze/seed 424242 mentre la barra di banda (`D0C` fitted 3,597108 di E38) veniva da un router fittato su 32/42424 -- ogni numero fitted qui e `+0,207238` fuori dal suo riferimento, il **29% dell'intero spread fra le partizioni**; la DIREZIONE del verdetto regge (il vincitore E il controllo) e l'addendum B rifa i sei router a 32/42424 con `G-E41D` che pretende 3,597108 +-0,001 come noto-positivo, sotto la regola run-2 di E36 (non puo promuovere). Predizioni **0 HIT puliti / 2 split / 3 MISS** (avevo registrato `PARTITION-HELPS` a 2,90 contro me stesso; il modello che sbagliava tre volte prezzava la partizione per COME LA MASSA E DISTRIBUITA, mentre cio che decide la colonna attenibile e il divario oracolo->fitted, cioe quanto il raggruppamento e PREVEDIBILE da `x`, di cui non avevo alcun modello). **QUARTA misura indipendente della stessa strada chiusa** (E37 conversione, E38 selettore, E40 forma dell'attenzione, E41 raggruppamento): la richiesta T4 in COMMUNICATION.md -- H1, il carve ALLENATO invece che APPLICATO -- resta in piedi, rafforzata da una misura in piu e non da un argomento.) --- (precedente: 2026-09-12 (**E40 `ATTENTION-LEVERS-EXHAUSTED`: il lato VELOCITA dell'obiettivo e RAGGIUNTO (100 tok/s a dieci miliardi veri, con la FFN ANCORA VIVA) e nello stesso colpo la CONVERSIONE POST-HOC e CHIUSA per sempre.** Tre bracci, **ognuno esattamente 9.999.220.736 parametri** (`F` risolto per centrare l'intero di E36 con `F%256==0`), tirando ogni leva d'attenzione che questo motore ha DAVVERO: `k/v` da 8 teste a 2, e il rango di `q/o` da 512 a 128. **`R128` (`F=49152`, NKV=2, rango 128) legge `112,73`/`106,44` tok/s a k=3 e passa i 100 tok/s all'`1,65%`/`1,74%` di attivazione: PRIMO artefatto del programma che e insieme un 10 B vero, sopra il traguardo OTTIMO, e che sta ancora calcolando qualcosa nella FFN** (l'`R512` di E39 arrivava a 100 solo a `k*~0`, cioe con la FFN SPENTA). Scala dei pavimenti: E36 `58,1` -> E39 `~93` -> `NKV2` `~127` -> **`R128` `~163` tok/s, cioe `2,8x` sul termine che E34 aveva chiamato IL MURO**, e **dimezzare le teste `k/v` e la leva grossa piu economica del programma** (i parametri tornano dentro `F`, il conteggio non si muove). **MA LA DOMANDA CHE IL PROBE DOVEVA DECIDERE E' L'ALTRA, e la risposta e NO**: a 50 tok/s il budget compra **`6,0 +-0,3%`** di FFN attiva (5,99% run 1 registrata, 6,18% run 2, 6,49%/6,00% sulle mediane) **e E38 aveva misurato che al 6,25% il miglior selettore attingibile legge `3,597` BPB contro un denso `0,767595` e un caso `4,069819` -- l'86% della strada verso il caso. Il ritmo che la scatola si permette e il ritmo a cui scegliere vale qualcosa NON SI SOVRAPPONGONO, e il divario e `4x`, non una leva.** Quindi: E37 ha mostrato che la conversione fallisce al ritmo che la velocita chiede, E38 che nessun router la salva, **E40 che la scatola non puo essere portata dove scegliere pagherebbe. LA CONVERSIONE POST-HOC E' FINITA COME STRADA PER QUESTO OBIETTIVO; resta ALLENARE DENTRO IL FORMATO, l'asse di H0, ed e cio che va comprato con le sessioni T4.** **LA BANDA NON PORTA INFORMAZIONE e lo dico in prima riga: il bordo che avevo scelto (6,00%) passa IN MEZZO alla misura**, e la mediana della run 1 (6,49%) cade a un centesimo dalla mia predizione registrata di 6,50% -- **non la prendo** (E14 sez.6 vieta di promuovere una statistica post-hoc), la statistica registrata e la MEDIA. **`G-E40A` E' ANDATO VOID SUL PRIMO BUILD E SI E PORTATO VIA UN BRACCIO MIO**: `ALL` (con la testa LEGATA) leggeva `10.133.438.464`, sopra di `V·D` esatti, perche `synth_export.py:207` registra da molti esperimenti che un modello a testa legata fa girare la testa come l'EMBEDDING fp32 -- il 52% del token a S05 -- e quindi `--head ternary` forza `tied=0`. **Avevo sbagliato sul legare la testa DUE volte in direzioni opposte: legare risparmia PARAMETRI e mai PESI ADDEBITATI, e su questo motore non e una leva ma un rallentamento di 5x sulla testa.** Addendum A l'ha ritirato per `R128`, un braccio PIU FORTE (base 0,218 G contro 0,252 G), pushato prima che l'oggetto esistesse; **predizione 1 scorata MISS e il gate NON e stato rifatto girare fino a passare.** `G-E40C`, il controllo piantato (`NKV2` addebita strettamente meno di `R512`, quindi DEVE essere piu veloce), **scatta in entrambe le run a tutti e cinque i `k`**. **RISPOSTA A E39 sez.8 punto 2: la penalita del fattorizzato e la FORMA, non il numero di chiamate** -- `R128` tiene le chiamate matvec fisse a 145 e perde lo stesso il 12% per peso addebitato (ritmi base misurati: rango 512 a NKV 8 `38,9`, rango 512 a NKV 2 `40,5`, rango 128 `35,6` G-pesi/s). **Predizioni 0 HIT puliti / 2 a meta / 3 MISS**, e il `k*` per i 100 tok/s l'ho mancato NELLA DIREZIONE BUONA (4,2 gruppi contro i <=2 registrati: **quattro volte piu FFN viva a 100 tok/s di quanta ne avessi predetta**). Dispersione CATTIVA e riportata (due outlier di prima ripetizione, 48,9% e 34,9% di spread peggiore). **Il pavimento adesso e per il 61,5% LA TESTA** (134.217.728 su 218.103.808; attenzione 30,8%, router 7,7%): **la testa e piu grossa di tutta l'attenzione messa insieme e non e mai stata sondata su quest'asse.** **PESI SINTETICI: e la meta VELOCITA.** Dettaglio: `probes/E40_ATTENTION_LEVERS_EXHAUSTED.md`, ledger sez.50.) --- (precedente: 2026-09-12 (**E39 `RANK-BUYS-SPEED`: LO STESSO IDENTICO 10 B, MESSO ALTROVE, e per la prima volta il PAVIMENTO sta a 100 tok/s.** `A10B-R512` -- `D=4096 F=48128 L=16`, carve 256 (gruppo 188), `q_proj`/`o_proj` scritti come FATTORI a rango 512 -- ha **esattamente 9.999.220.736 parametri, l'intero di E36, e lo stesso totale per layer (608.174.080): le due forme differiscono in UNA cosa sola, DOVE sta il peso.** Costa `419.430.400 + 36.962.304·k` addebitati, **1,75x meno di `A10B` a k=3**, e legge **78,46 tok/s** contro i **46,34** di `A10B` ricronometrato nella stessa sessione (**1,693x dentro la sessione**). **LA BANDA NON E IL RISULTATO, e lo dico in prima riga**: la cella sta a 1,5 tok/s dal bordo e le due run lo scavalcano (run 1 registrata 78,46 -> `RANK-BUYS-SPEED`, run 2 a ordine rovesciato 80,55 -> `TEN-B-NEAR-HUNDRED`), e **la regola run-2 di E36 -- pushata a `9819226` e adottata dal brief PRIMA che esistesse una qualunque delle due run -- dice che vince la run 1 e che la run 2 non puo promuovere niente** (pendenze d'accordo entro la dispersione: 10,4% contro uno spread peggiore del 19,3%). **L'ho applicata nella direzione che va contro di me, che e l'unica in cui una regola cosi significa qualcosa** -- tanto piu che la run registrata e proprio quella che ha FALLITO la sua predizione 5 (controllo a -7,2% da E36, fuori dal +-5%), per cui il rapporto fra sessioni che il runner ha stampato (`1,61x`) e RITIRATO. **IL NUMERO CHE SOPRAVVIVE A ENTRAMBE LE RUN E IL PAVIMENTO: `10,059` ms e `9,996` ms, d'accordo allo `0,62%` mentre la pendenza discorda del 10,4% -- cioe `99,7` tok/s con la FFN a zero. E34 aveva misurato il pavimento di T10 a `20,03` tok/s con la FFN cancellata e aveva chiuso i 50 tok/s a quella forma; QUESTA forma ha il pavimento sul traguardo OTTIMO, e tiene dieci miliardi di parametri mentre lo fa.** `k*` per 100 tok/s = `-0,064` / `+0,005` gruppi, cioe **ZERO: i 100 tok/s costano tutta la FFN.** **META COSTRUTTIVA: a 50 tok/s il budget compra `2.046`/`2.298` neuroni FFN attivi per layer su 48.128 = `4,25%`/`4,78%`, contro l'`1,17%` di `A10B` -- ~4x l'attivazione a parita di parametri, comprata con ZERO parametri** (non basta a fare un buon modello: E37 legge `4,029398` BPB all'1,17% e E38 mette il picco del valore della selezione al 25%). **COSTO NUOVO, MISURATO CON UN CONTROLLO E NON CON UN FIT: il cammino FATTORIZZATO consegna il `4-6%` di pesi addebitati al secondo IN MENO di quello denso** (`R0`, stessa forma di file con `q/o` densi: 43,359 vs 41,607 e 45,469 vs 42,716) -- il meccanismo e nel log dell'esportatore, rango 512 su `q/o` porta le chiamate matvec per token da **113 a 145**, e **l'invarianza di throughput addebitato di E25, misurata dentro una banda dell'1,54%, NON si estende qui.** Il `0,80` di gather penalty di E36 e sparito ma per due cause OPPOSTE (base -12% per peso, gruppo +19% per peso a 188 contro 180): registrato come `~1,0 +-0,1`, non una terza cifra. `G-E39C`, il controllo piantato, **SCATTA in entrambe le run** (rango 4096, addebitato 1,4698 G contro 0,9330, legge 30,80 < 46,47 e 31,66 < 48,74: un rango che costa di piu E COSTATO di piu, quindi il cronometro e sul cammino fattorizzato). Tutti e quattro gli artefatti passano il layout v4 indipendente di E1 a tolleranza zero -- **e quel predittore aveva SCATTATO DAVVERO in mattinata**, rifiutando il primo export carve+rango perche `layout_bytes_v4` non aveva il termine di rango. **Predizioni 1 HIT / 4 MISS, e condividono UN errore: avevo prezzato l'attenzione fattorizzata come se muovere il 49% di pesi in meno comprasse il 49% di tempo in meno. Ne compra il 41,5% (17,2 -> 10,06 ms), perche quello che resta si muove ~12% piu lento per peso -- e la legge della Fase 61 (un microbench non compone) che arriva sul lato VELOCITA: un modello da tavolino non attraversa un cambio di TIPO di matvec.** **PESI SINTETICI: prezza una FORMA, non dice che un modello di quella forma sia buono -- ma e la forma che H0 ha dimostrato ALLENABILE (`q/o` ternari rango 512, tf 28 -> 111).** Dettaglio: `probes/E39_THE_SAME_TEN_BILLION_PUT_SOMEWHERE_ELSE.md`, ledger sez.49.) --- (precedente: 2026-09-12 (**E38 `SELECTION-IS-DEAD`: il ramo "costruiamo un router migliore" e CHIUSO, e deciso su CPU senza un'ora di GPU.** Ho dato al carve un **oracolo** -- la scelta perfetta per ogni token, il tetto di QUALUNQUE router che ordini i gruppi per massa -- al ritmo che la velocita di E36 richiede (`k=3` di 256, **1,17%**): legge **`4,131817`**, cioe **`0,062` SOPRA la linea del caso** (`4,069819`), contro un denso fp32 a `0,767595`. **Nemmeno barare alla perfezione salva l'1,17%.** **Onesta sul margine: la banda voleva l'oracolo entro `0,50` da un selettore casuale e il numero e `0,4584` -- presa per QUATTRO CENTESIMI, e il documento lo dice in prima riga.** **Il risultato vero e la FORMA, non la cella: il valore di scegliere bene ha un PICCO.** `oracolo - a caso` = `0,000` (k=256), **`2,5546` (k=64, 25%)**, `0,7838` (k=16), `0,4584` (k=3), `0,0258` (k=1): **al 25% scegliere bene vale 2,5 bit per byte, all'1,17% quasi niente, e il ritmo che ci serve sta SOTTO il picco.** E **la posizione del ginocchio e una proprieta del SELETTORE e non della forma** (MISS della pred. 5: a k=64 l'oracolo ha pagato il **18%** del suo danno finale, il selettore casuale l'**83%**) -- nessuna sonda di questo ramo l'aveva mostrato. **CORREGGE UNA LETTURA MIA, non un numero:** in E37 avevo letto l'addendum B come "un router che trova il 71% della massa e uno che ne trova il 6% danno lo stesso modello"; **quella misura viveva in una finestra di `0,5941` BPB (denso ternario -> caso) mentre in fp32 ce ne sono `3,3022`, e il divario fitted-vs-random che E38 misura a k=64 e `2,37` -- QUATTRO VOLTE l'intera finestra di E37**, che quindi non poteva risolvere un effetto-router di nessuna taglia. Il verdetto di E37 (`S15-K3 = 4,029398`, `SPARSITY-DEGRADES`) e le sue predizioni restano come misurati: cambia cosa quella banda AUTORIZZA a dire, e E37 sez.5 porta adesso un rimando in avanti. **Apre un parametro di progetto mai prezzato: il RAGGRUPPAMENTO.** Lo stesso selettore statico legge `2,2119` sulle etichette D0c e `4,5833` su una partizione casuale a k=64 -- **`2,37` BPB di oscillazione dal solo modo di raggruppare**, e ogni carve di questo programma ha usato UNA famiglia di etichette. Tre gate scattati, `G-E38A` a **esattamente `0,00e+00`** (a k=E la maschera e l'identita per tutti e quattro i selettori) e `G-E38B` a `-3,59e-08` dall'ancora E22. Predizioni 3 HIT / 1 MISS / 1 a meta. **Nessun timing: fp32 in PyTorch, per costruzione, e l'assoluto NON e confrontabile con E37.** Dettaglio: `probes/E38_IS_THERE_ANY_SELECTOR_AT_ALL.md`, ledger sez.48.) --- (precedente: 2026-09-12 (**H0 `GATE PASSES`: la CONVERSIONE non arriva (E37, qui sotto) ma ALLENARE DENTRO IL FORMATO SI', ed e l'unico ramo post-hoc mai provato.** `QO512-TB` -- `q_proj` e `o_proj` di tutti i 28 layer sostituiti da fattori TERNARI a rango 512 (88.109.056 parametri allenabili), straight-through, tutto il resto intatto. **Top-1 con contesto dato: `28 -> 111` su 160, contro una barra registrata a `>= 48`. BPB `2,812226 -> 0,825358` dove il donor intatto legge `0,767595`: il 97,2% del danno e sparito.** E l'ha fatto con il **12,5% dell'allenamento che aveva chiesto** -- il job si e fermato da solo al tetto di 2,8 h allo **step 500 di 4000**, ~4,1 M token su 16 M. **DUE ANCORE PIANTATE girate oggi con lo stesso strumento sulla stessa fetta congelata**: l'oggetto PRIMA dell'allenamento legge `2,8122382` / free 1 / tf 28 (riproduce a sei decimali il `2,812226` / 1 / 28 pubblicato da E22) e il donor INTATTO legge `0,7675949641` / **160/160** / **160/160** / mean rank 1,00 (contro lo `0,767595` pubblicato, **differenza 3,6e-08**) -- uno strumento che legge perfetto su un modello sano e 28 su uno rotto ha il diritto di dire 111 su questo. Dati di allenamento != dati di valutazione, e il file lo registra da solo con un campo che si chiama `eval_slice_sha256_NOT_THIS` (stream `part=calib`, seed 90011, il seed di calibrazione 42424 di E23 DELIBERATAMENTE evitato). **`G-H0e`, il controllo piantato del TRAINER, e scattato sul run vero**: primo aggiornamento APPLICATO allo step 5 dopo 4 rifiuti del `GradScaler` (scala 65536 -> 4096, esattamente le quattro dimezzature attese di uno scaler fp16 freddo), master mossi 8,2e-06, **0 microbatch non finiti** -- cioe i due difetti che avevano ucciso il run 1 (il gate che leggeva i master senza verificare che uno step fosse stato APPLICATO, e la cache del quantizzatore che tirava `A` e `B` fuori dal grafo lasciando allenare solo `s`, 512 numeri su 88,1 M, **che avrebbe FABBRICATO il null di H0**) sono corretti e VERIFICATI in produzione. **L'ASSE CHE NON SI E MOSSO, ed e quello di cui parla la frase dell'obiettivo: la generazione libera resta a `8/160`**, banda `AT-FLOOR`, **SOTTO** i `15/160` che E22 aveva misurato sulla configurazione fp32. **Il modello indovina il token successivo e NON scrive** -- la stessa spaccatura che questo programma incontra a ogni scala, e H0 non la risolve. **LIMITI: 1,5 B; `q/o` soltanto; niente carve, niente router, niente FFN ternaria, niente testa ternaria** (esclusi apposta perche il gate misurasse UNA cosa sola), quindi l'oggetto allenato NON e quello che spedisce -- `STACK` legge `4/160` post-hoc ed e non testato sotto allenamento. **I pesi non sono ricostruibili** (il T4 non esiste piu, e a differenza del bundle iniziale non sono funzione di input fissati): due copie verificate per hash, `s1/results/h0_kaggle_run2/h0_trained.npz` e `D:\_ktmp\h0_run2_weights\`, 352.967.686 B, sha256 `6d3fd3e3d0a15f5c...`. **Dovuto, in ordine di costo: (1) i 3500 step che restano; (2) `STACK` sotto allenamento; (3) IL CARVE ALLENATO INVECE CHE APPLICATO** -- E37 ha misurato che applicarlo costa `+0,5537` BPB col router provatamente irrilevante, H0 ha misurato che il formato si puo allenare, e **nessuno ha ancora messo quei due fatti nella stessa run.** Dettaglio: `probes/H0_TRAINING_INTO_THE_FORMAT.md`, proposta T4 sez.11.) --- (precedente: 2026-09-12 (**E37: per la PRIMA VOLTA in questo ramo UN SOLO artefatto porta sia un cronometro sia una BPB, e la meta QUALITA del ritmo di E36 e adesso misurata su pesi VERI.** Donor `S15` (Qwen2.5-1.5B) davvero allenato, spinto al ritmo di attivazione ESATTO che E36 richiede (`k=3` su 256 gruppi da 35 neuroni = **1,17%**, la stessa frazione di `A10B`): verdetto **`S15-K3 = 4,029398` BPB -> `SPARSITY-DEGRADES`**, cioe **+0,5537 sopra il denso** (3,475706) e **0,0404 SOTTO la linea del caso** (4,069819). Batte il lancio della moneta per quattro centesimi di bit e NON e un modello. **Il cambio, sullo STESSO file: `k=3` legge 107,84 tok/s contro i 30,52 del denso (3,53x) al prezzo di 0,00716 BPB per ogni tok/s guadagnato.** **Il risultato vero pero e il CONTROLLO, ed e caduto sul MIO stesso gate.** `G-E37D` era scattato in modo convincente (il router fittato recupera il **71,3%** della massa dell'oracolo contro il **6,1%** di uno random, 28/28 layer) -- l'addendum B, pushato a `341b505` PRIMA che il controllo esistesse, chiedeva se scattare volesse dire qualcosa. Esportato il gemello identico in tutto tranne il router (sintetico, `--carve-seed 26`): alla `k` che decide il sintetico e **MIGLIORE di 0,0088 BPB** -> **`ROUTER-IS-NOT-THE-CONSTRAINT`**. **Un router che trova il 71% della massa e uno che ne trova il 6% producono LO STESSO MODELLO: il danno e QUANTI neuroni sopravvivono, non QUALI.** La vittoria di `G-E37D` e reale e INERTE -- lo specchio di E14 sez.3, un risultato di RANGO messo al posto di un risultato di SCORE che non implica. **La curva e una scogliera e poi un altopiano**: gia a `k=64` (25% di attivazione) si pagano **+0,4517**, e da `k=16` a `k=1` tutto sta fra 3,99 e 4,03 -- **un cambio di 40x nell'attivazione muove la qualita meno della dispersione fra vicini**, e l'unico punto SOPRA la linea del caso e `k=32`, in mezzo. Ipotesi registrata PRIMA (addendum B sez.5, e il controllo la rafforza perche anche la curva sintetica non e monotona): **una FFN PARZIALE e peggio di QUASI NESSUNA FFN** -- `down_proj` somma i sopravvissuti, buttarne la maggior parte non restringe l'aggiornamento verso zero, lo rende SISTEMATICAMENTE SBAGLIATO della grandezza originale. Non testabile qui: `ffn_carved` blocca `k >= 1`. Difetto di plumbing ESCLUSO (`--carve-dump`: esattamente `k` gruppi distinti in range per layer per token, 0 malformati). **Predizioni: 3 HIT / 3 MISS**, e le MISS puntano tutte nella stessa direzione -- avevo registrato `SPARSITY-DESTROYS` (sotto il caso) e un ginocchio dolce a 25% di attivazione: modellavo il danno come funzione di QUANTA massa FFN manca, e non lo e. MISS anche la predizione 4: il denso a S15 legge **30,52 tok/s**, NON supera i 50 come avevo scritto. HIT la 5: i pesi addebitati scendono di **3,746x** e la velocita sale di **3,533x** = **0,943** di quello che promette un conteggio di pesi. **Tre controlli incrociati sono venuti gratis**: il denso legge **47,116 G-pesi/s**, DENTRO l'inviluppo base 47,2-48,5 di E36 misurato su un'altra forma e su rumore; `K256` (byte-neutro) e **-7,15%** rispetto al denso contro il **-6,80%** del controllo piantato di E26, cioe **la macchina del carve costa ~7% prima che cada un gruppo**; e il fit `tempo = 9,16 ms + 0,140 ms/gruppo` da una **penalita di gather 0,74-0,81 contro lo 0,80 di E36** -- due forme, due scale, rumore e pesi veri, e un peso di FFN costa ~1,25x uno di attenzione in entrambi. Il costo del fold e risultato **esattamente zero**: `e37_dense_nf.bin` e **byte-identico** all'ancora pubblicata (sha256 `5d50e377...`), perche il JSON dell'ancora non ha la chiave `fold` e precede il flag. Due dei miei gate erano SBAGLIATI come scritti e sono stati riscritti PRIMA di qualunque numero (addendum A, `8cc405a`): `G-E37A` chiedeva parita a 1e-6 ignorando che `ffn_carved` riordina la somma di `down`, e `G-E37B` confrontava con un riferimento che il carve NON PUO eguagliare (`qwen_export.py:390` rifiuta `--quant carved` senza `--fold none`). Terzo gate, `G-E37C`, nominava un campo che nessuno scriveva: NON allargato, reimplementato dall'INTESTAZIONE del file. **LIMITE REGISTRATO: un donor, una scala, un set di label, una famiglia di router. Questo NON dimostra che non esista un 10 B sparso -- dimostra che questa CONVERSIONE non ne produce uno. La leva che resta e ALLENARE DENTRO IL FORMATO, non convertire ancora.** Dettaglio: `probes/E37_WHAT_THE_SPEED_COSTS_A_TRAINED_MODEL.md`, ledger sez.47.) --- (precedente: 2026-09-12 (**E36: per la prima volta esiste un artefatto che E' DAVVERO ~10 B e che il motore fa girare al ritmo dell'obiettivo.** `A10B` -- `D=4096, F=46080, L=16, V=32768`, carve a 256 gruppi da 180 neuroni -- **9.999.220.736 parametri, 5,11 GB, verificati in TRE modi dal file stesso** (conteggio dell'esportatore == conteggio ricalcolato dall'INTESTAZIONE DEL FILE >= 9,9 G, e byte su disco == il layout v4 che E1 aveva scritto indipendentemente). Con **540 dei suoi 46.080 neuroni FFN per layer attivi per token (1,17%)** legge **49,96 tok/s** contro una barra di 50,0: verdetto `TEN-B-NEAR-FIFTY`, **quattro centesimi sotto**, e questo documento NON lo chiama un passaggio pulito. **Il file pesa 10,77x quello che costa un token** -- e il primo artefatto del ramo in cui TAGLIA e COSTO sono due numeri diversi. **Il numero robusto e l'INCROCIO, misurato due volte con l'ordine dei bracci ROVESCIATO: `k* = 2,867` e `2,861`, cioe 516 e 515 neuroni su 46.080, 1,12% di attivazione -- d'accordo a UN neurone su quarantaseimila**; e entrambe le run sono strettamente monotone in `k` (il partner di RANGO che E14 sez.3 pretende). **La run 2 ha stampato 51,50 sulla cella del verdetto e NON le e stato permesso di promuoverlo**: la sua regola era stata pushata a `9819226` PRIMA che esistesse, la sua unica domanda era se la pendenza in `k` dipende dall'ordine dei bracci, e le pendenze coincidono (+6,2%, dentro la dispersione 12-19% delle ripetizioni) -> verdetto invariato. Rovesciare l'ordine ha reso la pendenza PIU RIPIDA, non piu piatta: l'ordine fisso non la stava fabbricando. **Il risultato vero e la MISS della predizione 3.** Separando il costo invece di mediarlo: la **base** (attenzione+testa+router) legge **47,2-48,5 G-pesi/s = l'inviluppo pieno**, mentre **un gruppo di carve marginale legge 37-39, cioe `0,80`** -- quindi `GSZ=180` NON ha ricomprato il gather e **lo `0,85` di E31 a 11.520 B era ottimista**. **`tempo = 17,2 ms + 0,93 ms per gruppo`: un peso di FFN costa `1,25x` un peso di attenzione, e ogni tabella che li ha prezzati uguali e ottimista sulla sua meta FFN.** Corollario sull'obiettivo: la sola base tappa `L=16` a **~58 tok/s con una FFN gratis**, quindi **100 tok/s a larghezza 4096 comprano ~8 layer e nessuna FFN -- e un problema di ATTENZIONE, non di FFN.** **Registrato prima della run e ripetuto qui: questa e solo la meta VELOCITA. I pesi sono rumore. "Far funzionare un LLM gia addestrato" NON e soddisfatto da questa sonda.** Sonda `probes/E36_TEN_BILLION_AT_FIFTY.md`, ledger sez.46.) --- (precedente: 2026-09-12 (**E35: per la prima volta i 50 tok/s sono RACCHIUSI FRA DUE MISURE invece che avvicinati da sotto.** A larghezza 4096 e profondita ridotta, con la FFN al pavimento, `L=16` legge **56.16 tok/s** e `L=12` **71.35**, mentre `L=24` legge 40.35: il verdetto e `DEPTH-IS-HALVED`, `L* = 19.33`. **L'INVILUPPO: a 50 tok/s questa scatola si permette `0.9451 G` di pesi ATTIVI ADDEBITATI per token** -- ed e gia comprensivo del gather, perche ogni braccio e un artefatto carvato che legge un gruppo per layer. A `L=16` restano **`+0.1120 G` = ~10 gruppi di carve = 560 neuroni su 14.336 = 3.9% di attivazione** per una FFN. Il controllo piantato scatta a **-0.8%** sull'artefatto stesso di E34. **Predizioni 3 HIT / 2 MISS, ed entrambe le MISS sono difetti del MIO modello**: il coefficiente per-layer registrato e il pavimento puro mentre i bracci portano il residuo del carve, quindi `L*` legge 19.33 come registrato e 18.56 comprensivo di residuo -- **scorato come registrato, la lettura che mi dava ragione NON e stata presa**; e la linearita si e rotta al 5.1% sul braccio piu profondo, che e un costo FISSO per layer che un modello a pesi addebitati non puo vedere. **Cosa autorizza: una SPECIFICA -- ~16 layer a 4096 con ~560 neuroni FFN attivati per layer; un 10 B totale a quella forma vuole ~46.000 neuroni per layer, cioe ~1.2% di attivazione.** Sonda `probes/E35_THE_FIFTY_TOK_S_ENVELOPE.md`, ledger sez.45.) --- (precedente: 2026-09-12 (**E34 `FLOOR-IS-THE-WALL`: alla forma LETTERALE di T10 i 50 tok/s non sono raggiungibili su questa scatola NEMMENO CANCELLANDO TUTTA LA FFN.** Nessuno l'aveva mai misurato: da E18 a E33 abbiamo picchiato sulla FFN perche e nove decimi del peso, ma il decimo che resta non entra nel budget. Con la FFN ridotta a un solo gruppo T10 legge **20.03 tok/s**, `2.50x` corto; il pavimento attenzione+testa e **2.1475 G addebitati / 1.0786 GB mossi** per token, cioe **`2.297x`** il budget addebitato per 50 tok/s, e contro il tetto MISURATO di E30 **un kernel PERFETTO su quel pavimento legge 33.65 tok/s, ancora `1.486x` corto.** La residenza non lo chiude: 16 MB di L3 sono `0.016 GB` del pavimento mentre il buco e `0.353 GB` -- **22x l'intera L3**. Tutti e tre i gate scattano e quello portante e `G-E34C`: il numeratore di questa sessione (46.74 G-pesi/s) predice il `T10-K4` gia misurato da E26 a `+5.4%` e cade entro l'**1.3%** dal numeratore packed indipendente di E28. **Aritmetica da tavolino sul tetto misurato: 31 dei 48 layer di attenzione entrano nel budget, il 65% della profondita, con la FFN a ZERO.** Quindi la leva rimasta e la FORMA DELL'ATTENZIONE -- profondita, rango su `q/o`, teste KV -- non la FFN. Predizioni 5 HIT / 0 MISS; run 1 ANNULLATO perche un braccio senza `--carve-k` su un file carvato eredita il `k` del file e il mio controllo 'denso' era il pavimento travestito -- **l'hanno beccato entrambi i controlli piantati**. Sonda `probes/E34_THE_FLOOR_UNDER_THE_FLOOR.md`, ledger sez.44.) --- (precedente: 2026-09-12 (**E33 `LOCALITY-PARTIAL`: la leva del carve grossolano e `1.12x` a S15 e NON RISOLVIBILE alla forma dell'obiettivo.** Terza sforbiciata alla stessa mia affermazione, la seconda da una MISURA: `2.01x` -> `1.25x` (rileggendo il motore) -> **`1.1164` misurato a S15, `1.0432` a T10 -- sotto la linea dell'`1.06` in tutti e cinque i jackknife.** Solo il **46%** del guadagno previsto si materializza. Il MECCANISMO regge (ordine monotono `E256 < E64 < E16` a entrambe le forme), la TAGLIA e l'utilita no. Il modello da tavolino sovrastimava perche addebitava l'intera lettura della FFN alla grana di `down`, quando `gate`/`up` -- due matrici su tre, due terzi dei byte -- erano GIA nel tratto piatto. Per la sezione 6 del brief stesso: **l'aritmetica di E31 sez.6.1 non puo piu prezzare niente.** Due gate miei non potevano scattare come scritti (il router e ADDEBITATO e si restringe con `E`) e l'ho calcolato e registrato in un addendum PRIMA di esportare un solo artefatto. **Run 1 ANNULLATO e tenuto: la contesa era il mio stesso lavoro di documentazione sulla stessa scatola, e la banda non era robusta a quella ripetizione.** Sonda `probes/E33_IS_DOWN_THE_WHOLE_COST.md`, ledger sez.43.) --- (precedente: 2026-09-12 (**E32 `ACTIVATION-COSTLY`: il kernel su cui era costruita tutta la storia lato-motore NON sopravvive al suo stesso protocollo.** E14 aveva squalificato il proprio verdetto nella propria sezione 1 (senza `--seqlen` il motore legge UNA sequenza da 12.288 token invece di 24 documenti da 512) e tre sonde si portavano dietro la ri-esecuzione. Rifatta al protocollo di E1: **il SEGNO si e ribaltato** -- `dBPB(--lutblk)` a 1.5 B passa da `-0.016961` (un GUADAGNO) a **`+0.048991`**, cioe `2.4x` la linea COSTLY di E14 e ~`10x` il `sigma_seed`, e i due bracci sono deterministici quindi anche questo confronto e generoso. Tutti e tre i gate scattano, ancora di E1 riprodotta a **2.8e-6**. **Il kernel operativo torna al packed: `0.639` del tetto invece di `0.909`, 46.16 G-pesi/s invece di 61.64, `11.46x` dall'obiettivo invece di `8.05x`** -- banner di correzione IN CIMA a E28 e E30. **Nessuna conclusione architetturale si muove** (predizione 6 registrata PRIMA del numero): un kernel perfetto a T10 legge sempre 6.83 tok/s e 50 tok/s vogliono sempre `7.32x` l'intera banda -- anzi il motore e a `1.57x` dal muro, quindi c'e PIU margine di kernel di quanto E30 avesse scritto. **Meta costruttiva: `--lut-group 32` costa `+0.011739`, un quarto -- la cosa cara e la GRANA DELLA SCALA di attivazione, non l'int8 -- e non e MAI stata cronometrata: e la misura piu preziosa rimasta.** Predizioni 3 HIT / 2 MISS, entrambe mie sulla cella che conta. Sonda `probes/E32_THE_OWED_PROTOCOL.md`, ledger sez.42.) --- (precedente: 2026-09-12 (**E31 `GATHER-COSTS`: il budget di E30 valeva per una lettura DENSA, e tutte le strade che restano leggono un SOTTOINSIEME.** Una riga FFN di T10 alla volta consegna il **58%** del ritmo denso NEI BYTE CHE HAI CHIESTO, quindi il budget vero e **`0.840 G`/token = il 7.93% del token denso**, non `1.45`. Ma la penale non e permanente: **0.842 a 8 KB, `0.944` a 32 KB (sedici righe consecutive)**. Cioe: **la sparsita a grana di neurone costa il 42% del budget, a blocchi da 32 KB costa il 6%, e fra i due disegni ci sono `1.63×`** — contro l'`1.24×` che era tutto quello che restava al numeratore. **CORRETTO LO STESSO GIORNO**: avevo scritto che il carve legge UNA RIGA alla volta e che quindi un gruppo piu grosso vale `2.01×`, “la leva piu grande del programma”. Leggendo il motore invece del mio riassunto: `gate`/`up` sono group-major e **gia contigui** (26.9 KB a S15), e la grana fine e **`down`**, che il motore PRETENDE `MK_PACKED_T` con `PT_BLK = 64` — **una cache line per neurone selezionato**, quindi `GSZ × 64` = 2.240 B a S15. **`down` da solo si porta tutto il costo di localita del carve**, e la leva del carve grossolano vale `1.25×` a S15 e `1.14×` a T10, **non `2.01×`**: la direzione regge, la taglia e ritirata (ed e aritmetica da tavolino su una tabella misurata, non una misura). Un gather ingenuo a grana di cache-line consegna `0.197` della macchina. Con la meta residente di E30 l'obiettivo e finalmente prezzato in una riga: **~32 M residenti gratis + `<= 0.84 G` streammati a grana di riga, oppure `<= 1.37 G` a blocchi da `>= 32 KB`.** Tre run ANNULLATI e tenuti sul repo (il controllo camminava sull'array degli offset, quindi si muoveva con la variabile che doveva controllare, e stava al denominatore di ogni rapporto; poi le ripetizioni stavano tutte nella stessa finestra di tempo finche non le ho interlacciate) — **la barra al ±15% non e mai stata allargata**. Sonda `probes/E31_WHAT_A_GATHERED_BYTE_COSTS.md`, ledger sez.41.) --- (precedente: **E30 `AT-THE-WALL`: adesso il tetto e MISURATO e il numeratore e FINITO come direzione di ricerca.** Banda di lettura di questa scatola **`36.30 GB/s`** (controllo piantato a **12.5×**, dirupo L3 a 16 MB dove Probe-3 lo aveva messo); `T10-LUTBLK` ne usa **`0.909`**, `T10-PACKED` `0.639` — quindi il “core-bound” di E10 era giusto PER IL SUO kernel e a muoversi e stato il kernel, non la macchina. **Un kernel PERFETTO alla forma dell'obiettivo legge 6.83 tok/s**: e il tetto duro di tutto il lavoro di kernel su questa macchina, e siamo gia a **1.10×** da li. **50 tok/s vogliono `265.64 GB/s` = `7.32×` l'INTERA banda della macchina** e permettono **`1.45 G` pesi MOSSI per token** — il budget allargato da E28 (`1.2328`) e gia l'**85% del limite fisico**. La meta costruttiva: un nucleo residente da 16 MB costa lo **0.18%** di un token da 20 ms, cioe e GRATIS, quindi la specifica onesta dell'obiettivo e **~32 M residenti + ≤ 1.45 G streammati per token** — lo split thinking/knowing di `SCALEUP_ARCHITECTURE.md` scritto come disuguaglianza. `G-E30B` riproduce l'artefatto AL BYTE e cosi facendo ha beccato la tabella del mio stesso brief (mancavano i bias q/k/v). Il nome della banda sfiora il confine: col tetto piu generoso mai letto su questa scatola il rapporto e `0.809`, ma **l'81% della banda totale resta**, margine ≤ `1.24×`. **Voce demota: il guardiano per-matrice `--lut` non e piu “la prossima build” — compra EFFICIENZA di byte, non byte.** Sonda `probes/E30_IS_THE_ENGINE_AT_THE_WALL.md`, ledger sez.40.) --- (precedente: **E28 `CONTAINER-COSTS`: il NUMERATORE non e mai stato una costante.** Da E10 in poi ogni sonda ha attaccato il DENOMINATORE di `tok/s = (G pesi attivi/s) / (G pesi attivi/token)` e il numeratore era fisso a 49.9 G-pesi/s. Alla forma dell'obiettivo, col kernel di E13, legge **`61.64` -- `1.235x`** -- e **la leva CRESCE con la forma**: 0.5 B `1.217x` -> S15 `1.258x` -> T10 `1.338x`, l'unica cosa in questo programma che si trasferisce VERSO L'ALTO. Scatola scarica, due ancore E25 passate, noto-positivo a 1.633. **Ma `donor_engine.c:1437` rifiuta `--lut` su qualunque contenitore che non sia `quant==2`, quindi nessun braccio a rango o a carve puo usare il kernel che ha prodotto il numero** -- vale `1.34x` ed e la prossima sonda. Il divario dall'obiettivo scende **10.6x -> 8.60x**: primo movimento dal lato motore da E10, e lontanissimo dal bastare. Sonda `probes/E28_DOES_THE_KERNEL_TRANSFER.md`, ledger sez.39.) --- (precedente: **E29 `RESIDUAL-RANKS`: il controllo contro il MIO stesso risultato migliore. E27 aveva battuto `L21-LAST`, che pero cancella il layer 27 -- il SECONDO piu attivo dei 28 -- quindi il confronto era truccato in mio favore. Contro un controllo ONESTO (sette layer a caso dalla stessa fascia interna, tre semi) l'ordine registrato TIENE -- **`MAXRES-IN` 78 < `RANDMID` 89.0 < `MINRES` 113, a costo addebitato IDENTICO** -- ma **il margine vero e `+24`, non `+56`**: E27 sez.3 e la proposta T4 sez.10 sono corretti IN CIMA, la raccomandazione resta. Cinque gate di replica a `abs_diff 0.000e+00`. Post-hoc, NON un gate: la massa di residuo buttata ordina 17 coppie su 20, e l'eccezione vera separa le due storie -- **`LAST` butta MENO residuo di `MAXRES-IN` ed e 21 token peggio, quindi gli ESTREMI contano oltre la massa**. Sonda `probes/E29_DOES_THE_RESIDUAL_RANK.md`.) --- (storico: **E26 parte B `UN PESO RACCOLTO COSTA PIU DI UNO STREAMMATO`: l'invariante di E25 NON si estende al carve, e questo corregge OGNI tabella di budget del programma.** Sonda `probes/E26_WHAT_AN_ACTIVATED_WEIGHT_COSTS.md`. **E25 aveva misurato throughput addebitato dentro l'1.54% su bracci che differivano del 15%** -- e quello che autorizza a leggere i tok/s da un conteggio di pesi. **Col carve la banda si apre: 12.54% a `T10`, 18.00% a `S15`.** Il modello di addebito `3*D*GSZ*k` e quindi OTTIMISTA e se lo portano dietro E18 sez.31, E19, E23 sez.7, E24 ed E27. **E il CONTROLLO PIANTATO stavolta si RISOLVE**: a `k = E` il carve tiene ogni gruppo quindi e byte-neutro a `-0.47%`/`-0.71%` REGISTRATI, e legge `-6.80%`/`-4.98%` -- **il costo PROPRIO della macchina del carve e `-6.36%` a `T10` e `-4.30%` a `S15`**, pagato prima che cada un solo gruppo e addebitato a NIENTE in ogni tabella scritta finora. (E25 al confronto: il costo proprio del percorso FATTORIZZATO era sotto la risolvibilita. Il rango e quasi gratis, il carve no.) **Il contenitore invece e gratis**: `S15-PACKED` contro `S15-DENSE`, stessi pesi in container untagged e in `quant==4` senza carve, leggono `1.0056` contro una predizione a byte di `1.0000`. **IL TRIGGER REGISTRATO NON SCATTA e la predizione primaria e comunque FALSIFICATA**: avevo registrato "se i bracci profondi `K16`/`K4` scendono sotto 40 G pesi/s allora un peso raccolto costa piu di uno streammato" -- leggono 41.41 e 41.67, **non scatta, e non pretendo che sia scattato**. Era la statistica sbagliata: **la perdita e una U, non una pendenza** (`T10` rispetto al suo denso: `k=256` 0.936, `128` 0.902, `64` **0.882**, `16` 0.960, `4` 0.967), perche a `k` piccolo la FFN e il 5.7% del token e **il pavimento denso MASCHERA l'effetto proprio dove il trigger guardava**. La banda era la statistica giusta ed era gia nel brief: non l'avevo legata all'alternativa. **Isolando l'efficienza propria della FFN raccolta** (post-hoc, per E14 sez.6 NON diventa un gate): a `S15` degrada da `0.943` a `k=E` a **`0.564` a `k=16`, intervalli DISGIUNTI**; a `T10` da `0.921` a `0.825`, intervalli sovrapposti; e `T10-K4` da `[0.390, 1.339]`, **non risolvibile, in tabella solo perche nessuno citi lo 0.621**. **LA LUNGHEZZA DELLA RIGA E LA VARIABILE, e la predizione 5 (registrata come direzione) arriva su tre confronti indipendenti**: una riga tenuta e 768 B a `S15` e 2048 B a `T10`, e `S15` soffre di piu sulla banda (18.00 vs 12.54), sull'efficienza a `k=16` (0.564 vs 0.825, disgiunti) e sullo scarto dalla predizione a byte (17-24 punti sotto contro un `T10` profondo che la SUPERA). **Quindi e un costo di LOCALITA, e ha una leva attaccata: un carve piu grossolano -- meno righe ma piu lunghe a parita di frazione attivata -- dovrebbe recuperarne una parte, ed e una modifica all'ESPORTATORE, non al kernel.** **Cosa costa alle tabelle**: il `K156` di E24 a `T10` passa da ~8.1 a **~7.4 tok/s**, il `FLOOR-MIN` di E27 da 9.37 a **~9.0**, e in generale alle profondita che il budget permette a `T10` (`k~2..18`) la correzione sul TOKEN e il 3-5% ma **sull'ALLOWANCE DELLA FFN e ~17%: il budget permette un sesto di gruppi in meno di quel che dice il modello di addebito.** **Nessun verdetto si muove** -- E19, E24 ed E27 sono negativi o condizionati alla qualita e una correzione che peggiora il carve li rafforza tutti e tre. **VALIDITA, come vincolo e non come nota a pie' di pagina.** Il primo tentativo (gioco aperto, 5.82 core su 12) era VOID. Qui: il testimone e stato RICOSTRUITO -- il vecchio guardava il MAX di un campione corto a una barra del 12% e ha rifiutato un desktop scarico tre volte di fila, quindi ora guarda la MEDIA e **`--selftest` e un controllo piantato sullo strumento stesso** (quieto 12.1%, sei processi che girano 61.5%: SCATTA sul noto-positivo E accetta la scatola quieta), con la barra spostata a 25% **per derivazione fra quelle due popolazioni** e la derivazione, ammissione inclusa, scritta sopra `IDLE_BAR`; in-run 10.6/5.8/6.5/8.0% medio. **Ma lo strumento VERO e l'ANCORA E25, e per poco non falliva: `S15-PACKED` legge 26.79 contro i 29.70 pubblicati = `-9.81%` contro una barra del `+-10%`, passata per 0.19 di punto**, con entrambi i bracci `S15` in calo monotono sulle tre ripetizioni -- deriva TERMICA, non contesa. **Percio questo record e un record di RAPPORTI**: nessun tok/s assoluto ne esce, e **la forma assoluta della predizione 3 ("ogni braccio dentro 45-55 G pesi/s") e NON TESTABILE qui** -- il controllo denso stesso legge 43.12 dove E25 leggeva ~49.9 -- quindi il suo fallimento **non lo conto come prova**. Il rapporto regge la deriva: ricalcolando ogni rapporto APPAIATO dentro ogni ripetizione non cambia niente (`T10-K256` 0.9312 appaiato contro 0.9320, disaccordo massimo 0.4%). **Dovuto**: un braccio che gira la selezione e poi usa tutti i gruppi (separa overhead fisso da gather), il braccio a granularita grossa, un re-run a scatola fredda, e **l'ancora E25 come equipaggiamento standard di ogni sonda di velocita.** **6.79 tok/s INTATTO.**)** Previously: (**E27 `FLOOR-IS-NOT-ENOUGH`: E24 aveva indicato la leva -- non la FFN ma il PAVIMENTO (attenzione + testa) -- ed E27 lo attacca su tutti e due i suoi assi, LARGHEZZA (rango su `q/o` e su `k/v`) e PROFONDITA (togliere interi layer), **senza carve e senza router da nessuna parte**. Dodici bracci, `VOID: none`, 4345 s, zero timing. (`probes/E27_THE_FLOOR_NOT_THE_FFN.md`, brief `808ba43` pushato prima che il runner esistesse.) **Nessuno dei due assi apre la forma dell'obiettivo, e il motivo e che i due assi sono ANTI-CORRELATI con la qualita proprio dove il budget comincia a essere soddisfatto: ogni braccio che sta dentro `0.998 G`/token e rotto, ogni braccio che regge qualitativamente lascia il budget lontano un fattore 5-8.** `G-F0`/`G-F1`/`G-F2`/`G-F3`/`G-F4` SCATTANO tutti; **`G-F1` e quello che conta: `L28-PASSTHROUGH` -- la macchina della profondita girata con `n=0` -- e BIT-IDENTICA a `base`** (il codice reindicizza `self_attn.layer_idx` e riscrive `num_hidden_layers`, e sbagliare uno dei due e una risposta sbagliata SILENZIOSA, non un errore). **LA FRAZIONE DI RANGO NON SI TRASFERISCE: `r/D = 1/3` legge 144 token teacher-forced, `1/8` ne legge 56, `1/16` ne legge 42.** E' un dirupo, non una pendenza: non esiste nessuna frazione liscia da portare a `D=4096`, e questo CHIUDE in negativo l'item lasciato aperto da E21 sez.8. **E i `k/v` sono il peggior affare del tabellone**: a `T10` sono `[1024,4096]`, NON quadrati, quindi il rango risparmia solo se `r < 819` e a `r=96` toglie lo 0.8% dei pesi (`1.5436 -> 1.5312 G`) al prezzo di **76 token teacher-forced** (`160 -> 84`) -- **tenerli in fp32 in E21-E24 non era contabilita di comodo, era evitare un taglio da 76 token per un risparmio da errore di arrotondamento.** **IL RISULTATO VERO DI E27 NON STA SULL'ASSE DEL BUDGET: `L21-MINRES` legge `113/160` contro i `57/160` di `L21-LAST` alla STESSA profondita, lo STESSO conteggio di parametri e lo STESSO costo addebitato -- 56 token guadagnati SOLO scegliendo QUALI sette layer togliere**, quattro volte la banda 0-15 registrata in anticipo. Il profilo `MINRES` (definito nel brief PRIMA del run: media sulle posizioni di calibrazione di `||out-in||/||in||`) **non e monotono**: 19.05 al layer 0, minimo 0.2836 al 15, e **risale a 0.7618 al 27** -- percio "togli gli ultimi `n`", la mossa di default, rimuove alcuni dei blocchi PIU attivi. I sette piu piccoli sono **una banda centrale CONTIGUA, 12-18**, ed e esattamente quella che `L21-MINRES` toglie. **Si puo buttare un quarto di un transformer addestrato quasi gratis, se si buttano i layer giusti, senza carve, senza router, senza rango e senza riaddestrare niente.** Ma la profondita non e gratis: `L14-MINRES` a meta layer crolla a `54/160`, e il dirupo fra 21 e 14 non e stato bisecato. **ALLA FORMA DELL'OBIETTIVO, con la FFN lasciata DENSA, la leva-pavimento da sola compra questo: `base` 4.68, `QO-512` 4.93, `QO192+KV96` 5.44, `L24-LAST` 6.17, `L21-MINRES` 7.75, `FLOOR-MIN` 9.37, `L14-MINRES` 15.21 tok/s** -- e l'unico sopra 10 legge `54/160` e non e un modello. **Per arrivare a 50 da `L21-MINRES` la FFN dovrebbe scendere a `-0.684 G`, che non e un numero: il pavimento DA SOLO e 1.68x l'intero budget.** A 36 layer con `q/o` denso, **cancellare la FFN del tutto porta a 29.7 tok/s, non a 50.** E `FLOOR-MIN` -- il pavimento piu aggressivo che E27 sappia comporre, l'unico braccio con spazio vero per la FFN (`k = 17.3` di 256, 6.8% di attivazione) -- legge `38/160`: **la configurazione che sta nel budget e la configurazione che non funziona.** L'ALTERNATIVA REGISTRATA NON SCATTA. Due predizioni su cinque mancate e una mancata di 4x in magnitudine, entrambe le mancate informative e trovabili solo perche i numeri erano depositati prima. **Niente timing, niente esportato: 6.79 tok/s e i 4.70 tok/s di `T10` restano INTATTI.** **H0 resta l'unico ramo aperto del donor, ed e senza router e senza carve per costruzione, quindi ne E23 ne E24 ne E27 lo toccano.**)** Previously: (**E24 `DEPTH-RECOVERS`: il `ROUTER-COSTS` di E23 era un artefatto della PROFONDITA, e alla profondita che il budget permetteva da sempre la configurazione composta si RIPRENDE -- con un router vero, addebitato ed ESEGUITO.** (`probes/E24_THE_DEPTH_THE_BUDGET_PERMITS.md`, brief `b78ce4d` pushato prima che il runner esistesse; nove bracci, `VOID: none`, 3842 s, zero timing.) **`G-U1` non riproduce E23 dentro `1e-9`: lo riproduce a `abs diff 0.0` su ENTRAMBE le ancore** (`QO512+V52-LINEAR` `1.2014771810176486`/9/102 e `QO512+V52-ORACLE` `1.0050386831300866`/15/126), il che dimostra inerte sia il refactor di `fit_routers` in `fit_routers_multi` sia la fattorizzazione low-rank calcolata UNA volta invece di otto. **LA CURVA: `k=133` 102 -> `k=139` 105 -> `k=148` 110 -> `k=156` 118 token teacher-forced, monotona su entrambe le metriche.** **`K156-LINEAR` -- `1.0590 G` attivi, DENTRO `0.982-1.060 G` di E18 sez.31, col router da `11.0 M` addebitato e girato -- legge `118/160`, un token sotto CHEAPER: e la PRIMA configurazione di questo programma che sta nel budget, non usa NESSUN oracolo, ed e COMPARABLE sull'ordinamento. L'ALTERNATIVA REGISTRATA SCATTA.** **E il divario che costa il router si DIMEZZA con la profondita: oracolo meno ridge e 24 token a `k=133` (126-102) e 12 a `k=156` (130-118).** **Il router si GUADAGNA i suoi 11.0 M**: `K148-STATIC` -- stessi 148 gruppi per ogni token, nessun punteggio per-token, addebitato a `1.0118 G` senza router -- legge `96/160` contro 110, quattordici token sotto e perfino sotto il 102 di `k=133` pur tenendo 15 gruppi in piu: **il TERZO ESITO registrato NON scatta e la tabella delle profondita regge.** `K148-LOG1P` legge `111` contro `110`: **un token e un null, registrato in anticipo e NON promosso** (E14 sez.6). **Tre predizioni prese, due mancate -- e la 2 e la mancata istruttiva: avevo detto 103-106 argomentando dalla curva di E19 (`0.14` BPB su 48 punti di carve) e ha letto 118. La pendenza vera qui e ~1.8 token per punto di attivazione, circa cinque volte quella media: il danno del carve NON e lineare nella profondita, ACCELERA scendendo -- cioe l'errore sta nella direzione che peggiora il trasferimento, non lo migliora.** **E IL CONTRAPPESO, che non va sepolto: questo e il conteggio ASSOLUTO del donor da 1.5 B contro un budget assoluto, e la ricetta NON si trasferisce alla forma dell'obiettivo come FRAZIONE.** Trasposta a `T10` sulla linea MISURATA di E25 (49.9 G pesi/s), `QO512+K156` fa `6.14 G` = **~8.1 tok/s, non 50**; e al contrario, a `T10` il budget da 50 tok/s lascia alla FFN **`k ~ 2...8` di 256 = 0.8%-3.2% di attivazione** (pavimento non-FFN `0.9899 G` a `r=512`, `0.7885 G` a `r=256`; con `q/o` denso o a `r=D/3` e IMPOSSIBILE), dove E24 ha misurato la qualita al **51.9%-60.9%** e l'ha vista SALIRE con la profondita. **Quindi la leva che apre la forma dell'obiettivo e quella che abbassa il PAVIMENTO non-FFN -- rango su `q/o`, poi la testa -- non piu profondita di FFN**, ed e l'item aperto di E21 sez.8 (`r/D=1/3` validato solo a `D=1536`) e di E22 sez.8. **E ogni tok/s qui e CONDIZIONATO**: assume l'invariante di throughput addebitato di E25 anche per un braccio SCOLPITO, che e esattamente cio che misura E26 parte B -- **e E26 parte B non ha ancora un record valido** (primo tentativo su macchina contesa, VOID). **Free-running ancora AT-FLOOR (15/160 contro pavimento 12): si e mosso solo l'ordinamento.** **Niente timing, niente esportato: 6.79 tok/s e i 4.70 tok/s di `T10` restano INTATTI.**)** Previously: (**E25 `RANK-PAYS-WHAT-IT-WEIGHS`: `engine.c` HA un matvec fattorizzato, e girato contro PyTorch, e alla forma dell'OBIETTIVO la leva del rango rende esattamente i pesi che toglie.** (`probes/E25_WHAT_THE_RANK_COSTS.md`, brief `e506c7b` pushato prima che il runner girasse.) **L'item dovuto da E21 sez.8, ripetuto da E22 sez.8, E23 sez.9 e dalla proposta T4 sez.6, e SALDATO**: E21 ed E22 misuravano il taglio di rango calcolando `A.B` e installando una matrice DENSA -- il modo giusto di prezzarne la QUALITA e nessun modo di prezzarne il COSTO -- quindi ogni conseguenza del rango sui tok/s era ARITMETICA. `donor_engine.c` guadagna una specie `mat_t` fattorizzata (`y = A(s . (B x)) + b`, due chiamate agli STESSI kernel, intermedio di taglia `r`) e **`quant == 3`, un contenitore TAGGATO** dove ogni matrice porta il proprio `int32` kind: senza di quello le configurazioni misurate qui non erano nemmeno ESPRIMIBILI (E21/E22/E23 lasciano `k/v` in **fp32** mentre tagliano `q/o`, e un flag `quant` globale non lo sa dire) -- **fino a oggi ogni risultato di rango era un numero PyTorch senza artefatto eseguibile dietro.** **`G-E25P` SCATTA: l2 relativo peggiore `6.445e-04` contro una barra di `2e-3`, accordo top-1 `1.0000` su 10/10**, sul `QO512-TB` di E22 -- lo stato da cui H0 parte -- contro un riferimento PyTorch che monta `h0_qat.TernaryLowRank`, lo stesso modulo che il T4 allena. **`G-E25a`** tiene fermi i percorsi vecchi: motore patchato e non patchato danno logit BIT-IDENTICI. **E il CONTROLLO PIANTATO e il cuore della parte B**: a `r = D/2` una proiezione quadrata muove ESATTAMENTE gli stessi pesi (`2.D.(D/2) = D^2`), quindi quello che perde e il costo PROPRIO del percorso fattorizzato -- e legge **`-0.43%` a `T10` e `-2.46%` a `S15`, con dispersioni 7.3% e 6.4%: non risolvibile da zero.** Le 56/96 regioni OpenMP in piu per token costano meno di quanto questa macchina sappia misurare. **Alla forma dell'obiettivo (`T10`, 10.60 G attivi, le dimensioni dell'"es 10B") il rango 512 su `q/o` legge `5.36` contro `4.70` tok/s = `+14.12%`, contro una predizione a byte di `+12.86%`: l'alternativa registrata ("non batte del 8%") NON scatta.** **E l'invariante vale piu di ogni rapporto: quattro bracci `T10` i cui ritmi differiscono del 15% consegnano throughput addebitato dentro una banda dell'1.54% (49.59-50.36 G pesi/s)** -- il motore converte pesi attivi in tempo a un ritmo che NON guarda come sono disposti, quindi il rango compra velocita attraverso UN solo meccanismo, meno pesi, ed e per questo che `2.D.r` e l'addebito giusto. **La linea di budget di E18 sez.31 (`0.982-1.060 G`/token per 50 tok/s) mappa, al throughput MISURATO di `T10`, su 47.0-50.7 tok/s: costruita su forme piu piccole, abbraccia 50 alla forma dell'obiettivo, misurata per la prima volta.** **E la distanza, misurata invece che derivata: `T10` gira a 4.70 tok/s, l'obiettivo e 50, quindi 10.6x -- e il rango su `q/o` la porta a 9.3x.** Il rango su `q/o` vale `1.2 G` dei `9.6 G` che devono uscire; **la FFN e `8.45 G` di `T10`, nove decimi del problema che resta**, ed e esattamente dove puntano il carve di E19 e il router di E23/E24 -- **E24 e ancora da girare.** Due predizioni prese, una parziale, due mancate (la 3 SOPRA la banda, nella direzione buona). **Niente sulla qualita: i pesi della parte B sono RUMORE, `T10` e una FORMA e non un modello, e `T10-R512` e `r/D = 1/8` dove E21 ha validato solo `1/3`.** **`6.79 tok/s` resta INTATTO** -- quello e il 7.072 B vero, un altro artefatto.)** Previously: (**E23 `ROUTER-COSTS`: il carve SOPRAVVIVE a un router vero, la COMPOSIZIONE no -- e il pezzo che non sopravvive e esattamente quello per cui avevo chiesto le settimane di T4.** (`probes/E23_A_REAL_ROUTER.md`, brief `25bde22` pushato prima che il runner esistesse; sette bracci, `VOID: none`, 2694 s.) **OGNI numero di carve mai pubblicato qui -- D0, D0c, E19, E22 -- usava un router ORACOLO** che legge la massa di attivazione VERA e poi tiene i top `k`. E23 lo sostituisce con una ridge per layer dall'input del blocco a `sqrt(massa di gruppo)`: forma chiusa, zero gradienti, zero tuning, `11.0 M` addebitati al budget. **Sul carve DA SOLO tiene il 91% del valore dell'oracolo** (`V52-LINEAR` tf `110/160` contro 117, retention `0.9067`, `ROUTER-HOLDS`) -- **meglio di quanto avessi registrato, e l'ALTERNATIVA REGISTRATA SCATTA: il carve NON e un artefatto dell'oracolo.** **Ma sulla configurazione COMPOSTA tiene il 71%** (`QO512+V52-LINEAR` tf `102/160` contro 126, retention `0.7143`, `ROUTER-COSTS`, banda WORSE) **e quella composta e `QO512+V52`, cioe il bersaglio di `decisions/T4_HEALING_PROPOSAL.md`.** **LA SCOPERTA: la composizione CAMBIA SEGNO.** Sotto oracolo aggiungere l'attenzione low-rank GUADAGNA 9 token (117->126); sotto router vero ne PERDE 8 (110->102): oscillazione di 17. L'oracolo legge le attivazioni post-gate VERE, quindi il danno a monte non lo raggiunge; la ridge legge l'INPUT DEL BLOCCO, che il taglio di rango ha gia perturbato -- **i due tagli sono indipendenti sotto oracolo e ACCOPPIATI attraverso l'input del router sotto uno vero** (rango medio `2.03->5.15` composto contro `2.49->4.17` sul solo carve). **Quindi la lettura di additivita di E22 sez.6 era CONDIZIONATA ALL'ORACOLO**, e cinque misure di composizione ora non concordano: **nessuna legge di additivita, uno stack VA MISURATO.** **Seconda cosa, e sorprendente: `V52-STATIC` -- gli STESSI `k` gruppi per ogni token, scelti sulla media in calibrazione -- legge `99/160`, retention `0.76`, da un router che NON GUARDA IL TOKEN.** Tutta la decisione di routing token-dipendente vale **11 token** (110-99); il divario dal casuale e 68. La predizione 2 (STATIC sotto 40, argomentata da Probe-4) e MANCATA: **il falsificato hot-pool di Probe-4 riguarda i working set per-NEURONE, e la massa per-GRUPPO sui 256 gruppi di D0c NON e i.i.d. su questo donor.** E lo statico e GRATIS. **Terza cosa, aritmetica, e ribalta l'obbligo del verdetto: `k = 133` non e MAI stato la profondita giusta.** Col router addebitato il fisso e `354.5 M` (q/o r512 `88.1` + k/v `22.0` + testa `233.4` + router `11.0`), quindi la FFN puo pesare `627.5-705.5 M` = **54.28%-61.03% = `k = 139...156` di 256**, mentre `k=133` da `0.9551 G`, **2.8% SOTTO il pavimento del budget.** Era ereditato da E19, fissato prima che un router costasse qualcosa: **la profondita piu bassa che `ROUTER-COSTS` impone NON e una ritirata, e la profondita che il budget permetteva da sempre, ed e qualita gratis.** Se basti a recuperare i 5 token che mancano a `107` NON e predetto: e E24. **Tre predizioni prese, due mancate. Nessun timing, niente esportato: 6.79 tok/s resta ESATTO.** **E H0 NON e toccato: e senza router e senza carve per costruzione, scelta fatta apposta perche E23 non potesse invalidarlo.**)** Previously: (**E22 `FITS-BUT-NOT-IN-THE-FORMAT`: l'oggetto che STA nel budget e l'oggetto che FUNZIONA non sono lo stesso oggetto, e a separarli e il FORMATO.** (`probes/E22_DOES_CHEAP_COMPOSE.md`, brief `3f8465a` pushato prima ancora che il runner esistesse; nove bracci, `VOID: none`.) **TRE REPLICHE ESATTE prima di leggere qualunque braccio composto**: `G-S0` base, `G-S1` riproduce E21 (`0.8202837636996289`, free 26, tf 144) e `G-S2` riproduce E19 (`0.909440994415161`, free 12, attivazione `0.51953125`), tutte a `< 1e-9` -- possibile solo perche il runner IMPORTA `e21_rank.lowrank`, `e19_carve_rank.install` e `t2_rules.r3_actsearch` invece di reimplementarli. **META' BUONA: `QO512+V52` -- il rango 512 pesato sulle attivazioni su `q/o` PIU' il carve al 52% della FFN -- e la PRIMA configurazione mai derivata qui DENTRO il budget da 50 tok/s (`0.9441 G` attivi contro `0.982-1.060 G`, 3.9% sotto l'estremo basso) ED E' il donor modificato che ORDINA MEGLIO di tutti: `126/160` teacher-forced, SOPRA ogni testa ternaria di E20, `+0.237444` BPB, rango mediano `1.0`, top-5 su 148/160, per-prompt `[27,24,22,28,25]` UNIFORME. L'ALTERNATIVA REGISTRATA SCATTA.** **META' CATTIVA, ed e quella che conta: quell'artefatto e fp32, e il `0.9441 G` era prezzato in TERNARIO.** Messa nel formato in cui dovrebbe girare, la stessa configurazione muore: ternarizzare ENTRAMBI i fattori del rango-512 da `2.812226` BPB e **28/160** (la versione bilanciata; quella registrata nel brief, `1.5570` di errore relativo, ne da **1/160**), contro **`1.903569` dell'attenzione ternaria DENSA di T2b su un insieme di organi PIU' DIFFICILE (`k/v` inclusi)**: **+0.908657 BPB peggio**. Gli errori dei due fattori si MOLTIPLICANO -- layer 0 `q_proj`: denso ternario `0.8084`, rango-512 fp32 `0.3515`, due fattori ternari `0.9874`. E lo `STACK` completo legge BPB `3.947669`, free `5/160`, tf `4/160`. **Quindi: rango x precisione NON compone, e il percorso resta quello di E18 -- ALLENARE DENTRO il formato -- ma ora con un BERSAGLIO: attenzione low-rank + FFN scolpita.** **Lo smoke ha beccato un difetto nella sez.3 del MIO brief** (tutta la gamma dei valori singolari nelle COLONNE di A, mentre la regola ha solo una scala per RIGA: 77.6% di A va a zero) e **entrambe le costruzioni sono state girate e messe in tabella**, perche ripararne una registrata in silenzio e come ando storto il run 1 di E20. **ADDITIVITA': tutte e tre le composizioni di E22 sono SUPER-additive in BPB** (`+0.0429`, `+0.0641`, `+0.1462`) mentre l'unico dato di E21 era SOTTO-additivo per `-0.3425`: E21 componeva due tagli con lo STESSO obiettivo (errori sovrapposti), E22 compone rango con sparsita (meccanismi diversi). **Nessuna delle due e una legge; quattro composizioni non fanno una legge: uno stack VA MISURATO, su ENTRAMBE le metriche.** **E la sez.4a di E21 va INDEBOLITA**: "la BPB non ordina fra assi" veniva da UNA coppia; qui `V52` (`+0.141846`) contro la miglior testa ternaria (`+0.170414`) legge `117` contro `117` -- l'ordinamento ha tenuto al token. Resta: **puo fallire, quindi va CONTROLLATO, non assunto.** Tre predizioni prese, **due mancate, entrambe nella direzione FAVOREVOLE**. **Il router del carve e un ORACOLO** (soffitto, non risultato eseguibile), free-running `15/160` = il modello ANCORA non genera, **nessun timing e niente esportato: 6.79 tok/s resta ESATTO**, e sul 7B i due tagli lasciano `4.090 G` = 4.0x il budget: **la TESTA resta il vincolo nominato.**)** Previously: **E21 `ATTENTION-YES-HEAD-NO`: per la PRIMA volta un taglio strutturale e quasi GRATIS -- ma non sulla testa.** (`probes/E21_CAN_RANK_BUY_IT.md`, brief `24b8832` pushato prima di ogni braccio; `VOID: none`.) **Il RANGO non era mai stato misurato end-to-end qui** (D2 si era fermata agli spettri di tre layer). Ogni braccio sostituisce le matrici del donor col minimizzatore ESATTO di rango `r` dell'errore **pesato sulle attivazioni** `||(W-Wr)X||_F`, non `||W-Wr||_F`. **`QO-ACT-512` -- rango 512 su `q_proj`+`o_proj` di tutti i 28 layer -- costa `+0.052689` BPB e tiene il token del donor al PRIMO posto su 144 posizioni su 160 a contesto fisso, rango medio `1.16`, top-5 su 160/160.** E' **la modifica strutturale MENO dannosa mai misurata in questo programma** (la miglior testa ternaria di E20 costa `+0.170414`, il `V52` di E19 `+0.141846`) e **l'UNICO braccio in E18/E19/E20/E21 a leggere `RANK-IS-CHEAPER`**. La trappola di E20 (un prompt fortunato) e stata cercata e NON c'e: lo split per prompt e `[30,26,30,29,29]`, uniforme -- ed e il primo donor modificato che scrive testo CORRETTO (Parigi, 212/32 gradi F, un ramo Fibonacci funzionante). **`G-R2` regge per `-2.44`--`-3.14` BPB**: la SVD semplice su `q/o` a r=256 finisce **SOPRA la linea del caso** (`4.686939` contro `4.069819`), la stessa a pesi pesati legge `1.545880` -- **una matrice che NON e low-rank nello spazio dei pesi puo esserlo dove viene USATA** (energia pesata tenuta `0.8937` contro `0.3638` di Frobenius), seconda conferma dell'ablazione Hessiana di D4. **Ma la configurazione che il brief doveva testare FALLISCE: `BOTH-ACT-256` legge 48/160**, peggio di entrambe le meta separate (68 e 93) -- **ma la sua BPB BATTE la predizione additiva di `0.342498`**: le due metriche dissentono in direzioni OPPOSTE sulla stessa coppia, quindi "il danno si compone" vale per il RANKING e NON per la BPB. E l'alternativa registrata era agganciata a QUEL braccio e li non scatta (va detto cosi, non riciclato). **Due predizioni prese, due mancate.** **Tre asimmetrie da tenere: (1) l'attenzione tollera il rango, la TESTA no** (stesso rango, stesso run: `+0.0527`/144 contro `+0.2781`/101); **(2) la composizione e SOTTO-additiva in BPB (`-0.342498`) e SOTTO entrambe le parti nel ranking -- le due metriche dissentono in direzioni opposte; (3) la BPB NON ordina interventi tra assi diversi** (`H-ACT-256` e `R0H` di E20 stanno dentro `0.01` BPB e leggono 68 e 107 teacher-forced). **Il pavimento di E19 scende ma non si apre**: alla frazione di rango davvero validata (`r/D ~ 1/3`) `attn+head` sul Coder-7B va da `1.367 G` a **`1.127 G`, ancora 1.06-1.15x l'INTERO budget da 50 tok/s con la FFN a zero**. **La TESTA e il vincolo nominato**: `545 M` = 51-56% del budget di un 7B, resiste al rango, e nessuna leva mai misurata qui la rimpicciolisce. **Nessun timing, niente esportato: 6.79 tok/s resta ESATTO**, e `engine.c` NON implementa un matvec fattorizzato (due GEMV con intermedio di taglia `r`) -- finche non lo fa, nessun risultato di rango diventa tok/s. **Il bersaglio dello HEALING cambia: non piu un donor ternario al 70% per passo, ma ATTENZIONE LOW-RANK al 90%.**)** Previously: **E20 `RULE-EXHAUSTED-DRIFT-DOMINATES`: il vincolo NON e la regola -- e la metrica con cui lo abbiamo misurato era DUE cose.** (`probes/E20_RULE_OR_FORMAT.md`, brief `e5d438c` pushato prima di ogni braccio; run 1 VOID `6c7b7a4`.) **Otto teste ternarie, UN formato** -- ternario + UNA scala fp32 per riga, stessi byte, stesso kernel, `0.500000` B/peso, tutto il resto bit-esatto. **La testa ternaria MIGLIORE costruibile qui costa `+0.170414` BPB, sta `3.131810` SOTTO la linea del caso, e concorda col PROPRIO donor su 11 token su 160 -- SOTTO il pavimento `12/160`.** Nessun braccio raggiunge `RANKS`: **l'alternativa REGISTRATA non scatta**, e il qualificatore di E16 sez.9 ("solo 2 regole su 4") e **saldato su sei regole**. **Il gate ha beccato la PREMESSA del brief**: la sez.0 dava per scontato che qui si fosse sempre ternarizzato con `mean|w|` RTN, ma la regola SPEDITA e `r3_actsearch`, **pesata sulle attivazioni, testa inclusa** -- e **GPTQ era gia in casa** (T2: `R4` 4.299819, `R5` 2.027495, **la regola migliore mai misurata, NON implementata nell'exporter**). `G-Q2` ha reso VOID il run 1 e nel run 2 riproduce **due ancore pubblicate da due sonde diverse**: BPB a `1.09e-08` da T2b e **`9/160`** identico a E18. **r(BPB, accordo) = +0.6238, segno SBAGLIATO per la terza volta.** **E la banda `PARTIAL`, dovuta da E14 sez.5 item 3, arriva -- e si scioglie guardandola**: `R2H` fa `[1,3,32,2,3]` e `OPTH` `[2,2,32,3,3]`, cioe **UN prompt riprodotto INTERO** (32/32, 18 id distinti, non degenere) e gli altri quattro al rumore. **Quel prompt e l'unico senza nessun quasi-pareggio** (gap top-2 minimo `1.1630` contro `0.0081`-`0.2740` degli altri): **160 posizioni sono CINQUE prove da trentadue, n efficace = 5.** **Parte B, la scoperta piu grossa: tenendo FISSO il contesto del donor, OGNI testa ternaria mette il token del donor al primo posto sul 67-74% delle posizioni** (free-running: 6-26%). Il divario di **75-106 token e DERIVA autoregressiva**, e l'identita esatta `G-B1` regge **50/50**, quindi e aritmetica. Le famiglie si separano sul **rango medio**: regole data-aware `2.40`-`2.98`, weight-space `12.85`-`16.92` -- **l'obiettivo pesato preserva la GEOMETRIA dei logit, non il confine top-1/top-2 dove vive l'argmax**, ed e questo il meccanismo dell'inversione. La sopravvivenza segue il margine del donor: **94-98%** nel terzile alto, **38-47%** in quello basso. **Il percorso donor resta fallito** (70% per passo non sopravvive a 32 passi) e **nessun numero di velocita si muove: 6.79 tok/s resta ESATTO.** **Ma la frase va riscritta: non "non sanno scegliere un token", bensi "scelgono il token del donor due volte su tre e non sopravvivono alla terza".**)** Previously: **E19 `CARVE-DOES-NOT-RANK`: the structural escape E18 left open is closed, on BOTH halves.** (`probes/E19_DOES_THE_CARVE_RANK.md`, brief `3173edc` pushed before part B ran.) **Half one, arithmetic, published in the brief BEFORE the run: FFN-only carving cannot reach 50 tok/s at any depth, including deleting the FFN outright.** On the Coder-7B `attn+head` alone are **1.367 G** active weights against a 50 tok/s budget of **0.982-1.060 G**, so a zero-FFN 7 B still runs at **35.9-38.8 tok/s** -- and EVERY carve this programme has built (D0, D0c, Probe-4) is FFN-only. Projected to the goal's size on each donor's MEASURED attn+head share, a 10 B carries a **1.93-2.51 G uncarvable floor = 1.82-2.37x the whole budget** (19.6-27.4 tok/s with its FFN at zero). **Half two, measured: no carve ranks.** Seven arms, **oracle** router (it reads the true activation mass -- a CEILING, so a null here is strong): `base` and `FULL` hold **160/160**, then **`V52` 12, `S1` 7, `A0` 5, `N0` 6, `D10` 3 -- ALL AT-FLOOR.** The cell that matters is **`V52`: keeping 52% of the FFN -- the depth this donor's OWN budget requires -- costs +0.141846 BPB, sits 3.160 BELOW the chance line, and agrees with its own donor on 12 of 160 tokens, EXACTLY the constant-`\n` floor.** Controls at **zero error**: `FULL` token-identical to `base` (BPB diff 0.000e+00) and **`G-C2` reproduces D0c's four published BPBs to 0.000e+00 -- sixteen decimals**, which is what licenses a restated hook. Unlike E18 the ordering is NOT scrambled -- **r(BPB, agreement) = -0.8562**, the expected sign over 1.896726 BPB -- and **that sharpens it: the degradation is well-behaved and its FIRST usable point is already at zero information.** **And co-activation buys BPB but NOT ranking**: A0-N0 = -0.720513 BPB, 5/160 vs 6/160 (floor noise) -- so D0/D0c's decisions rest on a gap that carries no ranking signal. **Three called directions, three misses, for the FOURTH experiment running**; prediction 3 was argued from a TRUE mechanism (a carve leaves survivors bit-exact) and was wrong by 68 tokens -- **un tipo DIVERSO di danno non e un tipo MINORE di danno.** **Nessun timing preso, 6.79 tok/s resta ESATTO.** **Il fallimento non e una proprieta della ternarizzazione ma della MODIFICA POST-HOC di un denso preallenato in quanto tale.** Resta solo: allenare DENTRO il formato; sul ramo donor, solo lo HEALING e non provato.)**))))))))**)))

This is the map. Every row names the artefact that holds the detail; nothing here is a claim that
is not written up somewhere with its controls and its pre-registration.

---

## 0. Where the goal actually stands

| | status |
|---|---|
| **A pretrained donor executes on our runtime** | ✅ **YES** — Qwen2.5-0.5B, parity vs PyTorch `rel l2 2.8e-06`, top-1 `1.0000`; and since E1 the engine **scores the same BPB as PyTorch to `1.5e-05`** on both donors, so the quality numbers below are statements about the deliverable, not about a simulation |
| **at a size the goal is about** | ⚠ **7.07 B of REAL trained weights, since E7** (`probes/E7_REAL_LARGE_DONOR.md`) — 67% of the 10 B target, exported, loaded from a **30.46 GB** artifact (15× the largest this runtime had ever taken), parity `1.07e-05` / top-1 `1.0000`, and **160/160 greedy tokens identical to PyTorch**. Nothing bigger exists offline: `Qwen3-8B` and the A3B files are **1 MB config stubs** |
| **and generates its own text** | ✅ **YES, since E6** — `--generate`, greedy, real fp32 weights: **160/160 tokens identical to PyTorch's greedy trajectory** over five frozen prompts (`probes/E6_GENERATION.md`). Everything before E6 was **teacher-forced** — the next token always came from the corpus, and `--bench` feeds the engine a counter on purpose. **Not true of the ternary build**: it emits tokens, not language |
| **At the target speed** | ❌ **E26 parte B QUALIFICA l'invariante di E25: vale per il RANGO, NON per il CARVE.** La banda del throughput addebitato passa da `1.54%` (rango) a **`12.54%` a `T10` e `18.00%` a `S15`** quando i pesi sono RACCOLTI invece che streammati, e il controllo piantato a `k = E` -- byte-neutro a `-0.47%` REGISTRATO -- legge `-6.80%`: **la macchina del carve costa `-6.36%` di suo, prima che cada un gruppo.** Quindi `3*D*GSZ*k` e ottimista: alle profondita che il budget permette a `T10` la correzione sul token e il 3-5% ma **l'allowance della FFN perde ~17%**, e il `K156` di E24 scende da ~8.1 a **~7.4 tok/s**. **Nessun verdetto si muove** (E19/E24/E27 sono negativi, una correzione che peggiora il carve li rafforza). **Record di RAPPORTI soltanto**: l'ancora E25 e passata per 0.19 di punto (`-9.81%` su `+-10%`, deriva termica), quindi nessun assoluto ne esce. Previously: ❌ **E25 da il PRIMO numero misurato alla forma dell'obiettivo: `T10` (10.60 G attivi, le dimensioni dell'"es 10B") gira a 4.70 tok/s su questa macchina -- 10.6x dai 50 -- e il rango 512 su `q/o` la porta a 5.36 (`+14.12%`), cioe 9.3x.** Il matvec fattorizzato che E21/E22/E23 chiedevano ESISTE e ha passato la parita end-to-end (`6.445e-04`, top-1 `1.0000`); il controllo piantato byte-neutro dice che il suo costo proprio e **sotto la risolvibilita** (`-0.43%` a dispersione 7.3%); e **l'invariante e che quattro bracci `T10` a ritmi diversi del 15% stanno dentro l'1.54% in G pesi/s**, quindi il rango compra solo i pesi che toglie e `2.D.r` e l'addebito giusto. **La linea di E18 sez.31 mappa su 47.0-50.7 tok/s al throughput misurato di `T10`** -- il budget regge alla forma per cui e stato scritto. **Ma il rango su `q/o` vale `1.2 G` dei `9.6 G` da togliere: la FFN e `8.45 G`, nove decimi del problema.** Previously: ❌ **E23 mette un PAVIMENTO sotto il soffitto: il carve regge a un router vero (91%), la COMPOSIZIONE no (71%).** Sostituito l'oracolo con una ridge in forma chiusa addebitata `11.0 M`, `V52` passa da tf 117 a **110** (`ROUTER-HOLDS`) ma `QO512+V52` -- la configurazione dentro il budget -- passa da 126 a **102**, sotto il pavimento `107`: **`ROUTER-COSTS`**. La composizione **cambia segno** (+9 con oracolo, -8 con router vero) perche la ridge legge l'input del blocco, che il taglio di rango ha gia perturbato. **E l'aritmetica del budget col router addebitato dice che `k=133` sta 2.8% SOTTO il pavimento**: il fisso e `354.5 M`, la FFN puo pesare `627.5-705.5 M` = `k = 139...156`, quindi la profondita va RI-DERIVATA verso l'alto (E24), il che e qualita gratis e non una ritirata. Nessun timing. Previously: ❌ **E22 puts a configuration INSIDE the budget for the first time -- and shows it cannot be reached by CONVERSION.** `QO512+V52` (activation-weighted rank-512 `q/o` + E19's 52% FFN carve) is **`0.9441 G` active ternary weights/token against `0.982-1.060 G`**, 3.9% under the low end, and at fp32 it is the best-ranking modified donor ever measured here (`126/160` teacher-forced, `+0.237444` BPB). **But the budget was priced in ternary and the measured artefact is fp32**: the same configuration with both low-rank factors ternarized reads `2.812226` BPB / `28/160`, and the fully assembled `STACK` reads `3.947669` / free `5/160` / tf `4/160`. **The budget-feasible object and the working object are two different objects.** On the 7 B the two cuts leave `4.090 G` = 4.0x the budget, and once `q/o` is cut the budget permits NEGATIVE FFN -- **the `545 M` head is the binding constraint** (E21). The carve's router is an ORACLE, a ceiling. No timing taken. Previously: ❌ **E21 lowers E19's uncarvable floor for the first time and it is still not enough -- and it NAMES what blocks it.** Activation-weighted rank-512 on `q_proj`+`o_proj` costs **`+0.052689` BPB** and keeps 144/160 fixed-context tokens (the cheapest structural cut ever measured here), so at the validated fraction `r/D ~ 1/3` the Coder-7B's `attn+head` falls from **`1.367 G` to `1.127 G`** -- **still 1.06-1.15x the whole 50 tok/s budget (`0.982-1.060 G`) with the FFN at ZERO.** What remains is **the output head: `545 M`, 51-56% of a 7B's entire budget, `+0.278096` BPB to halve by rank against `q/o`'s `+0.053`, `+0.170414` to ternarize (E20) -- no lever measured in this programme makes it small.** And **no rank result becomes tok/s until `engine.c` has a factored matvec** (two GEMVs, intermediate of size `r`) and `QWENDON1` a kind for one; neither exists. No timing taken. Previously: ❌ **E19 removes the structural escape: FFN-only carving cannot reach 50 tok/s at ANY depth.** On the Coder-7B `attn+head` alone are **1.367 G** active weights against a 50 tok/s budget of **0.982-1.060 G**, so **a 7 B with its FFN deleted outright still runs at 35.9-38.8 tok/s** -- and every carve this programme has built (D0, D0c, Probe-4) is FFN-only. Projected on each donor's MEASURED attn+head share, a 10 B carries a **1.93-2.51 G uncarvable floor = 1.82-2.37x the entire budget** (19.6-27.4 tok/s with its FFN at zero). **A carve that reached the target would have to cut ATTENTION and the OUTPUT HEAD too** -- never attempted, and prima facie hostile to E17's finding that the head is where ranking lives. No timing taken. Previously: ❌ **E18 gives the shortfall a BUDGET instead of a ratio: 50 tok/s permits 0.98-1.06 G active ternary weights per token = **9.8-10.6% of a 10 B model** (100 tok/s: 4.9-5.3%), derived over measured quantities only -- bytes/weight RECOMPUTED from the artifact at exactly 0.500000 -- and the 7 B donor activates **7.07 G**. On the organ ladder the **FASTEST rung is the one that converts EVERYTHING, at 7.21 tok/s (weight path), 6.9x short**, while every rung that keeps an organ exact is slower still (an fp32 weight costs **5.87x** a ternary one here). **Quality improves down that table and speed improves up it: no point on the conversion ladder is both.** So the target is not reachable by converting a dense donor at ANY conversion quality -- it needs a model TRAINED INTO the format that activates ~10% of itself per token. No timing was taken and no speed number moves.** Previously: ❌ **6.79 tok/s exact on the real 7.072 B — 7.4× short of 50 — AND (E15) that rate belongs to an artifact that DOES NOT PREDICT: `qwen25-coder7b_p.bin` scores 5.299200 BPB against a chance line of 4.070106, i.e. **1.229 BPB worse than guessing uniformly**, while the same weights in fp32 score 0.674027. The speed figure itself is untouched — a ternary weight costs the same bandwidth whatever scale multiplies it — but **it is not a rate for a working 7 B**, and closing that is now a QUALITY problem (E15 owed item B2: `--rule R3` at 7 B, never built), not an engine one.** Previously: **6.79 tok/s exact — with a 1.358× lever available on a LOSSY path (E13), which would make it 9.21 and 5.4× short if its quality cost proves acceptable.** **That cost is now MEASURED (E14) and the lever is still not released.** At 1.5 B, int8 activations move BPB by `-0.016961` (whole-vector) — which §4's one-sided band reads as `ACTIVATION-CHEAP` — while changing the **top-1 token on 54% of positions** (greedy 45.6% vs the fp32-activation arm). And the composition is **crossed**: E13's 1.358× was measured on `--lutblk` alone = the whole-vector arm, the one with the larger quality move, while the quality-cheap group-32 arm has **no speed number at all**. So the exact figure is the one quoted. **E13 also closes the engine side**: the weight path now runs at **97% of this machine's demonstrated streaming rate** (33.76 vs 34.75 GB/s in the same sweep), so the 1.46× E10 had left is taken and **there is no third kernel to write**. Three gated changes on 2026-09-07 worth **1.52× exact / 2.065× lossy**: **E8** (the weight `matvec` ran at FMA latency, not bandwidth — 1.349×) and **E9** (the SwiGLU glue ran on one thread — 1.056×, bit-exact). Weight path **16.7 → 24.0 GB/s**, **48.0 G-weights/s**. Ledger §19.3's engine budget was 1.7–2.5×, 1.52× is taken, and **E10 measured the weight path's share of the remainder at ~1.03× — that kernel is on its own ceiling at every footprint**; the rest of the gap is not available to engine work at this shape. Previously: ❌ **and now measured on TRAINED weights too: E7 reads 4.460 tok/s @300 on a real 7.07 B — 11.2× short of 50.** The synthetic proxy was checked in the same breath and held (32.8 vs **31.5** G-weights/s, −4.0%, inside ±5%), so the figure below is not an artefact of untrained weights. Previously: **measured at the target shape, not extrapolated: ~3.1–3.2 tok/s** (`T10`, 10.6 B active) — **~16× short of 50**, and it is the **weights** that are short. E3 found the *non-weight* term `f` broken (9.0× its reservation at 300 context, 22.3× at 800) and concluded a 10 B could not pass **38.3 tok/s at 800 context even with a free weight path**. **E4 overturned that**: the `Q·K` reduction was latency-bound, and 40 lines of AVX2 took `f` from 24.678 → **12.735 ms**, the ceiling `1000/f` from 40.5 → **78.5 tok/s**, and the 50 tok/s active-weight budget at 800 context from **0 → 259 M** — at **ΔBPB 3e-06**. **50 tok/s at 800 context is a weight-side problem again** |
| **At usable quality** | ❌ **NO**, but the number keeps moving: FFN conversion **+3.309 → +1.260 BPB** (T2), still 252 σ_seed. **E12 adds the reference that was missing: chance is `log2(151936)/4.229452` = 4.069819 BPB, and T2's `+3.309` — the rule `qwen_export.py` ships by DEFAULT — lands a model +0.007 ABOVE it, i.e. at chance; `+1.260` (R5) lands 2.042 BELOW it. So the 62% T2 removed is the difference between a model at chance and a model that predicts, and `--rule R3` has never been run across scale.** The **whole runnable model** cost **+2.708111** (T2b) and is now **+2.465779** — E2 confirmed the RMSNorm fold *through the engine* at T3's exact `−0.220001` and it is **adopted as the exporter default** |
| **The binding constraint** | **as of 2026-09-12 it is NEITHER the rule nor the format — it is POST-HOC CONVERSION ITSELF, and the replacement is TRAINING.** E37 measured that converting a really-trained donor to the activation rate the speed needs yields `4.029398` BPB, 0.040 under chance, **and not because the router is bad: E38 handed the same carve a per-token ORACLE — the ceiling of every router that ranks groups by mass — and at that rate it reads `4.131817`, 0.062 ABOVE chance. There is no router to build.** (E37's own reading of this, that a router finding 71.3% of the oracle's mass and one finding 6.1% give the same model, was made in a 0.5941 BPB window and is corrected in E38 §4 — the interpretation moves, no number does.) The same day H0 measured the opposite direction working: straight-through training of the ternary factored form takes teacher-forced `28 → 111`/160 against a registered bar of 48 and removes **97.2% of the BPB damage**, on 12.5% of its own schedule. **The one axis that still does not move is FREE-RUNNING GENERATION** — H0 reads `8/160`, below E22's fp32 `15/160`. **And as of E39 the SPEED side is no longer where the wall is: the same 9,999,220,736 parameters, with `q/o` at rank 512 and the weight moved into the FFN, have an attention+head floor of `10.0` ms = `99.7` tok/s (both runs agree to 0.62%), against E34's `20.03` tok/s for T10 with the FFN deleted. The 50 tok/s budget now buys ~4.5% FFN activation instead of 1.17%. The constraint is unchanged in KIND — it is still that nobody has TRAINED anything into this shape at 10 B — but the shape it would have to be trained into is no longer out of reach.** Earlier reading (T2, `RULE-HELPS`) kept for the record. **E41 chiude il TERZO asse post-hoc: la PARTIZIONE.** Sei raggruppamenti dei soliti 8.960 neuroni, e al 6,25% (quello che 50 tok/s comprano a 10 B) il miglior selettore attenibile e `D0C`, la famiglia che usavamo gia: **3,804346 BPB, 92,0% della finestra denso->caso perduta**; a 1,17% tutte e dodici le celle stanno sopra il caso. E l'ORDINE fra oracolo e router attenibile SI ROVESCIA (`COACT` migliore soffitto, terzo nel fitted), quindi anche il modo economico di prezzare i raggruppamenti e fuorviante. ⚠ **CORRETTO lo stesso giorno: E41 NON conta.** L'addendum B ha rifittato i router al budget giusto, l'ordine e cambiato (`COACT` 3,583800 passa primo e scavalca la barra di 0,0133) e **per mia regola registrata la cella e UNRESOLVABLE**: le misure indipendenti restano **TRE** -- conversione (E37), selettore (E38), forma dell'attenzione (E40). Quello che sopravvive a entrambi gli strumenti di E41 e solo questo: al 6,25% il migliore dei sei raggruppamenti perde l'85-92% della finestra e manca la barra di usabilita di 2,58 BPB, e all'1,17% tutte le celle stanno sopra il caso. **Il ritmo che ci si permette e il ritmo che servirebbe non si sovrappongono, ma E41 non e la misura che lo dimostra.** (`probes/E41_PARTITION_IS_NOT_THE_LEVER.md`) |

> ⚠ **Until E1 (`33f0add`) the runtime could not load a model over 2 GB at all** — 32-bit
> `ftell`, silently reported as `bad magic` against an intact file. The largest artifact it had
> ever been given was 1.84 GB. **A 10B ternary packed model is ~5 GB**, so the target was not
> slow, it was unloadable, and no speed probe could have found it.

**The one-line state:** the road exists end to end — safetensors → export → runtime →
**generated tokens** — and since **E6** that last clause is finally backed rather than asserted:
the fp32 build reproduces PyTorch's greedy continuation **token for token**. The **ternary** build
generates too, and what it generates is not language — which is what T2b's **+2.466 BPB** looks
like when you read it instead of quoting it. T2 has now shown the damage at the far end was
**62% a bad map into the format**, not the format itself, and removed that much of it on CPU with
no gradients. What remains is a real quality gap and a 20× speed gap.

**The number that prices everything else, re-derived by E3 at the shape it is about:** a 10B donor
needs **≤318 M active weights/token — 3.0% of a 10 B — for 50 tok/s at 300 tokens of context**, and
**at 800 tokens of context there is no budget at all**, because `f` alone exceeds the whole 20 ms.
100 tok/s is **out of reach at any weight cost** at a 10 B shape: `f` = 10.576 ms > 10.

The old figure, **522 M**, is superseded twice over. It was also **arithmetically wrong in its own
terms**: `27.7 G-w/s` is `493,961,216 / 17.833 ms` = weights / **wall**, so `f` is already inside the
denominator, and `× (20 − 1.17)` charges it a second time; self-consistently it is 554 M.

**The head is no longer the floor.** It is 20.5% of the token at 0.5 B and **1.1% at a 10 B shape**;
the attention projections are 17.9%, sixteen times more. The tokenizer claim itself survives and is
now end-to-end rather than a weight count — `M7` and `Q8`, same width, **4.637× the vocabulary and
4.629× the head time, 0.2%** (§7).

## 1. The runtime (this is the deliverable)

| what | where |
|---|---|
| Export a donor to a flat binary (fp32 / ternary / packed; optional ternary head) | `benchmarks/donor_adaptation/engine/qwen_export.py` |
| The runtime: RMSNorm, GQA + RoPE + KV cache, SwiGLU, ternary matvec, packed `pshufb` path | `benchmarks/donor_adaptation/engine/donor_engine.c` |
| Parity gate vs PyTorch on identical weights | `benchmarks/donor_adaptation/engine/parity_gate.py` |
| Report: profile, the two optimisations, honest position vs the goal | `probes/R1_DONOR_RUNTIME.md` |

**Measured, Qwen2.5-0.5B, 3600X, `--bench 300`, idle, median of 3:** 12.45 tok/s (t1) →
**56.1 tok/s (t6, packed)**. Trajectory: 23.5 (fp32 head) → 38.0 (ternary head) → 48.3 (packed)
→ 50.9 (same binary, re-measured 2026-09-05) → **56.1 (rope hoisted out of the head/layer loops,
bit-identical, `SPEED_LEDGER.md` §12)**. `--fuse --lut --lut-group 32` adds ~3.9% on top, but the
LUT half of that is not numerically free.

> **Two rates have been withdrawn here for two different reasons, and both were load-bearing.**
> R1's "40–46" and §10's 36.1 were taken on a **contended machine**. §11.4's per-organ table was
> taken with **the wrong work inside the timer** — both `rope()` calls sat in the `qkv` bucket, so
> it reported qkv at 4.1 GB/s (real: 14.2) and produced a "32 µs per call" anomaly that does not
> exist. **A tok/s figure is also only comparable at the same `--bench` length**: attention is
> `O(position)`, worth 1.7 ms/token between 300 and 800.

## 2. The speed side — what is priced and what is measured

| probe | question | answer | where |
|---|---|---|---|
| **Ledger** | what must a donor cost per token to hit 50/100 tok/s? | **≤522 M active weights/token** for 50 tok/s, ≤245 M for 100 — from the *measured* **27.7 G-weights/s**, not a byte-rate ÷ bits-per-weight | `SPEED_LEDGER.md` §12.2 |
| **P2** | is the expert path bandwidth- or compute-bound? | **MIXED — ~60% arithmetic.** A denser pack buys ≤1.32× on the FFN, not 2.5× | `probes/P2_EXPERT_PATH_DECOMPOSITION.md` |
| **P3** | what do donor SHAPES cost on the engine's kernels? | 0.5B 81 / 1.5B 30.7 / 3B 15.1 tok/s (matvec only). The ledger was 18% conservative because it priced dense FFNs at a *gather* rate | `probes/P3_DONOR_SHAPE_ON_ENGINE.md` |
| **R1** | what does a real runtime cost? | **56.1 tok/s** at 0.5B (§12). The packing bought nothing, exactly as P2 predicted; the LUT buys 2.2% and is not free; **the one big win was a profiling artefact — `rope()` was 9.6% of every token** | `probes/R1_DONOR_RUNTIME.md`, `SPEED_LEDGER.md` §12 |
| **E3** | what does the engine actually do at the target shape, end to end? | **`RESERVATION-BREAKS`.** 6 shapes 0.5–10.6 B x 2 context lengths, synthetic weights gated against real artifacts at 0.000% (1.5 B) and 0.357% (0.5 B). `T10` = **3.090 tok/s**; rate **rises** to 32.8 G-w/s; **`f` is 9.0× the reservation at 300 context and 22.3× at 800** | `probes/E3_ENGINE_AT_TARGET_SCALE.md` |
| **E4** | is the attention loop latency-bound, as E3 read it, or bandwidth-bound? | **`LATENCY-CONFIRMED`.** The `Q·K` dot loop was **6.5× below its own memory limit**: 14.647 → **2.242 ms**, **5.4 → 35.1 GB/s of unique K bytes**. ILP alone 2.31× (same bytes, less time — bandwidth falsified on its own), SIMD alone 4.28×, both **6.53×**. **`f` 24.678 → 12.735 ms, ceiling 40.5 → 78.5 tok/s**, ΔBPB **3.03e-06**. The floor is now `R` = softmax + `A·V`, **81.4%** of the organ | `probes/E4_ATTENTION_ACCUMULATORS.md` |
| **E5** | what is `R`, the 81.4% of the attention organ E4 could not see inside? | **`OVERHEAD-DOMINATED`.** `R` = softmax `S` **25.3%** + `A·V` `Y` **20.6%** + `P` **54.0%** at `T10` @800 — **more than half of `R` is neither loop**. Nine 3× predictions passed, worst −1.15%; `X` = 2.253 ms reproduces E4's 2.242 by a method sharing no arm. The split **inverts with scale**: `S05` is SOFTMAX-dominated at 47.7% | `probes/E5_DECOMPOSE_R.md` |
| **E6** | does a real donor actually generate text here, or only score it? | **`GENERATION-CONFIRMED`.** New `--generate` mode, greedy. fp32 Qwen2.5-0.5B: **160/160 tokens identical to PyTorch**, five prompts, no divergence. Planted control fires — ternary 0.5 B **1.9%**, ternary 1.5 B **6.2%**, neither writes language. G-P: the prefill logits are **byte-identical** to `--logits`, so this is E1's gated forward pass. **And `1000/f` is a ceiling, not a rate: measured is 3.09–3.14 tok/s at `T10`** | `probes/E6_GENERATION.md` |
| **E7** | does any of this hold on a real donor at a size the goal is about? | **`REAL-WEIGHTS-CONFIRMED`.** Qwen2.5-Coder-7B, **7.07 B active/token**: 30.46 GB consumed exactly, parity **1.070e-05** / top-1 **1.0000**, **160/160** greedy tokens identical to PyTorch. Speed **4.460 tok/s** @300 (3 reps, 0.45% spread) against a **pre-registered 4.0–5.4** — **11.2× short of 50**. And the synthetic proxy checked at last: 32.8 vs **31.5 G-weights/s**, −4.0%, inside ±5% | `probes/E7_REAL_LARGE_DONOR.md` |
| **E7 §8–§9** (extension, pre-registered `8252031` / `9103c26`) | is there a fixed cost inside the timed region, how does `f` grow past 800, and which kernel is even running? | **Fixed cost REFUTED** (1600 median **4.230** against a 4.580 threshold fixed first; the 300→800 inversion was **2.7%**, inside ±5% — noise given a mechanism it did not have). **`f` is LINEAR to 1600**, 0.0319 ms per token of context, two intervals agreeing to 0.2% over a 5.3× range — **E4's owed item 3 CLOSED**. And `donor_engine.c:116` defaults `g_attn` to **`ATTN_SERIAL`**, E4's second-slowest arm: every E7 and E3 figure is a `serial` reading. **No ledger number needs correcting** (E4 published `avx4` vs `serial` at `T10` as **+3.9%**, inside ±5%) — but attention is 2.2% of the token, so this is a correctness note, not a speed result | `probes/E7_REAL_LARGE_DONOR.md` §8–§9 |
| **E7 §10** (pre-registered `9103c26` / `db53f63`) | does E4's `avx4` win show up as a RATE on a real donor? | **Not answerable with the un-profiled instrument.** @300 the **null was called in advance** and landed (4.450 vs 4.460); @1600 the baseline's own spread (6.1%) exceeded the effect (5.7%); the interleaved re-run is **VOID** (paired median **0.973** against a pre-registered 1.00–1.09, within-arm dispersion **11.5%**). **The win does reach the wall** — profiled organs sum to the wall to **0.03%**, the non-attention remainder is arm-invariant, and the arithmetic is consistent to **0.4% of the token** — worth ~6–7% at 1600, **published as no rate at all**. **New general defect: `--bench` has no contention witness** | `probes/E7_REAL_LARGE_DONOR.md` §10 |
| **E7 §11** (pre-registered `55bd8c6`, re-registered `02baefa`) | can `--bench` tell whether the machine was quiet? | **`WITNESS-CONFIRMED`.** `ffn~` on the `BENCH` line, layer 0, on by default. **Failed its first gate** — 0.9877 against 0.990, a 1.2% cost against a 0.008% prediction — rebuilt from 48 timestamps/token to 2, then **0.9996** (predicted 0.995–1.000), `--logits` **byte-identical**, and the **planted control fires** at **+34.3%** under load. A filtered median that would have acquitted the failed build is recorded and **refused as circular**. Validates nothing retroactively | `probes/E7_REAL_LARGE_DONOR.md` §11 |
| **E8** (pre-registered `406b02b`, pushed before the run) | is the weight path bandwidth-bound, or a dependency chain? | **`CHAIN-CONFIRMED`.** One accumulator per `matvec` → the loop ran at FMA **latency**. Four accumulators: **Coder-7B 4.73 → 6.37 tok/s, 1.3491 paired over 10 pairs (1.8% spread)**; 16.7 → 22.5 GB/s; qkv/o/ffn/head all move 1.41–1.49× together. Parity 3.3e-06 and 160/160 greedy. **Negative control on the fp32 kernel predicted 0.99–1.03, measured 1.0029**, and showed 33.3 GB/s through the same kernel. Predicted 1.5–1.7×, got 1.349 — the next binder is the kernel again. **Now the default**; `--mvacc 1` reproduces every earlier rate byte for byte. FFN decomposed: `gate+up`/`down` = 1.990 against a structural 2.000 while chopped 10.6× differently, so per-row cost was never it | `probes/E8_MATVEC_DEPENDENCY_CHAIN.md` |
| **E9** (pre-registered `82dbb54`, pushed before the run) | the glue between the FFN matvecs moves no weights — what does it cost, and is it threaded? | **`GLUE-CONFIRMED`.** It was **not** threaded: 530,432 scalar `expf` per token, **11.346 ms, 7.5% of the token, zero weight traffic**. An elementwise map with no reduction → **bit-exact**, gated on **sha256** rather than parity (`b94b56d0…548765`, identical). **6.45 → 6.79 tok/s, paired median 1.0557 (10 pairs, 3.8% spread), inside the 1.055–1.066 predicted before the run**; glue 11.346 → **2.938 ms**, `sum/ffn` 0.9999. `rmsnorm` deliberately left serial — 0.330 ms over 56 calls cannot pay 2.7 µs per region. The `expf` itself is untouched and is the next item: it would beat this, but it forfeits the sha256 gate | `probes/E9_GLUE_PARALLEL.md` |
| **E10** (pre-registered `a0366d0`, pushed before the bench existed) | is the packed kernel core-bound or stream-bound at 22.5 GB/s? — E8 §9 item 2 | **`CORE-BOUND`.** Same `matvec` source, footprint swept 4 MB → 2 GB with `n_in` fixed: **49–53 G-w/s at every cell, all seven within ±4% of the arm median**, while fp32 moves **4.4×** over the same range. **Planted control read first and PASSED (4.40 vs ≥2.0)**, reproducing probe-3's 16 MB cliff unprompted; G-K2 tied the bench to the engine's 26.1 GB/s within 2.3%. **The engine's FFN organs are AT the ceiling** (51.66 / 52.22 G-w/s). **Withdraws §19.4's 37 and 42 GB/s rows for the packed path** — the assumption §19.5 flagged as weakest — and cuts the weight path's remaining engine budget to **~1.03×**. Not FMA-bound either (0.29/cycle/core of 2): the side is named, **the mechanism is not, deliberately**. G-K3 was aimed at the wrong statistic and it is recorded | `probes/E10_PACKED_KERNEL_BINDER.md` |
| **E11** (pre-registered `00d4446`, pushed before the arm was written) | does the `--lut` kernel have a higher per-weight ceiling than packed? — E10 §6 item 1 | **`NO-LIFT`.** At the 512 MB verdict cell named in advance: **21.90 vs 55.95 G-w/s, ratio 0.391** — the LUT path is **2.5× slower per weight** at donor footprints. **Corrects §12's «1.05×» and §2.1's «3.9%»**, both taken against a pre-E8 single-accumulator packed arm. **But inside L3 it is the fastest kernel measured here — 91 G-w/s, 1.8× packed — and falls 4.2× across 16 MB**, because tile-major puts consecutive reads 299 KB apart at scale: the property that makes it fast is the property that kills the stream (exploratory, outside the bands). Bears on `SCALEUP_ARCHITECTURE`'s ≤16 MB keystone. Run 1 VOID by a concurrent worker; G-L2 then failed by my own mis-specification | `probes/E11_LUT_KERNEL_CEILING.md` |
| **E13** (pre-registered `89fd63e`, pushed before the arm existed) | is the LUT collapse a LAYOUT artifact? — E11 §3's untested mechanism, and the 1.46× E10 left unread in its own table | **`LAYOUT-CONFIRMED`.** `build_tm` stored `tm[t*Mpad + o]`, so consecutive 32-byte reads sat **18.5 KB** apart at `gate|up` and **3.5 KB** at `down` — a different page nearly every read, 32 bytes of every 64-byte line, for the **same total bytes**. `--lutblk` stores each 32-row tile contiguously: same bytes permuted, `t` order and accumulate tree untouched → **bit-identical, gated on sha256** (`7a9b3e04…f48e44`, 311 MB, both arms). **G-M1 = 67.51 G-w/s at E11's pre-named 512 MB cell against ≥65; predicted 65–77 and landed inside.** Blocking recovers **3.0–3.5×** at the largest cells, the 4.2× cliff becomes **1.53×**, and **103.05 G-w/s at 12 MB is the fastest weight kernel measured here**. At 512 MB it moves 33.76 GB/s vs the fp32 arm's 34.75 = **97%** → **the 1.46× is taken; no third kernel to write.** End-to-end **1.358×** (9.21 vs 6.78 tok/s) — but on the **LOSSY** int8-activation path whose 1.40e-01 cost was never carried to BPB, so **6.79 exact remains the quoted rate**. Confirms E11's `NO-LIFT` end-to-end (`--lut` = 0.557×); overturns only its untested §3, which holds. Known-positive reproduced to 0.15%; a 3-rep pass with 180% spread discarded as warm-up | `probes/E13_BLOCKED_TILE_MAJOR.md` |
| **E12** (pre-registered `11a9d83`, runner `87ba957`, both pushed before the sweep; §9 amendment + Z-dispersion `ae403c2`) | does the ternarization cost SHRINK with donor scale? T2's rule is measured at ONE shape and it is the assumption under the whole MoE road | **`CHANCE-LINE` — the question could not be ASKED at these settings. Supersedes an earlier `CONTROL-FAILED`, WITHDRAWN.** dBPB 3.715658 / 4.738239 / 5.010249 at 0.5/1.5/3 B, each against its OWN fp32 baseline. **`log2(151936)/4.229452` = 4.069819 BPB is chance, and EVERY ternarized arm at EVERY cell is above it** (+0.518/+1.436/+1.665) while every fp32 baseline is far below. Above that line BPB measures how confidently WRONG a model is — so `r = 1.3484` is not reported, and the deterministic `F` > `FA` inversion at 3 B needs no instrument bug. **`I - base = +0.000e+00` exactly at all three cells**; the instrument was sound. The withdrawn verdict rested on arm `Z`, **which T2 §4(a) had already retired and t1's own docstring and `ARMS` table label mis-specified**; measured dispersion **spread(3B) = 1.695938 -> `Z-UNSTABLE`, predicted and landed**, larger than the 1.490 gap it was read off, and the outcome FLIPS with the seed at 0.5 B (2/5). **Owed first: FINISH the R3 sweep at 3 B** — `R0` is only the flag DEFAULT; every standing engine artifact ships `--rule R3`, which E1 measures ABOVE chance at 0.5 B and BELOW it at 1.5 B, so the shipped rule crosses the line at an unlocated shape | `probes/E12_TERNARY_COST_VS_SCALE.md` |
| **E14** (pre-registered `537b496`, pushed before the run; G-N3 runner `ba0b15c`, pushed before it ran) | what does the int8 activation quantization that gatekeeps E13's 1.358x lever COST in quality? | **`CHEAP-BUT-NOT-NEUTRAL`.** At the 1.5 B verdict cell, **0.625503 BELOW the chance line** where BPB means what it usually means, int8 activations **LOWER** BPB: `dBPB(A1) = -0.016961`, `dBPB(A2) = -0.005991`. Section 4's bands are **one-sided**, drawn for a cost, so a negative delta reads `ACTIVATION-CHEAP` **by construction**. **G-N3 says it is not free: the top-1 token changes on 54% of positions** (greedy agreement 45.6% whole-vector, diverging at token 0; 64.4% group-32, token 1). **The label is true of the band and false of the model, and no registered gate could tell the difference** -- section 6 forbade promoting G-N3 after the fact, so the gap IS the result. **The lever is not released, and the composition is CROSSED**: E13's 1.358x was measured on `--lutblk` = arm A3 = A1 (G-N0 `+0.000e+00`, and token-for-token identical under G-N3), the arm with the LARGER quality move, while the quality-cheap A2 has **no speed number at all**. E13's bit-identity now holds **three independent ways**. Protocol defect recorded: no `--seqlen` was passed, so every arm scored as ONE 12,288-token sequence; arm-to-arm gates unaffected, a 512 re-run is owed | `probes/E14_ACTIVATION_INT8_COST.md` |
| **E26** (pre-registered before the runner existed; part B run 1 VOID -- contended box) | what does an ACTIVATED weight cost in the engine? -- E25's invariant (charged throughput inside 1.54% across arms differing 15%) is what licenses reading tok/s off a weight count, and it was established on RANK. Does it survive a CARVE, where weights are GATHERED rather than streamed? | **`A GATHERED WEIGHT COSTS MORE THAN A STREAMED ONE`.** Part A: three gates fire on the real donor (`E26-P` `4.481e-06` top-1 `1.0000`, `E26-S` 224/224, `E26-R` `1.156e-06`). **Part B: the invariant does NOT extend -- the charged-throughput band opens from E25's 1.54% to 12.54% at `T10` and 18.00% at `S15`**, so `3*D*GSZ*k` is optimistic and E18 s31, E19, E23 s7, E24 and E27 all inherit that. **THE PLANTED CONTROL RESOLVES THIS TIME**: at `k = E` the carve is byte-neutral to a REGISTERED `-0.47%`/`-0.71%` and reads `-6.80%`/`-4.98%`, so **the carve machinery's own cost is `-6.36%`/`-4.30%`** -- paid before a single group drops and charged to nothing anywhere until now. (E25's factored path's own cost was BELOW resolvability: rank is nearly free, the carve is not.) **The container is free** -- `S15-PACKED` vs `S15-DENSE` read `1.0056` against a byte prediction of `1.0000`. **The registered trigger does NOT fire and the primary claim is falsified anyway**: `K16`/`K4` read 41.41/41.67 against a 40 G-w/s alternative, **and I do not claim it fired** -- it was the wrong statistic, because **the loss is a U** (`T10` vs its dense: 0.936, 0.902, **0.882**, 0.960, 0.967) and at small `k` the FFN is 5.7% of the token so **the dense floor masks the effect exactly where the trigger looked.** Isolating the gathered FFN's own efficiency (**post-hoc; per E14 s6 NOT a gate**): `S15` `0.943 -> 0.564` with **disjoint** intervals, `T10` `0.921 -> 0.825` overlapping, and `T10-K4` `[0.390, 1.339]` **not resolvable**. **ROW LENGTH IS THE VARIABLE and prediction 5 lands on three independent comparisons** (768 B at `S15` vs 2048 B at `T10`): band 18.00 vs 12.54, efficiency at `k=16` 0.564 vs 0.825 disjoint, and `S15` 17-24 pt short of bytes where deep `T10` overshoots -- **a LOCALITY cost, with an EXPORTER-side lever: coarser runs at equal activated fraction.** **Cost to the tables**: E24's `K156` at `T10` 8.1 -> **~7.4 tok/s**, E27's `FLOOR-MIN` 9.37 -> **~9.0**; at the depths the budget permits (`k~2..18`) the token correction is 3-5% but **the FFN allowance loses ~17%.** **No verdict moves** -- E19/E24/E27 are negative or quality-conditioned and a worse carve strengthens them. **VALIDITY IS A CONSTRAINT HERE**: the witness was REBUILT (the old max-based 12% bar refused an idle desktop 3x; now the mean, with `--selftest` a planted control on the instrument -- quiet 12.1%, six spinners 61.5%, fires AND accepts -- bar re-derived to 25% between those populations, admission written above `IDLE_BAR`), **but the real instrument is the E25 ANCHOR and it nearly failed: `S15-PACKED` 26.79 vs 29.70 published = `-9.81%` against `+-10%`, passed by 0.19 pt**, both `S15` arms declining monotonically = THERMAL drift. **So this is a RATIO record: no absolute tok/s leaves it, and prediction 3's absolute form is UNTESTABLE here** (the dense control itself reads 43.12 where E25 read ~49.9) **so its failure is not counted.** Pairing verified: per-rep paired ratios differ from mean-of-means by at most 0.4%. **6.79 tok/s untouched.** | `probes/E26_WHAT_AN_ACTIVATED_WEIGHT_COSTS.md` |
| **E27** (pre-registered `808ba43`, pushed before the runner existed) | the FLOOR, not the FFN -- E24 named the lever and stopped there: at `T10` the FFN is nine tenths of the weight but the non-FFN floor decides how much of it the budget can afford. Does WIDTH (rank on `q/o`, on `k/v`) or DEPTH (whole layers) open the goal's shape? No carve, no router, no timing | **`FLOOR-IS-NOT-ENOUGH`.** Twelve arms, `VOID: none`, 4345 s. All five gates fire; **`G-F1` is the load-bearing one -- `L28-PASSTHROUGH`, the depth machinery run with `n=0`, is BIT-IDENTICAL to `base`**, which is the only thing that rules out a silent reindexing bug. **THE RANK FRACTION DOES NOT TRANSFER: `r/D = 1/3` reads 144 teacher-forced, `1/8` reads 56, `1/16` reads 42** -- a cliff, not a slope, and no fraction to carry to `D=4096`; **E21 s8 closes negatively.** **`k/v` are the worst trade on the board**: `[1024,4096]` at `T10`, not square, so rank saves only if `r < 819`, and `r=96` buys 0.8% of the weights (`1.5436->1.5312 G`) for **76 teacher-forced tokens** (`160->84`) -- leaving them fp32 through E21-E24 was avoiding a 76-token cut, not favourable bookkeeping. **THE ONE REAL POSITIVE IS OFF THE BUDGET AXIS: `L21-MINRES` reads `113/160` against `L21-LAST`'s `57/160` at identical depth, parameter count and charged cost -- +56 tokens purely from choosing WHICH seven layers to drop**, 4x outside the registered 0-15 band. The `MINRES` profile (defined in the brief before the run) **is not monotone**: 19.05 at layer 0, minimum 0.2836 at 15, **rising to 0.7618 at 27**, so "drop the last `n`" removes some of the MOST active blocks; the seven smallest are a **contiguous middle band, 12-18**. **A quarter of a trained transformer's layers can go for almost nothing if the right ones go -- no carve, no router, no rank, no retraining.** But `L14-MINRES` at half depth collapses to `54/160` and the cliff between 21 and 14 was not bisected. **At `T10` with the FFN left DENSE the floor lever alone buys: `base` 4.68, `QO-512` 4.93, `QO192+KV96` 5.44, `L24-LAST` 6.17, `L21-MINRES` 7.75, `FLOOR-MIN` 9.37, `L14-MINRES` 15.21 tok/s** -- and the only one above 10 reads `54/160`. **To reach 50 from `L21-MINRES` the FFN would have to fall to `-0.684 G`, which is not a number: the floor ALONE is 1.68x the whole budget**, and at 36 layers with dense `q/o` **deleting the FFN entirely reaches 29.7 tok/s, not 50.** `FLOOR-MIN` -- the cheapest floor E27 can assemble and the only arm with real FFN room (`k = 17.3` of 256, 6.8% activation) -- reads `38/160`: **the configuration that fits the budget is the configuration that does not work. The registered alternative does NOT fire.** Three predictions held, two missed, one missed its magnitude by 4x. **No timing, nothing exported; 6.79 tok/s and `T10`'s 4.70 tok/s untouched.** **H0 is unaffected -- router-free and carve-free by construction.** | `probes/E27_THE_FLOOR_NOT_THE_FFN.md` |
| **E36** (pre-registered `1738aa2`, pushed before the runner existed; **run 2's rule pushed at `9819226` before run 2 existed**) | thirty-five probes priced the goal's sentence and none of them ever BUILT the thing. **Does an artifact that is ACTUALLY ~10 B read 50 tok/s on this box, when its activated slice is sized to the envelope E35 measured?** Speed only, synthetic weights, registered as such before the run | **`TEN-B-NEAR-FIFTY`.** A **9,999,220,736-parameter** artifact (5.11 GB, verified three ways from the file itself) with **540 of its 46,080 FFN neurons per layer active per token (1.17%)** reads **49.96 tok/s** against a 50.0 bar -- four hundredths short on the registered run. **The file weighs 10.77x what a token costs.** The robust number is the CROSSING, measured twice under OPPOSITE arm orders: **`k* = 2.867` and `2.861` -- 516 and 515 of 46,080 neurons, 1.12% activation, agreeing to one neuron in forty-six thousand**; both runs strictly monotone in `k` (the RANK partner E14 sez.3 demands). **Run 2 printed 51.50 on the verdict cell and was NOT allowed to promote it**: its rule was pushed before it ran, its only question was whether the k-slope depends on arm order, and the slopes agreed (+6.2%, inside the reps' 12-19% dispersion) -- so the verdict is unchanged. Reversing the order made the slope STEEPER, not shallower: the fixed order was not manufacturing it. **The finding is prediction 3's MISS.** Splitting cost instead of averaging it: the **base** (attention+head+router) reads **47.2-48.5 G-w/s = the full envelope**, while a **marginal carve group reads 37-39, i.e. `0.80`** -- so `GSZ=180` did NOT buy back the gather and **E31's `0.85` at 11,520 B was optimistic**. **`time = 17.2 ms + 0.93 ms per group`: an FFN weight costs `1.25x` an attention weight, and every table that priced them equally is optimistic on its FFN half.** Corollary: the base alone caps `L=16` at **~58 tok/s with a free FFN**, so **100 tok/s at 4096 wide buys ~8 layers and no FFN -- an attention problem, not an FFN problem** | `probes/E36_TEN_BILLION_AT_FIFTY.md`, ledger sez.46 |
| **E35** (pre-registered `17e7fc8`, pushed before the runner existed) | E34 answered “can the goal's literal shape be reached by shrinking the FFN” with a measured no. This asks what the goal actually asks: **what shape DOES run at 50, and how big can it be?** Depth is the only variable; the FFN is pinned at the floor | **`DEPTH-IS-HALVED`, `L* = 19.33`** (brief predicted 19). **The first arms in this programme to CLEAR the goal at a 4096-wide shape: `T10-L16` reads 56.16 tok/s and `T10-L12` 71.35**, with `L=24` at 40.35 -- so 50 tok/s is BRACKETED BY MEASUREMENT on both sides. **THE ENVELOPE: 0.9451 G active charged weights/token at 50 tok/s**, gather-inclusive (every arm is a carved `quant==4` artifact, so E31's penalty at `GSZ=56` is already inside it), implied numerator 47.254 G-w/s against E34's independent 46.74. At `L=16` that leaves **+0.1120 G = ~10 carve groups = 560 of 14,336 neurons = 3.9% activation** for an FFN. `G-E35A` fires at **-0.8%** against E34's own artifact. **Predictions 3 HIT / 2 MISS and both misses are defects in MY model**: the registered per-layer coefficient is the pure floor while the arms carry the carve residue, so `L*` reads 19.33 as registered and 18.56 residue-inclusive -- **scored as registered, the flattering reading not taken**; and linearity broke at 5.1% on the deepest arm alone, which is a per-layer FIXED cost a charged model cannot see | `probes/E35_THE_FIFTY_TOK_S_ENVELOPE.md`, ledger sez.45 |
| **E34** (pre-registered `684e677`, pushed before the runner existed) | every probe from E18 to E33 attacked the FFN because at T10 it is nine tenths of the weight. **What does T10 read when the FFN is GONE?** If the attention+head floor alone already exceeds the 50 tok/s budget, no amount of FFN work can reach the goal at T10's literal shape | **`FLOOR-IS-THE-WALL`. `T10-FLOOR` reads 20.03 tok/s -- 2.50x short WITH THE FFN DELETED.** The attention+head floor is **2.1475 G charged / 1.0786 GB moved** per token: **2.297x** the charged budget for 50 tok/s, and against E30's measured ceiling **a PERFECT kernel on that floor reads 33.65 tok/s, still 1.486x short.** Residency cannot close it -- 16 MB of L3 is `0.016 GB` of the floor while the gap is `0.353 GB`, **22x the whole L3**. All three gates fire, and the load-bearing one is `G-E34C`: this session's numerator (46.74 G-w/s) predicts E26's already-measured `T10-K4` to `+5.4%` and lands within **1.3%** of E28's independent packed numerator. **Desk arithmetic on the measured ceiling: 31 of T10's 48 attention layers fit the budget, 65% of the depth, with the FFN at zero.** So the remaining lever is the SHAPE OF ATTENTION -- depth, `q/o` rank, KV heads -- not the FFN. Predictions 5 HIT / 0 MISS; run 1 VOID because the un-flagged arm on a carved file inherits the file's `k` and my 'dense' control was the floor arm in disguise -- both planted controls caught it | `probes/E34_THE_FLOOR_UNDER_THE_FLOOR.md`, ledger sez.44 |
| **E33** (pre-registered `2fa83e9`; **addendum `c6090f4` pushed before any artifact was exported**, correcting two of my own gates) | does the carve's cost live where E31 section 6.1 says -- all of it in `down` at `PT_BLK = 64` -- and does a coarser group buy the predicted `1.25x` at S15 / `1.14x` at T10, end to end, in the engine? A probe built to kill a claim of mine that had already been corrected once the same day | **`LOCALITY-PARTIAL`: `1.1164` normalised at S15 (raw `1.1334`) against a desk model of `1.253` -- only **46%** of the predicted gain is real -- and at **T10, the goal's own shape, `1.0432`, below the `1.06` line in every one of five jackknives.** Third cut to the same claim and the second from a measurement: `2.01x` -> `1.25x` -> **`1.12x` at S15, nothing resolvable at T10**. The MECHANISM survives (monotone `E256 < E64 < E16` at both shapes); the SIZE and the usefulness do not. The desk model over-credited because it charged the whole FFN read at the `down` run's granularity when `gate`/`up` -- two thirds of the bytes -- were already flat. Per the brief's own section 6, **E31 section 6.1's arithmetic may no longer price anything.** `G-E33C` FAILS as written (the router is charged and shrinks with `E`; the addendum computed that before exporting), `G-E33C'` / `G-E33A'` / `G-E33B` all fire. **Run 1 VOID and kept: the contention was my own documentation scripts, and the band was not robust to it** | `probes/E33_IS_DOWN_THE_WHOLE_COST.md`, ledger sez.43 |
| **E32** (pre-registered `1fb4c18`, pushed before the runner existed) | does `--lutblk` survive its OWN protocol? E14 disqualified its own verdict in its own section 1 -- no `--seqlen`, so ONE 12,288-token sequence against bands drawn from 512-context regimes -- and E14 section 5, E28 section 8 and E30 section 8 all carried the re-run as owed while the entire engine-side story was measured on the arm that verdict licensed | **`ACTIVATION-COSTLY`, `dBPB(A3) = +0.048990745` at 1.5 B.** THE SIGN FLIPPED: E14 read a GAIN of `-0.016961` at 12 k. `+0.049` is `2.4x` E14's own COSTLY line and ~`10x` `sigma_seed`, and the arms are deterministic so even that flatters it. All three gates fire (E1's published anchor to **2.8e-6**; `A3 - A1 = 0` at both cells; `A0(1.5B)` `-0.594` below chance). **The operative kernel falls back to packed: `0.639` of the ceiling not `0.909`, 46.16 G-w/s not 61.64, `11.46x` from the goal not `8.05x`** -- correction banners at the TOP of E28 and E30. **No architectural conclusion moves** (prediction 6 registered that in advance); the engine is `1.57x` from the wall, so there is MORE kernel headroom than E30 reported. **Constructive half: `--lut-group 32` costs `+0.011739`, a quarter as much -- the expensive thing is the GRANULARITY OF THE ACTIVATION SCALE, not int8 -- and it has never been timed.** Predictions 3 HIT / 2 MISS, both misses mine on the verdict cell | `probes/E32_THE_OWED_PROTOCOL.md`, ledger sez.42 |
| **E31** (pre-registered `8003fa3`, pushed before the runner existed AND before the instrument had ever been timed) | E30 left one variable, bytes per token, at most **1.45 G moved weights** for 50 tok/s — but that came off a DENSE stream, and every road to it (MoE, carve, sparsity) reads a SUBSET. What does a GATHERED byte actually deliver, and how contiguous must it be? | **`GATHER-COSTS`, `r_T10` = 0.579.** One T10 FFN row at a time delivers **58% of the dense rate in the bytes you asked for**, so the real budget is **0.840 G/token = 7.93% of the dense token**, not 1.45. The penalty is not permanent: **0.842 at 8 KB, 0.944 at 32 KB — sixteen consecutive rows — and the crossover to 0.90 is bracketed in (8 KB, 32 KB]**. So: **neuron-granular sparsity costs 42% of the budget, 32 KB blocks cost 6%, and the gap between those two designs is `1.63×`** — against the `1.24×` that was all the numerator had left. **CORRECTED THE SAME DAY — §6.1 first claimed the carve reads one ROW and is worth `2.01×` from a coarser group; reading the engine says `gate`/`up` are group-major and ALREADY contiguous (26.9 KB at S15), and the fine granularity is `down`, required to be `MK_PACKED_T` at `PT_BLK = 64` — one cache line per selected neuron, so `GSZ × 64` = 2,240 B at S15. `down` alone carries the entire carve locality cost, and the coarse-carve lever is `1.25×` at S15 / `1.14×` at T10, NOT `2.01×`. Direction survives, size withdrawn; desk arithmetic on a measured table, not a measurement.** A naive unsorted gather at cache-line granularity delivers **0.197** of the machine. With E30's resident half the goal is finally priced in one line: **~32 M resident and free + ≤ 0.84 G streamed at row granularity, or ≤ 1.37 G in ≥ 32 KB blocks.** Runs 1–3 VOID and kept — the control walked the offset array (moving with the variable it controlled for, in the denominator of every ratio), then reps sat in one wall-clock window until they were interleaved; **the ±15% gate was never widened**. 5 HIT / 1 PARTIAL. Ledger §41. | `probes/E31_WHAT_A_GATHERED_BYTE_COSTS.md` |
| **E30** (pre-registered `63515b4`, pushed before the runner existed) | is the engine still core-bound, or has E13's kernel put it against the MEMORY WALL? — E28 broke the numerator that priced this whole programme and immediately raised the only question that matters: how much MORE is there? The ceiling is measured on this box, never cited | **`AT-THE-WALL`.** `BW-CEIL` = **36.30 GB/s** (planted control `G-E30A` fires at **12.5×**, L3 cliff at 16 MB exactly where Probe-3 put it). T10 moves **5.31276 GB/token**, and `G-E30B` reproduces the artifact **to the byte** — catching the brief's own table, short by the q/k/v biases. `T10-LUTBLK` **33.01 GB/s = 0.909 of the ceiling**; `T10-PACKED` 0.639, so **E10's “core-bound” was right for the kernel it tested and what moved is the kernel, not the machine**. A perfect kernel reads **6.83 tok/s** at this shape — the hard cap on all future kernel work here, **1.10× away**. 50 tok/s needs **265.64 GB/s = 7.32× the whole machine**, and permits **1.45 G moved weights/token** — E28's widened budget is already **85% of the physical bound**. **The numerator is finished as a research direction.** Constructive half: a 16 MB resident core costs **0.18% of a 20 ms token**, so the goal's specification is **~32 M resident + ≤ 1.45 G streamed per token**. Band straddles: pooled ceiling 0.873 (same band), best-ever 0.809 (`PARTIALLY-BOUND`) — **81% of bandwidth against the most generous denominator this box has produced**, headroom ≤ 1.24×. 4 HIT / 1 MISS. Ledger §40. | `probes/E30_IS_THE_ENGINE_AT_THE_WALL.md` |
| **E28** (pre-registered `bb48974`, pushed before the runner existed) | does E13's kernel lever reach the goal's SHAPE? -- every probe since E18 shrank the DENOMINATOR of `tok/s = (G active weights/s) / (G active weights/token)`, while the numerator stayed the constant 49.9 G-w/s from E10 onward and nobody had ever run the fast kernel at `T10` | **`CONTAINER-COSTS`. The numerator was NEVER a constant: `61.64 G-w/s` at `T10` on `--lutblk` against the standing `49.9`, a `1.235x` lift.** Six arms, three interleaved reps, idle box (witness 5.5% mean / 14% peak vs a 25% bar), 254 s. Verdict cell named in advance: **`T10-LUTBLK / T10-PACKED = 1.3354`** (paired 1.3378). **The lever GROWS with the shape -- 0.5 B `1.217x` (E13) -> S15 `1.258x` -> T10 `1.338x` -- the only quantity in this programme that has transferred UPWARD.** Both E25 anchors pass (`-1.35%`, `-7.31%`), `G-E28C` byte-identical logits on both shapes, known-positive `G-E28D` fires at 1.633. **Plain `--lut` is still a trap and gets worse with scale (0.768 at S15, 0.571 at T10), confirming E11's `NO-LIFT` end to end.** Budget consequence: 50 tok/s means `1.2328 G/token`, not `0.998` -- **23.5% more room** -- BUT `donor_engine.c:1437` refuses `--lut` on any container that is not `quant==2`, so **no rank (`quant==3`) or carve (`quant==4`) arm can use the kernel that produced the number**: that gap is now a MEASURED 1.34x and is the next probe. **HONESTY ON THE BAND NAME: the 1.30 boundary sits INSIDE the per-rep dispersion (1.2022 / 1.3640 / 1.4471) and rep 1 alone would read `KERNEL-TRANSFERS`; no rep was dropped.** Against the goal: `4.70 -> 5.81` tok/s dense at `T10`, E27's best COMPARABLE arm scaled reads `10.35`, **gap closes 10.6x -> 8.60x and that is all the numerator can do** (registered as prediction 5 before the run). | `probes/E28_DOES_THE_KERNEL_TRANSFER.md` |
| **E29** (pre-registered `d804b88`, pushed before the runner existed) | does the residual RANK layers, or is E27's headline just "don't touch the ends"? -- E27 beat `L21-LAST`, which deletes layer 27, the SECOND-MOST-ACTIVE block of the 28, so beating it establishes almost nothing. A control on this programme's own best recent result, built to be able to kill it | **`RESIDUAL-RANKS`, and E27's finding survives at HALF the size it was quoted at.** Ten arms, `VOID: none`, 7,049 s, no timing. All five replication gates fire at `abs_diff 0.000e+00` -- E27's `Depth`, `measure_residuals` and `keep_set` are IMPORTED, not reimplemented. The registered three-point ordering HOLDS: **`MAXRES-IN` 78 < `RANDMID` 89.0 (90/87/90, spread 3) < `MINRES` 113**, all dropping 7 layers from the same interior band `[3,24]` at the identical charged cost of `1.2160 G`. **The margin over a FAIR control is `+24`, against the `+56` E27 published over a rigged one** -- E27 s3 and `T4_HEALING_PROPOSAL.md` s10 are corrected at the top and the recommendation STANDS. Post-hoc and NOT a gate (E14 s6): summed dropped residual orders 17 of 20 arm pairs, and the one real exception separates the two stories -- **`LAST` drops LESS residual mass than `MAXRES-IN` and is 21 tokens worse, so the ends matter BEYOND residual mass** (mean rank 3805.8 vs 39.3). `NOADJ` 106 is CONFOUNDED (non-adjacency forces worse layers in) and is reported inconclusive on contiguity. Cliff filled in: `L21` 113, `L18` 94, `L17` 79, `L14` 54 -- the knee is one layer wide. 3 predictions HIT, 3 MISSED, every miss narrow and all in the same direction (I expected less damage than there was). | `probes/E29_DOES_THE_RESIDUAL_RANK.md` |
| **E24** (pre-registered `b78ce4d`, pushed before the runner existed) | the depth the budget permits, with a router that EXISTS -- E23's `ROUTER-COSTS` obliged re-deriving the healing target at a shallower depth, and E23 s7 had already fixed by arithmetic, with the router charged, that the 50 tok/s budget permits `k = 139...156` of 256 while E19's inherited `k = 133` sits 2.8% BELOW the floor | **`DEPTH-RECOVERS`.** Nine composed arms, `VOID: none`, 3842 s, no timing. **`G-U1` reproduces E23's two composed arms at `abs diff 0.0`** -- bit-identical, not merely `< 1e-9` -- which is what proves the `fit_routers_multi` refactor and the once-computed low-rank `q/o` are inert. **The curve is monotone: `k=133` 102 -> `139` 105 -> `148` 110 -> `156` 118 teacher-forced.** **`K156-LINEAR` -- `1.0590 G`, inside E18 s31's `0.982-1.060 G`, with the `11.0 M` router charged AND executed -- reads `118/160`, one token below CHEAPER: the first configuration here that is simultaneously in budget, oracle-free and COMPARABLE. The registered alternative FIRES.** **The oracle/ridge gap HALVES with depth: 24 tokens at `k=133`, 12 at `k=156`.** **The router earns its budget**: `K148-STATIC` (same 148 groups every token, charged at `1.0118 G` WITHOUT a router) reads `96/160` vs 110 -- below even `k=133`'s 102 while keeping 15 more groups -- so the registered THIRD OUTCOME does not fire. `K148-LOG1P` `111` vs `110` is a null, pre-registered and not promoted. Three predictions held, two missed; **prediction 2 (103-106) missed by 12 in the good direction, and the measured slope -- ~1.8 tf tokens per point of activation, five times E19's average -- says the carve's damage ACCELERATES with depth, i.e. the error is in the direction that makes the transfer WORSE.** **The counterweight: this is the 1.5 B's ABSOLUTE active count against an absolute budget and it does not transfer as a fraction.** At `T10` on E25's measured 49.9 G weights/s the same recipe is `6.14 G` = **~8.1 tok/s, not 50**, and the 50 tok/s budget there allows the FFN only **`k ~ 2...8` of 256 (0.8%-3.2%)** at `r=512`/`r=256` and NOTHING with `q/o` dense or at `r=D/3`. **So the lever is the non-FFN FLOOR -- rank on `q/o`, then the head -- not more FFN depth** (E21 s8, E22 s8). **Every tok/s here is conditional on E25's charged-throughput invariant holding for a CARVED arm, which is what E26 part B measures and E26 part B has no valid record yet.** Free-running still AT-FLOOR (15/160). **No timing, nothing exported: 6.79 and `T10`'s 4.70 tok/s untouched.** | `probes/E24_THE_DEPTH_THE_BUDGET_PERMITS.md` |
| **E25** (pre-registered `e506c7b`, pushed before the runner ran) | what does the RANK actually cost? -- E21 s8, E22 s8, E23 s9 and the T4 proposal s6 all closed with the same owed item: `engine.c` had no factored matvec, so every rank result here was a PyTorch number with no runnable artifact behind it | **`RANK-PAYS-WHAT-IT-WEIGHS`.** `donor_engine.c` gains a FACTORED `mat_t` kind (`y = A(s ⊙ (B x)) + b`, two calls to the SAME kernels, intermediate of size `r`) and **`quant == 3`, a TAGGED container** where every matrix carries its own `int32` kind -- necessary because the measured configurations are MIXED (E21/E22/E23 leave `k/v` fp32 while cutting `q/o`) and a single global flag cannot say that. **`G-E25P` FIRES: worst relative l2 `6.445e-04` against a `2e-3` bar, top-1 `1.0000` on 10/10**, on E22's `QO512-TB` -- H0's start state -- against a PyTorch reference running `h0_qat.TernaryLowRank`, the module the T4 trains; **`G-E25a`** holds the legacy paths BIT-IDENTICAL. **THE PLANTED CONTROL is the point of part B**: at `r = D/2` a square projection is byte-neutral to the last weight, so what it loses IS the factored path's own cost -- **`-0.43%` at `T10`, `-2.46%` at `S15`, dispersions 7.3% and 6.4%: neither resolvable from zero**, the extra 56/96 OpenMP regions per token cost less than this box can measure. *(Disclosure: `S15`'s figure is carried by one rep -- 27.55, then 29.39, 29.39; reps 2-3 give `-0.74%`. The outlier is kept.)* **At the goal's shape (`T10`, 10.60 G active) rank-512 `q/o` reads `5.36` vs `4.70` tok/s = `+14.12%` against a `+12.86%` byte prediction -- the registered alternative does NOT fire.** **And the INVARIANT beats any ratio: four `T10` arms whose rates differ by 15% deliver charged throughput inside a `1.54%` band (49.59-50.36 G weights/s)** -- the engine converts active weights into time at a rate that does not care how they are arranged, so rank buys speed through ONE mechanism and `2.D.r` is the right charge. **E18 s31's `0.982-1.060 G` budget maps to 47.0-50.7 tok/s at `T10`'s measured throughput: built on smaller shapes, it brackets 50 at the goal's, measured.** **Distance measured instead of derived: 10.6x, or 9.3x with the rank lever; the FFN is `8.45 G` of `T10` = nine tenths of what is left** (E19's carve, E23/E24's router -- E24 unrun). Two predictions held, one partial, two missed (3 ABOVE its band, the good direction). **Nothing about quality: part B's weights are NOISE, `T10` is a shape and not a model, `T10-R512` is `r/D = 1/8` where E21 validated only `1/3`.** **`6.79 tok/s` untouched -- that is the real 7.072 B.** | `probes/E25_WHAT_THE_RANK_COSTS.md` |
| **E23** (pre-registered `25bde22`, pushed before the runner existed) | a REAL router -- every carve number here (D0, D0c, E19, E22) used an ORACLE that reads the true activation mass, so every one is a CEILING, and `QO512+V52` was the configuration the T4 proposal asked for GPU weeks to heal | **`ROUTER-HOLDS` on the carve, `ROUTER-COSTS` on the COMPOSITION -- and the composition is what the T4 request was about.** A per-layer ridge from the block input to `sqrt(group mass)`, closed form, no gradients, no tuning, `11.0 M` charged. `G-T0`/`G-T1`/`G-T2`/`G-T4` all FIRE, `VOID: none`; both oracles reproduce E22 exactly. **`V52-LINEAR` tf `110/160` vs oracle 117, retention `0.9067` -- THE REGISTERED ALTERNATIVE FIRES: the carve is not an oracle artefact.** **But `QO512+V52-LINEAR` reads `102/160` vs oracle 126, retention `0.7143`, band WORSE.** **The composition CHANGES SIGN: +9 under the oracle, -8 under a real router**, a 17-token swing -- the oracle scores from true post-gate activations so upstream damage cannot reach it, while the ridge reads the BLOCK INPUT that the rank cut already perturbed (mean rank `2.03->5.15` composed vs `2.49->4.17` alone). **So E22 s6's additivity reading was ORACLE-CONDITIONED; five composition measurements now disagree and there is still no law.** **`V52-STATIC` -- the same `k` groups for EVERY token -- reads `99/160`, retention `0.76`, from a router that never looks at the token**: the whole token-dependent decision is worth 11 tokens, and prediction 2 (STATIC below 40, argued from Probe-4) MISSED because Probe-4's i.i.d. finding is per-NEURON and group mass is not i.i.d. here. **And the budget arithmetic with the router charged says `k=133` sits 2.8% UNDER the floor**: fixed `354.5 M`, FFN allowance `627.5-705.5 M` = `k = 139...156`, so the shallower carve the verdict demands is the depth the budget always permitted -- free quality, not a retreat (E24). Three predictions held, two missed. **No timing, nothing exported; 6.79 tok/s exact.** **H0 is router-free by design and is NOT affected.** | `probes/E23_A_REAL_ROUTER.md` |
| **E22** (pre-registered `3f8465a`, pushed before the runner existed) | does cheap COMPOSE? -- E21's `QO-ACT-512` and E19's `V52` are the cheapest cuts on their axes, and the brief's own arithmetic put them together at `0.9441 G` active, INSIDE E18's 50 tok/s budget for the first time. Does the budget-feasible thing still work? | **`FITS-BUT-NOT-IN-THE-FORMAT`.** **Three replication gates FIRE exactly** (`base`; `QO512` = E21 to `< 1e-9`; `V52` = E19 to `< 1e-9`), possible only because the runner imports rather than reimplements. **Half one: `QO512+V52` is the first configuration ever derived inside the budget AND the best-ranking modified donor measured here -- `126/160` teacher-forced, above every E20 ternary head, `+0.237444` BPB, median rank `1.0`, per-prompt `[27,24,22,28,25]` uniform. THE REGISTERED ALTERNATIVE FIRES.** **Half two: that artefact is fp32 and the `0.9441 G` was priced in TERNARY.** Ternarize both factors of the rank-512 form and it reads `2.812226` BPB / `28/160` (balanced; the brief's registered construction gives `1/160`) against T2b's DENSE ternary attention at `1.903569` on a HARDER organ set -- **`+0.908657` BPB worse, because the two factors' errors MULTIPLY** (layer 0 `q_proj`: dense ternary `0.8084`, fp32 rank-512 `0.3515`, two ternary factors `0.9874`). `STACK` reads `3.947669` / free `5` / tf `4`. **The budget-feasible object and the working object are two different objects and the format separates them** -- E18's "train INTO the format" with a target attached. The smoke caught a defect in the brief's own s3 and **both constructions were run and tabled**. **All three E22 compositions are SUPER-additive in BPB** (E21's lone datum was SUB-additive by `-0.3425`): neither is a law, **a stack must be measured on both metrics**. E21 s4a **weakened** -- `V52` vs the best ternary head read `117` vs `117`, the ordering held. Three predictions held, **two missed, both favourable**. Oracle router = a ceiling; free-running `15/160` = it still does not generate. **No timing, nothing exported; 6.79 tok/s exact.** | `probes/E22_DOES_CHEAP_COMPOSE.md` |
| **E21** (pre-registered `24b8832`, pushed before any arm ran) | does RANK buy what precision and sparsity could not? -- E19 closed FFN-only carving and said a carve that reaches the budget must cut ATTENTION and the HEAD; rank had never been measured end-to-end here (D2 computed spectra for three layers and stopped) | **`ATTENTION-YES-HEAD-NO`.** Arms replace donor matrices with the exact rank-`r` minimiser of the **activation-weighted** error `‖(W-Wr)X‖_F`. **`QO-ACT-512` -- rank 512 on all 28 layers' `q_proj`+`o_proj` -- costs `+0.052689` BPB, keeps the donor's token FIRST at 144/160 fixed-context positions, mean rank `1.16`, top-5 at 160/160.** It is the **least damaging structural modification ever measured here** (E20's best ternary head `+0.170414`, E19's `V52` `+0.141846`) and the **only arm in E18/E19/E20/E21 to read `RANK-IS-CHEAPER`**; per-prompt `[30,26,30,29,29]`, **uniform** -- E20's one-prompt trap was looked for and is absent -- and it is the first modified donor that writes CORRECT text. **`G-R2` holds by `-2.44`--`-3.14` BPB**: plain SVD on `q/o` at r=256 lands **ABOVE the chance line** (`4.686939` vs `4.069819`), the weighted one at `1.545880` -- **a matrix that is not low-rank in weight space can be low-rank where it is USED** (weighted energy `0.8937` vs Frobenius `0.3638`), D4 confirmed on a second axis. **But the brief's own configuration FAILS: `BOTH-ACT-256` reads 48/160**, worse than either half alone (68, 93) **while its BPB BEATS the additive prediction by `0.342498`** -- the two metrics disagree in OPPOSITE directions about the same pair, so "il danno si compone" vale per il ranking e NON per la BPB -- and the registered alternative was keyed to that arm and does not fire there. **Two predictions held, two missed.** **Attention tolerates rank, the HEAD does not** (`+0.0527`/144 vs `+0.2781`/101 at equal rank), and **BPB does not order interventions across axes** (`H-ACT-256` and `R0H` within `0.01` BPB, 68 vs 107 teacher-forced). At the validated fraction `r/D ~ 1/3`, `attn+head` on the Coder-7B falls `1.367 G` -> **`1.127 G`, still 1.06-1.15x the ENTIRE budget with the FFN at zero**: **the head is the named binding constraint** (`545 M` = 51-56% of a 7B's budget, resists rank, no lever here shrinks it). **No timing, nothing exported; 6.79 tok/s exact**, and `engine.c` has no factored matvec **-- built and gated in E25, which measures the cut's COST for the first time**. | `probes/E21_CAN_RANK_BUY_IT.md` |
| **E20** (pre-registered `e5d438c`, pushed before any arm ran; run 1 VOID `6c7b7a4`) | is the constraint the RULE or the FORMAT? -- E16 sez.9 promoted "format over rule" while recording that only 2 of 4 rules were tested, and no rule had EVER been read in ranking | **`RULE-EXHAUSTED-DRIFT-DOMINATES`.** **Eight ternary heads, one format** (ternary codes + one fp32 scale per row, same bytes, same kernel, everything else bit-exact): `R0H` 17, `R1H` 13, `R2H` **41**, **`R3H` (SHIPPED) 9**, `R4H` 15, `R5H` 9, `OPTH` **42**, **`GPTQH` 11 of 160**. **The best head buildable here costs `+0.170414` BPB, sits `3.131810` below chance, and is BELOW the `12/160` floor** -- the registered alternative does NOT fire and **E16's rule qualifier is discharged on six rules**. **`G-Q2` VOIDed run 1 by catching the BRIEF'S OWN PREMISE**: the shipped rule is `r3_actsearch`, activation-weighted, head included -- and **GPTQ was already in the repo** (T2 `R4` 4.299819, `R5` 2.027495, **the best rule ever measured here and NOT in the exporter**). In run 2 it hits **two anchors from two probes**: BPB to `1.09e-08` of T2b and `9/160` exactly E18's. **r(BPB, agreement) = +0.6238, wrong sign, third time.** **The PARTIAL band E14 owed arrives and dissolves on inspection**: `R2H`/`OPTH` are **one prompt reproduced WHOLE** (32/32, 18 distinct ids) plus floor noise, and that prompt is the only one with **no near-tie** (min top-2 gap `1.1630` vs `0.0081`-`0.2740`) -- **160 positions are FIVE trials of thirty-two, effective n = 5.** **Part B (unregistered, reports only): with the donor's context held fixed EVERY ternary head keeps the donor's token first at 67-74%** against 6-26% free-running; the **75-106 token gap is DRIFT**, and the exact identity `G-B1` holds **50/50**. Data-aware rules reach **mean rank 2.40-2.98** vs 12.85-16.92 -- **they preserve the logit geometry, not the top-1/top-2 boundary.** Survival tracks the donor's margin: **94-98%** top tercile, **38-47%** bottom. **No timing; 6.79 tok/s exact.** | `probes/E20_RULE_OR_FORMAT.md` |
| **E19** (part A + brief `3173edc`, pushed before part B ran; runner `bdd87a4`) | does a CARVE rank -- the one structural lever that can reach E18's ~10% activation budget -- and how deep would it have to cut? D0/D0c scored every carve arm in BPB ONLY, the same omission E18 caught in T2b | **`CARVE-DOES-NOT-RANK`.** **Part A, before the run: FFN-only carving cannot reach 50 tok/s at ANY depth** -- on the Coder-7B `attn+head` alone are **1.367 G** vs a budget of **0.982-1.060 G**, so a **zero-FFN** 7 B still sits at **35.9-38.8 tok/s**; a 10 B at the same measured share has a **1.82-2.37x** uncarvable floor. **Part B: `base`/`FULL` 160/160, then `V52` 12, `S1` 7, `A0` 5, `N0` 6, `D10` 3 -- every carved arm AT-FLOOR**, under an **oracle** router (a ceiling). **`V52` keeps 52% of the FFN for +0.141846 BPB, 3.160 BELOW chance, and scores EXACTLY the constant-`\n` floor of 12/160.** Controls at zero error: `FULL` token-identical, and **G-C2 reproduces D0c's four BPBs to 0.000e+00**. **r(BPB, agreement) = -0.8562** -- right sign, and all five points still at/below the floor. **Co-activation's 0.720513 BPB advantage buys NO ranking** (5 vs 6). Three called directions, **three misses**. **No timing; 6.79 tok/s exact.** |
| **E18** (part B pre-registered `2ac74f5`, pushed before any arm ran; part C `b02a942`) | what does the ladder from «nothing converted» to «everything converted» look like, and is any rung of it fast enough? -- E17 section 8's owed floor, plus the first ranking read of the five organ arms T2b had only ever scored in BPB | **`CLIFF-NOT-SLOPE`.** The floor is **not zero**: the best CONSTANT predictor scores **11/160** (0.5 B) and **12/160** (1.5 B), both `'\n'` -- **every ternary arm ever built here is at or below it**, and E17's best was exactly `12/160`. On the ladder, `base` and the identity arm hold **160/160**, then **`H` 9, `A` 4, `F` 5, `FA` 12, `FAH` 10, all AT-FLOOR**: **ternarizing ANY single organ destroys ranking**, including the head alone at **+0.338989 BPB**, `2.963` BELOW the chance line, `9/160`. **`G-L2` reproduced the engine EXACTLY (12 and 10) before any empty rung was read.** `r(BPB, agreement) = **+0.4989**` across a `2.377667` BPB span -- the wrong sign. **Speed axis, derived (no timing, `6.79 tok/s` untouched): 50 tok/s = 0.98-1.06 G active ternary weights = 9.8-10.6% of a 10 B model, and the fastest rung -- the all-ternary one -- is 6.9x short.** Three called directions, **three misses**; the brief's registered ALTERNATIVE carried the verdict. **Scope: no healing/QAT -- the one untested branch.** |
| **E17** (pre-registered `cf33251`, pushed before any arm ran) | is the ternary HEAD what fails to rank? -- E16's arms were all `--head-ternary`, and on the untied Coder-7B that is a standalone 545 M-parameter output projection | **`HEAD-IS-NOT-THE-MECHANISM`.** Removing it buys **2 tokens of 160** at 1.5 B and **0** at 0.5 B, both still diverging at token 0 -- far below the `20/160` bar derived in advance, so the registered stage-2 export **does not run**. Nothing exported; every artifact already on disk. All four controls fired first, including a NEW third known-positive (1.5 B fp32 `160/160`, which also closes E1 s4.4 in the generate path) and `G-H0d`, which proves E6's and E16's binaries are token-identical and is the only reason the population table is licensed. **The head is not inert but IRRELEVANT** -- it rewrites 49% / 82.5% of the output while changing correct tokens by 2 / 0. **Population: 3 known-positives at exactly `160/160` across three scales and two families; 7 ternary arms across three scales, two rules, two folds, both head settings -- best `12/160`, all diverging at token 0. BPB spans `1.823493` across them while ranking spans 0-12/160**: inside the ternary regime BPB has resolution and ranking has none. **New debt: no floor for agreement by frequency coincidence, so `12/160` is not yet known to beat zero information.** No speed number moves | `probes/E17_DOES_THE_HEAD_RANK.md` |
| **E16** (pre-registered `be65920`, runner `2247923`, control `34e7ba3`, all pushed before the arms ran) | is the RULE what E15 found broken, and is there a ternary 7 B that predicts? -- E7 section 12 owed item 3 | **`SCORE-CROSSES-RANK-DOES-NOT`.** Three arms, one axis each. **`R0` -> `R3` with the fold and all else FIXED is worth `G-R2 = -1.281967`**, carrying the 7 B from +1.229094 above the chance line to **4.017233, -0.052874 BELOW** = 59x the band, so G-R1 reads **`RULE-FIXES-IT`**. **But every ternary arm agrees with the fp32 donor on 0 of 160 greedy tokens, diverging at token 0** -- B2 is 0.155 nats/token better than uniform and cannot rank the donor's next token once. **The label is true of the band and false of the model, and the brief registered ONLY scoring metrics** -- caught by E14's day-old law (pair scoring with ranking); the ranking run reports and decides nothing (E14 s6), and validates itself by reproducing E7's `0/160`. **G-R0 fires at 3.20e-07** against E1. **R3 is NON-MONOTONE in scale** (+0.461 / -0.594 / **+0.098** at 0.5/1.5/7 B): its damage grows with scale as `R0`'s does, one octave later. **The fold is load-bearing** (`G-R4 = -0.151093`) and is the only thing E15's artifact had right; its credit shrinks -0.529 / -0.220 / -0.151. Design gate passed before any BPB was read. **No speed number moves.** Predictions: G-R0 inside, G-R1 direction right and band missed by 0.018, **G-R3 direction WRONG** -- a trend extrapolated from two points, which the brief admitted could not establish curvature and leaned on anyway | `probes/E16_R3_AT_7B.md` |
| **E15** (pre-registered `7c4243f`, amended `c92ffd4`, both pushed before any arm ran) | does the packed Coder-7B that EVERY donor tok/s is quoted on actually predict? -- E7 section 5's "separate experiment nobody has run" | **`DOES-NOT-PREDICT`.** Full 24x512 heldout slice, `--seqlen 512`, chance **4.070106**, band **0.000896**. **`qwen25-coder7b_p.bin` reads 5.299200075 = +1.229094 ABOVE chance -- 1,372x the band, worse than guessing uniformly over the vocabulary.** **The planted control FIRES and fires hard**: the fp32 arm reads **0.674026555, -3.396080 below** the line, so **the engine is sound, the donor is sound, and it is the CONVERSION that is broken.** The 7 B is in fact **the best model this programme has ever run** and the fp32 sweep is monotone in scale on the identical slice (0.871795 / 0.767595 / 0.724450 / **0.674027**) -- a code-specialised donor winning on a prose-majority corpus, and the first evidence that bigger donors keep paying. **E7's two headline halves are about DIFFERENT artifacts**: the fp32 arm is the `160/160`, the packed arm is E7's planted control at `0/160` diverging at token 0. **No speed number moves** -- a ternary weight costs the same bandwidth whatever scale multiplies it -- **6.79 tok/s exact stands; what E15 removes is the right to call it a rate for a WORKING 7 B.** Predictions scored: the control's 0.55-0.85 **contained** 0.674027, while the amended 3.4-5.2 **missed** 5.2992 that the withdrawn 4.6-6.5 would have caught -- the amendment composed measured deltas across THREE configurations, which the brief had itself named as the move E12 forbids. **Owed and load-bearing: B2 -- re-export with `--rule R3` keeping `--fold layers`, so the rule is the only variable.** R3 reads 3.475707 at 1.5 B, **0.594 BELOW** the line | `probes/E15_DOES_THE_7B_PREDICT.md` |
| **P64 matrix** | is the runtime's rate set by OpenMP region count or thread wake-ups? | **neither.** A region costs **2.5–3.2 µs**, measured two ways; `OMP_WAIT_POLICY=active` does nothing. After the rope hoist all four organs sit in a **1.3× band** | `SPEED_LEDGER.md` §12.4, `engine/bench_matrix.py` |

**Terms nobody had attacked before the ledger, now priced:** the **output head** (was 40.4% of every
token in fp32 — fixed; and E3 shows it is **not** the floor at 10 B, §7), the **KV cache** (still
fp32, untouched — and E3 says this is now the binding term), the **attention projections** (17.9% of
a 10 B token, `qkv` + `o_proj`).

> **E4 answered the question E3 left open, and moved the section on again.** The attention loop was
> latency-bound and is not any more: with `R` (softmax + `A·V`) measured and subtracted, the `Q·K`
> loop went **6.53×** and now runs at **35.1 GB/s of unique K bytes** — this machine's DRAM. Two
> consequences. First, **the int8 KV cache named below is retired at ~1.16×** before being built:
> it cuts the dot loop's bytes 4× but the dot loop is only 18.6% of the organ now. Second, **the
> binding term is `R`**, 81.4% of the organ and untouched by anything measured so far. The GQA
> re-reads do **not** reach DRAM — 140 GB/s of touched bytes is not achievable from it — which E3
> explicitly declined to claim and E4 gets for free from the arithmetic.

> **E3 changed which term this section is about.** The weight path is *faster* than the ledger says
> at every shape above 0.5 B. What does not scale is `f`. E3 §4.6 measures the attention loop at
> **one FMA per ~4 cycles per thread across twelve points, ±7%**, invariant to KV size (16× range),
> GQA factor and vocabulary — the signature of a serial FP reduction, not a bandwidth wall:
> `d += qh[i]*kt[i]` compiled without `-ffast-math` cannot be reassociated or vectorised, while
> every matvec is hand-written AVX2 with an 8-wide accumulator. **The single highest-value engine
> experiment now open is giving that loop independent accumulators.**

### 2.1 The LUT kernel — `donor_engine.c --lut`, the lever P2 and R1 named

| what | status |
|---|---|
| tile-major layout, `pshufb` tables, grouped scales | **built.** A `--lut` model and a `--quant packed` model hold **bit-identical weights** — the copy is a pure transpose |
| kernel correctness | **bit-exact** vs a scalar-integer reference; two planted controls fire (`--selftest-lut` cases B and C) |
| numeric cost of the int8 activations it requires | **measured.** rel l2 `1.40e-01` per-vector → **`3.10e-02` at G=32 channels per scale** |
| why it costs that | activation crest factor `amax/rms` is 8.3 avg / 69.6 max, so a 63-step grid leaves ~4–8 usable levels. `--lut-diag` measures it on `x` alone, so the kernel is not implicated |
| **rate** | ⚠ **measured: 1.022×** after the rope hoist (`SPEED_LEDGER.md` §12.4). It helps the two big-matrix organs (ffn −0.40 ms, head −0.33) and **hurts the two small ones** (qkv +0.31, o_proj +0.06); `--fuse` first removes that penalty and the pair is worth 1.039×. **Not the 1.83× the ledger assumed** |
| BPB through the runtime | ❌ not run |

Two predictions were written before their sweeps and **both were wrong in magnitude**: clipping the
grid to `k·rms` (predicted an interior optimum; it is 3× worse at every `k`) and per-group scales
(predicted <0.02 at G=128; it is 0.052). Recorded in commits `95b7fd3` and `e02285c`.

## 3. The quality side — every structural result, and the conversion that undercuts them all

| probe | what was tried | outcome | where |
|---|---|---|---|
| **D0 / D0c** | carve the FFN into co-activation experts | **+1.09 BPB at E=32; +0.706 at the finest legal E.** Granularity helps, but the random null helps *more* → the mechanism does not strengthen (PARTIAL) | `probes/D0_COACTIVATION.md`, `probes/D0C_GRANULARITY.md` |
| **D1** | magnitude / structured pruning | see report | `results/d1_pruning.json` |
| **D2 / D3** | basis rotation, low-rank | see reports | `results/d2_basis.json` |
| **D4** | solve for thin replacement weights (Hessian) | recovery 0.483 honest vs 0.859 leaked → in-sample optimism; budget never swept | `probes/D4_RECONSTRUCTION.md` |
| **S1** | which bar predicts BPB under sparsity | \|h\| is the bar; A≡D at every digit | `benchmarks/donor_adaptation/s1/` |
| **T1** | **ternarize the donor — the engine's own rule** | **+4.738 BPB = 948 σ_seed. CONVERSION-FAILS** (measurement stands; verdict superseded in scope by T2) | `probes/T1_DONOR_TERNARIZATION.md` |
| **T2** | was that the FORMAT or one naive RULE? | **`RULE-HELPS`. It was the RULE.** FFN +3.309 → **+1.260**, 62% removed with no training. **BitLinear158 is statistically indistinguishable from RANDOM SIGNS** (−0.064 ± 0.126) | `probes/T2_TERNARIZATION_RULE.md` |
| **T2b** | does the winning rule survive outside the FFN? | **`UNIFORM`, by 1.0% of its bar.** The runnable model (197 tensors, R3) costs **+2.708**, `1.584×` the FFN alone vs a 1.60 bar — ci95 `[1.514, 1.649]`, the bar is INSIDE it. Head ternarization **not** withdrawn: the head costs `+0.339` alone and **`−0.009 ± 0.020` on top of a ternary FFN+attention**. Per weight **attention is 4.98× the FFN** | `probes/T2B_ORGAN_COVERAGE.md` |
| **T3** | rotate the residual basis (QuaRot/SpinQuant) before ternarizing | **`VOID` as written, `NULL` once the brief's own gate constant is corrected.** Rotation does not fail to help, it **hurts**: `+0.634 ± 0.039` (dense orth) and `+1.138 ± 0.072` (Hadamard) vs a fold-matched control. Kurtosis is not a predictor and cannot be made one. **Keeper: the RMSNorm fold alone is `−0.220 ± 0.053` free** | `probes/T3_ROTATION.md` |
| **E1** | does the model the ENGINE executes score the BPB PyTorch says it does? | **`LOOP-CLOSED`** on the 0.5B; every term of the same rule met on the 1.5B (labels there are `INCOMPLETE` because the brief split the arms across two runs). Largest disagreement over 5 arms and 2 donors **`+1.53e-05` BPB** = `0.003 sigma_seed`, and the delta **does not grow with the arm**. Gate A bit-identical on up to **1,543,569,408 codes**. T2b's `+2.708111` replicated **bit-identically** by a second runner through a file round-trip. **Found on the way: the engine could not load a model over 2 GB** | `probes/E1_BPB_THROUGH_ENGINE.md` |
| **E2** | does T3's RMSNorm fold survive into the artifact the engine runs? | **`FOLD-CONFIRMED`, and adopted.** `NL - TQ = -0.220001`, paired SE `0.052861`, ci95 `[-0.324988, -0.119593]`, 44 sigma_seed on the 1.5B -- **T3's point AND its dispersion**, now through the exporter, in the file format, on weights Gate A proves identical. The **runnable** model goes `+2.708111` -> **`+2.465779`**. Gate F passes on the deciding donor with 4.2e+04x of margin. **`--fold all` is NOT adopted**: folding the final gain into a *ternary* head COSTS `+0.176983` (1.5B) / `+0.629949` (0.5B). **Every sign replicates across donors; no magnitude does, and one comparison inverts** | `probes/E2_RMSNORM_FOLD.md` |

**T2's decomposition, paired between arms** (`probes/T2_TERNARIZATION_RULE.md` §4):

| what | Δ BPB | significant |
|---|---|---|
| searching the per-row scale at all | −0.686 | yes |
| **weighting that search by activation RMS** | **−0.914** | yes |
| GPTQ error compensation **on a well-placed grid** | −0.449 | yes |
| GPTQ error compensation **on the naive grid** | +0.223 | **no** |
| TWN instead of BitLinear158 | −0.225 | **no** |

> ⚠ **Every structural result above was measured on fp32 weights the engine cannot consume**, and
> none was ever composed with the conversion. "Does carving a *ternarized* donor cost the same as
> carving an fp32 one?" has never been asked — and it is now a cheaper question than it was, since
> the conversion it would have to compose with costs +1.260 rather than +3.309.

## 4. Open, in priority order

0. **Decompose `P`** — new, and it inherits first place from the item **E5 just closed**.

   **E5 is CLOSED, `OVERHEAD-DOMINATED`** (`probes/E5_DECOMPOSE_R.md`, brief §14). At `T10` @800,
   `R` = softmax `S` **25.3%** + `A·V` `Y` **20.6%** + `P` **54.0%** — **more than half of `R` is
   neither loop.** Nine 3× predictions the components were not fitted to all passed, worst error
   −1.15%; `X` = 2.253 ms independently reproduces E4's 2.242 by a method sharing no arm with it.
   Taking E5's shares onto E4's own `R` = 9.816: **`S` = 2.485, `Y` = 2.020, `P` = 5.301 ms.**
   The one candidate for `P` that has been priced — the OpenMP fork, run 5's `fork2` arm — is
   **1.4% of it**. So `P` is not the fork, and what it *is* has never been measured: the
   32-heads-over-6-threads imbalance, per-head address arithmetic, `out[]` init, the `mx` reduction.
   **That is item 0.** Two further things E5 hands over: the shape *inverts with scale* (`S05` @800
   is **SOFTMAX-dominated at 47.7%**, `T10` is OVERHEAD-dominated), so a softmax fix measured small
   arrives at target scale worth half what it looked; and `S` came in **48% above** its
   pre-registered band, with `expf` compiling to a *double-precision* `exp` call plus `vzeroupper`
   as the untested candidate.

   ~~**Decompose `R`** — inherited first place from the item E4 closed.~~ **Done.**
   **Pre-registered and running as E5**: `briefs/BRIEF_E5_DECOMPOSE_R.md` (`c1bdd70`, pushed before
   the arms), arms `--attnr {sm2,sm3,av2,av3,fork2}` at `00f4538`, G2 passed **bit-identical on all
   six arms**. The brief adds a third term this row did not name — `P`, the OpenMP region and
   everything that is neither loop — because its own arithmetic for the two loops reaches 3.0–5.5 ms
   against a measured `R` of 9.816, and pre-registers that 4–7 ms of the organ is in neither. `R` = the
   softmax pass + the `A·V` loop = **9.816 ms/token** at `T10` @800, **81.4% of the attention organ**
   after `avx4`, and the organ is ~97% of `f`. Nothing has ever measured what is inside it. The
   method is already built and already validated: E4's planted control split the organ by running the
   `Q·K` loop 2× and 3× **bit-identically** and predicting the third point to −0.56%. Do the same to
   each half — an arm that runs the softmax loop twice, an arm that runs `A·V` twice, both
   value-preserving — and `R` splits the same way. Cheap, engine-only, no format change. Candidate
   levers once it is split: the softmax `sum` is a serial reduction over `pos` with a scalar `expf`
   per element (`L*NH*pos` = 616 k calls/token at `T10` @800), and `A·V` reads V at the same byte
   rate the `Q·K` loop reads K.
   **~~The attention accumulator~~ — CLOSED by E4, `LATENCY-CONFIRMED`** (`probes/E4_ATTENTION_ACCUMULATORS.md`).
   The dot loop went 6.53× and `f` halved. **The int8 KV cache that E3 named as the alternative lever
   is retired at ~1.16×** — it was the right lever for the loop E4 already fixed.

0-bis. **A real 10 B donor.** Opened by E6, **half-closed by E7** (`probes/E7_REAL_LARGE_DONOR.md`, `REAL-WEIGHTS-CONFIRMED`): Qwen2.5-Coder-7B, **7.07 B active/token = 67% of target**, passes every gate — 30.46 GB loaded exactly, parity 1.07e-05, **160/160** greedy tokens against PyTorch — and decodes at **4.460 tok/s** (3 reps, spread 0.45%), **11.2× short of 50**. The prediction written before the export, 4.66, was off by −4.3%. **What remains is the last 33% of the size, and it needs a download** (~28 GB for a real 10–14 B); nothing bigger is on this disk. The original statement of the item, still worth reading:

   ~~**A real 10 B donor.** Opened by E6.~~ Every measurement at target scale in E3, E4 and E5 was taken on `T10`, a **synthetic shape file** — correct dimensions, no trained weights. E6 showed a real donor generating text and could only do so at **0.5 B and 1.5 B**, because those are the only real weights on disk. So "10 B at 50 tok/s" currently has a measured *speed* and no measured *model*, and nothing has ever run end to end at the size the goal is about. Exporting one is a conversion job, not a research question, which is exactly why it keeps not happening.

1. **D4b** — the calibration budget. Promoted from bookkeeping: T2's two best arms are both
   calibration-driven, so every one of their numbers is a **floor** — and after E2 that now
   includes the fold's own `−0.220001`, which was measured at 32 calibration sequences like
   everything else.
2. **Healing** (QAT / layer-wise distillation) — still on the critical path per T2 §7. **Its
   starting point moved**: the whole runnable model is now **`+2.465779`** (E2 arm `NLH`), not
   `+2.708111`. T2b §6 says where to aim it: **attention, 4.98× the FFN's damage per weight at 10%
   of the weights**. Note that T2's GPTQ result — at ternary width the per-layer error is the wrong
   objective on a badly-placed grid — constrains what "layer-wise" is allowed to mean here.
3. **Why the same fold buys on the layers and costs on the head** (E2 §5.4). `q,k,v,gate,up` gain
   `−0.220`; `lm_head` loses `+0.177`. E2 killed the obvious explanation — zero fraction does not
   predict it and does not even hold its sign — and put nothing in its place. The per-row structure
   of R3's threshold search against the per-column structure of a gain fold has not been measured.
   Cheap, exporter-only, and it is the one place the fold left value on the table.
4. **S1's scale arm** — blocked on the fp16 NaN (`eager` attention overflows QK^T; diagnosed, §5).
   Every sparsity result this programme owns is measured at one size.
5. An already-MoE donor, and **a donor with a small vocabulary** (§7). E3 sharpens both: a small
   vocabulary buys much less than §7 used to imply (Mistral-7B is still at **141% of budget at 800
   context** with the smallest vocabulary on the disk), and **nine of eighteen donors — every MoE
   and every hybrid — have no measured `f` at all**, so their screen rows are withdrawn, not
   restated.
6. ~~**`f` beyond 800 tokens of context.** It grows with position and nothing measured bounds it.~~
   **CLOSED by E7 §8.2** (`probes/E7_REAL_LARGE_DONOR.md`): **linear, 0.0319 ms per token of actual
   context, two intervals agreeing to 0.2% over a 5.3× range in context, no knee to 1600.**
   Measured on the real Coder-7B, with contended cells excluded by the `ffn`-invariance witness —
   six of nine survived, and the three discarded would have manufactured a knee between 300 and
   800. `1000/f` may now be extrapolated linearly to at least 1600 tokens.

**Closed since the last revision.** The `--fuse` × `OMP_WAIT_POLICY` matrix ran (`SPEED_LEDGER.md`
§12.4 — both hypotheses die; `--fuse` not adopted). T3 ran and closes the residual-stream rotation
line together with brief §5's two follow-ons (online Hadamard, activation-side rotation).
**E1 ran** (`probes/E1_BPB_THROUGH_ENGINE.md`): the engine scores what PyTorch scores to `1.5e-05`,
T2b's `+2.708111` is replicated bit-identically through a file round-trip, and the 2 GB load ceiling
that would have blocked the target model outright was found and fixed.
**E2 ran and closes the standing item 1** (`probes/E2_RMSNORM_FOLD.md`): the fold survives to the
artifact at T3's exact value **and its exact dispersion**, Gate F passes on the deciding donor with
4.2e+04× of margin, and it is **adopted** — `qwen_export.py --fold` defaults to `layers` as of
`49b6654`, with `--fold none` pinned in E1's runner in the same commit so its published numbers keep
reproducing (both directions checked by sha256 against artifacts already on disk). `--fold all` is
measured and **rejected**: into a ternary head the final gain costs BPB on both donors.

## 5. Bugs found in our own instruments (all fixed, all with controls added)

| bug | how it presented | fix |
|---|---|---|
| **A floor prediction charged the wrong bytes and the measurement beat it** | E4's brief predicted the `Q·K` loop could not go below **5–8 ms** because "315 MB at 50 GB/s is 6.3 ms". It measured **2.242 ms**. The 315 MB is **touched** bytes (GQA re-reads); only the **78.7 MB unique** reach DRAM, at 35.1 GB/s | the two conventions were already law here — a ceiling is a denominator, and this is where the wrong one hid. E4 §4.3 reports the prediction as wrong beside the number that beat it |
| **A gate compared across sessions and could not be answered** | E4's G4 required `serial` to reproduce E3's published tok/s; it missed by +3.4% to +7.0%. My first diagnosis blamed an address-arithmetic hoist — priced, that is **±2% with inconsistent sign**. Rebuilding **E3's own binary** and running it in the same session showed it misses **E3's own table** by −1.6% to −4.2% | the gate is `MALFORMED`, not failed. Any reference from another session must be **re-measured in the same sweep**; E4's run 2 re-timed all seven arms under one binary, which is the only reason the probe survived |
| **The A1.2 gate reported PASS over an all-NaN run** | `max(0.0, nan) == 0.0` in Python swallowed all 51 comparisons | hard-fail on any non-finite, minimum comparison count, and **7 planted self-test cases run before any model loads** — the old code fails 3 of them |
| **`l1_keep_count` turned a NaN into a plausible measurement** | `clamp_(1, F)` mapped a NaN row to "keep 1 neuron" → achieved sparsity `8959/8960` | raises `FloatingPointError` on non-finite input |
| **fp16 NaN blamed on the GPU** | my own diagnostic did not pass `attn_implementation` and tested SDPA, not the `eager` path the probe uses | reproduced on CPU with `--attn eager`; cause is HF eager computing QK^T in fp16 (**274,672 vs the 65,504 limit**) before dividing by √head_dim |
| **T1's planted control was mis-specified** | required random signs ≫ ternarization; they are only 1.21× apart, so it returned VOID on sound numbers | identity-substitution control added (bit-exact). **T2 §4 then showed the premise itself was false**: random signs are not ≫ the treatment, they are indistinguishable from it |
| **`--calib-seqs` defaulted to 8 while T2 measures at 32** | exporting `--rule R3` would have built a model on a quarter of the calibration budget that produced the number, and the BPB gap would have read as the runtime disagreeing with PyTorch | default → 32, stderr warning otherwise, and the sidecar records the budget and the organ lists |
| **the LUT diagnostic reported whole-vector crest while groups were active** | it kept calling the derived figure "effective levels" when the grid was per-group, i.e. a plausible number describing the wrong thing | crest computed in-group, both labels corrected, header states which scale is in force |
| **both `rope()` calls sat inside the `qkv` timer** | the per-organ table read qkv at **4.1 GB/s** against the head's 17.6 — a 4× anomaly that does not exist — and two experiments were built to chase it. It also hid that rope was **9.6% of every token** | `T_ROPE` is its own bucket; rope hoisted out of the head and layer loops (**+10.3%, bit-identical**); §11.4 marked superseded and its two derived claims withdrawn in §12.1 |
| **the bench harness called an idle machine CONTENDED, twice** | once because it compared an 800-token run to a 300-token reference (attention is `O(position)`), once because min-max over 6 rounds is set by a single bad round | reference must match `--bench` length; the gate is now the IQR; the harness prints which of its two blocks is readable |
| **a pre-registration contradicted itself and the gate fired on the contradiction** | T3 returned `VOID`: brief §3 fixed the organ set at FFN+attention (196 tensors), brief §4 pinned the replication constant to T2's **FFN-only** Δ (`+1.709372`, 84 tensors). The runner obeyed both halves and arm Q missed by 1.007 | the constant was checkable and was checked: arm Q reproduces T2b's arm FA **bit-identically** from a different runner. **A replication constant must name the arm, the organ set and the file it came from**, so a mismatch with the arms section is visible on the page |
| **`--quant ternary` had been dead since `--rule` landed** | `w_tern` squeezed a scale that `quantize()` already returns as a `[out]` vector → `IndexError`. Nothing caught it because every artifact this programme built used `--quant packed`, which does not go through that line | the second squeeze removed; verified empirically before the fix, not assumed |
| **two exports of the same command produced different files** | R3's calibration forward changes its reduction order with torch's thread count: 6 vs 1 threads moved **102,123** `act_rms` elements (worst `1.9e-06`), so the sidecar recorded a sha256 it could not reproduce | `--threads`, recorded in the sidecar. **The thread count is part of the artifact's identity, not a speed knob** |
| **the exporter was not loading the model the probes measured** | it omitted `attn_implementation`, so HF gave it **sdpa** while `common.load_model` — and therefore T1, T2, T2b, T3 — uses **eager**. E1's Gate A fired at `2.980e-08`, exactly one ulp. Measured: `act_rms` differs on **142,977** elements (worst rel `8.2e-06`), moving **132,844** stored scales by up to **6 ulp**. All **357,826,560 codes were identical throughout** — the artifact's *identity* was wrong, not its content | `eager` pinned in the exporter and recorded in the sidecar. Gate A then passed **as pre-registered**: 0 scales differing, 0.00 ulp. Same law as the fp16 row, in a second place: **reproduce the configuration, not just the model** |
| **the best quality claim had no artifact** | `qwen05b_packed.bin`'s sidecar has no `rule` field — it predates `--rule`, so every speed number was taken on **R0/BitLinear158** (zero fraction `0.327`), the worst of T2's five rules. **No R3 model had ever been exported.** The format is identical so the speed numbers stand | E1 exports R3 (zero fraction `0.4922`, consistent with T2b's `0.4714` at full budget). Found by reading the sidecar of the file on disk rather than the command that was supposed to have written it |
| **the engine could not load a model over 2 GB, and blamed the file** | E1's 1.5B fp32 arm died with `FATAL: bad magic -- not a QWENDON1 file` on a 6,174,857,268-byte export whose magic was intact. `long` is 32 bits on Windows even on x64, so `fseek(SEEK_END)` FAILS above 2 GB and `ftell` reports 0 → a zero-byte blob was allocated, zero bytes were read (**which equals the zero requested, so the `short read` guard passed**), and `memcmp` compared the magic against an empty buffer. The largest file the engine had ever been given was 1.84 GB, just under the ceiling. **A 10B ternary packed model is ~5 GB: the target was unloadable, and no speed probe could have found it** | 64-bit offsets chosen by platform (`_fseeki64`/`ftello`), a real error when the seek fails, 1 GB chunked reads. **Planted control before the rebuilt binary produced any number**: it re-scored the measured 0.5B TQ artifact at `NATS_TOTAL 162120.4241599279`, bit-identical. **Known-positive**: the 5.75 GB file then loaded and scored |
| **a sweep's `untied` field recorded the wrong thing, and arm state leaked** | `t2b_organs.json` says `"untied": false` on arm FAH, an arm that ternarizes a *tied* head — which would mean the embedding was ternarized too. It was not: E1's arm TQH reproduces FAH **bit-identically**, and two different models cannot. Arm `I` reports `untied: true` and every later arm `false`, because T2b's `restore()` restores weights but never re-ties | no number changed and none is withdrawn — arm `I` returns the base BPB exactly. Recorded because it was caught by a **replication**, not by the sweep: **the field means "did this arm untie", not "is the head untied here"**, and state crossed arm boundaries |
| **a result file was named after the model alone** | E1's pre-registered 1.5B fp32 **subset** run was about to overwrite the 55-minute TQ/TQH JSON written under the same name | a subset run now carries its arms and sequence count in the filename; only the canonical run keeps the bare name |
| **a gate that was never evaluated reported that it had failed** | E2's run 2 carries only the five ternary arms, so Gate F -- which is built from `XF`/`XA` against `F32` -- would have found no arms to compare, left `gate_f` empty, and printed `VOID (Gate F failed: the fold is not exact where it must be)`. The run owning the **label** would have reported the fold inexact on the strength of nobody having looked, and the brief itself had pre-registered the split that causes it (§3.2) without foreseeing what the runner would print under it. The same brief also predicted run 1 would return `INCOMPLETE`; run 1 holds all eight arms and returns a real label | `gate_F_measured` separated from `gate_F_ok`; `VOID` is now reachable only from a gate that was measured and failed, and a run missing the gate prints the fold term plus `GATE-F-NOT-MEASURED-HERE` naming the run that owes it. Brief §3.3 written before run 2, changing no threshold or arm. **All three states planted and shown to fire** (`3aa1bef`), the failing one via a copy with `GATE_F_FP32_TOL = -1.0` -- without it the patch would only have been shown not to say `VOID`, and a guard that never fires looks identical to one that fires correctly |
| **a budget formula charged the same milliseconds twice** | `SPEED_LEDGER` §12.2 computes `Delivered: 493,961,216 / 17.833 ms = 27.7 G-weights/s`, where **17.833 ms is the wall** — so the 1.17 ms of non-weight work is already inside the denominator. It then prices the budget as `27.7 × (20 − 1.17) = 522 M`, subtracting it a second time. Self-consistently the figure is **554 M** (`r_wall × 20`) or **558 M** (`r_w × (20 − f)`), which agree to 0.7% | the error was ~6% and in the **safe** direction, which is why nothing caught it for two weeks. `e3_budget_by_shape.py` prints both conventions side by side and never mixes them. **A rate and a reservation must be read off the same denominator**, and the way to check is that the two self-consistent forms agree |
| **a planted control was planted outside the range the instrument is used in** | E3's Gate V1 compared an **all-zero-code** model to a no-zero one and fired at 15× the IQR, voiding the run. The kernel was innocent: `qkv_proj`, `o_proj` and `head` — pure packed matvecs — were flat to **0.005 ms**, and the whole 2.080 ms sat in `ffn`, the only organ holding a transcendental. An all-zero model feeds `expf` exactly `0.0f`, every libm's early-out; 2.080 ms / 116,736 calls = **17.8 ns per call**. A zero fraction of 1.0 is not something any exporter can produce, and across the range a real export occupies (0.47 vs 0.00) the timing is flat to 0.08 tok/s | the `VOID` was honoured and no arm was generated under it. The replacement gate compares synthetic against a **real exported artifact of the same shape**, fixed at a scale where no synthetic file existed yet — and passed at **0.000%** (1.5 B). The failed gate is reported next to the pass, not deleted. The accident is that it also **measured** what it was meant to assume: the matvec's value-independence is no longer a reading of the source |
| **a speed gate contained a hard constant, and its own ground truth could not pass it** | E3 brief §8.4 fixed Gate V2′ at *"57.790, IQR 0.190"*, measured on a real artifact twenty minutes earlier. The synthetic file gave 56.180 and failed. Re-run **interleaved A/B/A/B**, the *same real file* gave **56.095** — it had moved **2.9%** — while synthetic and real differed by **0.200 tok/s, 0.357%**, with one pair in which both dipped together | **fourth time this ledger has been bitten by the same family**: §11 a rate under contention, §12 a timer bracketing the wrong work, E3 §2.3 an anchor on the wrong head, now a constant that did not survive twenty minutes. The rule is now explicit: **a speed gate may not contain a constant; it must name a file to be measured concurrently, interleaved** |
| **a brief asked for one configuration in prose and the code read another from a config file** | E3 §3 asked for *"a ternary head (the runnable configuration E2 settled)"*. `synth_export.py`'s `SHAPES` carried each donor's own `tied` flag, and **a tied model runs its head as the fp32 embedding** — 544.6 MB/token, 13.8 ms, **52% of the token**. Six arms were not generated on the strength of a configuration nobody intends to ship. `SPEED_LEDGER` §12.2 had said its anchor was untied and packed without naming it: `136,134,656 weights, 68.1 MB/token` is **0.5 bytes per weight** | found because the synthetic file agreed with the **real** artifact of the same configuration to 0.36% while both missed the ledger by 35% — a disagreement that could only be about *which* configuration. `--head {ternary,donor}` added, default `ternary`. **A configuration named in prose must be named in a flag**, and a table's units are a claim about which configuration produced it |

## 6. Working rules this programme has paid for

- **A negative is only as strong as the sweep behind the setting it was measured at.** (D0c, D4b, T2)
- **A control must itself be shown to fire.** A gate never seen to trip is decoration. (§5, row 1)
- **Profile before optimising.** The head was 40% of every token and nobody had looked. (R1)
- **When reproducing a failure, reproduce the CONFIGURATION, not just the model.** A different
  library default invalidated a conclusion. (§5, row 3)
- **Byte-rate ÷ bytes-per-weight is only valid where the path is bandwidth-bound.** (P2 → R1)
- **A Δ against a baseline cannot support a claim about one arm versus another.** The arms are
  correlated across sequences; the contrast has to be bootstrapped paired. Doing it moved two of
  T2's apparent results — TWN and GPTQ-on-the-naive-grid — from "worse/better" to *not
  distinguishable*. (T2 §4)
- **When an instrument returns an impossible ordering, test the instrument before the finding.**
  GPTQ scoring below its own starting point is not physically possible; two controls showed the
  code was right and the objective was wrong. (T2 §5)
- **A profiler bucket is a claim about what is inside it.** `rope()` inside the `qkv` timer both
  hid a 9.6% cost and manufactured a 4× anomaly that two experiments were then aimed at. Before
  deriving a rate from a bucket, name every operation the bracket contains. (§12.1)
- **A throughput number carries its bench length.** Attention is `O(position)`, so a longer bench
  has a genuinely lower tok/s — 1.7 ms/token between 300 and 800 on this donor. Two rates taken at
  different lengths are not comparable and their difference is not contention. (§12.3)
- **Quantization damage does not add across organs.** The head costs `+0.339` alone and
  `−0.009 ± 0.020` on top of a ternary FFN+attention; the three single-organ arms sum to `+3.184`
  where the combination measures `+2.708`. A per-organ cost is only a cost *in the company it was
  measured in*. (T2b §4)
- **A smoke run establishes that the apparatus runs, not the sign of the effect.** T3's 4-of-28-layer
  smoke passed every control (X at `6.4e-07`, planted null clean) and flipped the sign of all three
  contrasts, returning the opposite label to the run. **A truncated model is not a small model.**
  (T3 §4.5)
- **A gate constant is a claim, and it is checkable against the arm that produced it.** T3's
  `VOID` was a mis-specified constant, not a broken harness — provable because an independent
  runner had already measured the identical arm to the last bit. Report the label the rule
  returns, then show the check; do not rewrite the rule. (T3 §1.3)
- **A pre-registered threshold needs its own interval before the label is read as settled.** T2b
  passed its 1.60 bar at 1.584 — but the ratio's ci95 is `[1.514, 1.649]` and a third of the
  bootstrap lands on the other side. The label stands; the confidence in it does not. (T2b §5)

- **A ceiling nothing has reached is a ceiling nobody has tested.** The runtime could not load a
  model above 2 GB; the largest one ever handed to it was 1.84 GB, so the limit had never fired,
  and the error it produced accused the artifact instead of the reader. The target model is ~5 GB.
  **Before scaling a number up, check that the apparatus can hold the object the number is about.**
  (E1 §4.4)
- **An error message is a hypothesis, not a diagnosis.** `bad magic` was reported about a file
  whose magic was intact, by code that had allocated a zero-byte buffer and passed its own
  short-read guard because zero bytes read equals zero bytes requested. **A guard that compares a
  quantity to itself checks nothing.** (E1 §4.4)
- **A pre-registration that splits a decision across runs must say which run owns the label.**
  E1's 1.5B arms were split across two invocations by the brief, so neither could evaluate the
  decision function and both returned `INCOMPLETE` on numbers that met every term. Second time in
  three probes that the brief, not the code, produced the mechanical label. (E1 §6.2, T3 §1.3)
- **A small donor establishes the sign, not the magnitude — and not always the ordering.** Across
  E2's two donors every sign that mattered replicated and **no magnitude did**: the fold's benefit
  differed 1.5×, the head fold's cost 3.6×, the benefit a ternary head adds 8.9×, and one whole
  comparison **inverted** (`--fold all` is worse than no fold on the 0.5B, better on the 1.5B).
  Same shape as E1's "the head is free is a 1.5B result". **Give the deciding donor the label in
  writing before the small one produces a number.** (E2 §5.0, §3.2 of its brief)
- **A replication that shares the estimator's seed reproduces its dispersion too, and that is not
  extra evidence.** E2 recovered T3's fold as `−0.220001 ± 0.052861` — point *and* paired SE at the
  printed precision. Forced, not corroborating: same seed 7, same 24 sequences, same byte weights,
  and per-sequence nats agreeing to `1e-07`. **Count it once.** (E2 §4)
- **`ci95 excludes 0` is a statement about accumulation when the effect is at the ulp.** The same
  contrast, same arms, same code, **excluded** zero on one donor (`+2.037e-08`) and **contained**
  it on the other (`+3.827e-08`). Exclusion is not magnitude; a rule must pair it with a threshold.
  (E2 §5.2)
- **A default that changes behaviour is only safe when both sides are pinned to something already
  on disk.** Adopting the fold flipped `--fold` to `layers`, which would have silently re-exported
  every caller that omits the flag — including E1, whose reference builder still defaults to
  unfolded. The flip and the pin went in one commit, and both directions were checked by **sha256
  against artifacts that had actually been measured**. (E2, `49b6654`)
- **A planted control must be planted inside the range the instrument will actually be used in.**
  An all-zero model is not a model any exporter can produce; the gate that used one measured `expf`
  instead of the kernel it was defending, and voided a run for it. (E3 §2.2)
- **A speed gate may not contain a constant.** It must name a file to be measured **interleaved**
  with the thing under test. E3's reference artifact moved 2.9% in twenty minutes and could not pass
  the gate its own measurement had defined. Fourth instance of the same family in this ledger.
  (E3 §2.5)
- **A within-run IQR is not a reproducibility interval.** This machine's within-sweep
  dispersion is **0.000–0.015 tok/s** and its **between-sweep dispersion is 5–10%** — E3's own
  binary misses E3's own published table by −1.6% to −4.2%, and the same code read **3.240 and
  3.030 two hours apart** while its IQR stayed at 0.005. A gate that compares a measurement to
  a number from another session is measuring the calendar. **Re-measure the reference in the
  same sweep.** Third instance: E3 §2.5's reference moved 2.9% in twenty minutes, and the
  ledger's original 18.6 G-w/s was contended by 35%. (E4 §2.4)
- **A threshold placed inside the instrument's dispersion cannot decide anything.** E4 §5 drew
  `LATENCY-CONFIRMED` at ≤0.50× and the arm came in at **0.503×**. The remedy is not a new
  threshold but a better measurement: **interleaved A/B/A/B/A/B**, ratio per adjacent pair.
  The baseline arm moved **10.55%** across the three pairs; the ratio moved **2.98%**. That one
  comparison is the whole case for interleaving, measured. (E4 §2.5)
- **A planted control is worth more as a decomposition than as a gate.** E4's `serial2` runs
  the dot loop twice **bit-identically**, which does not just prove the timer is attached — it
  *solves* the organ into the loop under test and the rest. `serial3` then hits the third point
  to **−0.56%**, so the split is measured rather than fitted. Every mechanism claim in E4 rests
  on that, and the same trick is now the method for the next probe. (E4 §2.2–§2.3)
- **A rate and a reservation must be read off the same denominator.** `27.7 G-w/s` is weights/**wall**,
  so pricing `27.7 × (20 − f)` charges `f` twice. The check that catches it is cheap: the two
  self-consistent forms — `r_wall × 20` and `r_w × (20 − f)` — must agree, and they do (554 vs 558 M).
  **Committed a second time the same day, in the probe that wrote this law:** E3 first published the
  gap to 50 tok/s as `530 / r_w = 15.7×` when `530 G-w/s` is a *wall* quantity — it is `530 / r_wall`
  = **16.18×**, which is just `50 / 3.090`. *Where a rate-free form of the quantity exists, quote it:
  it has no denominator to swap.* (E3 §4.3, §6.7)
- **A configuration named in prose must be named in a flag.** E3's brief said "ternary head" and the
  code read `tied` out of a donor config; the two differ by 52% of the token. (E3 §2.3)
- **Extrapolating a fit requires publishing its residuals first.** `f` is fitted per donor shape only
  because the fit reproduces the twelve measured points to 7.7% worst and **0.0–1.2% at the shapes
  that decide**; the residual table is the licence, and it is printed above the extrapolation.
  (E3, `speed/e3_budget_by_shape.py`)
- **A shape can be measured without weights, but only if the instrument is gated against a real
  artifact.** E3's synthetic files match real exports **byte-for-byte in size** and to **0.000% in
  time** at 1.5 B — which is what buys the right to measure shapes nobody has 20 GB to download.
  (E3 §2.1, §2.4)

## 7. The head, the tokenizer, and the thing nobody priced

> ### ⚠ SUPERSEDED AGAIN BY E4 (`probes/E4_ATTENTION_ACCUMULATORS.md`) — the screen re-opens
>
> E3's re-run below priced every donor against an `f` measured on an engine whose `Q·K` reduction was
> latency-bound. E4 halved `f`, so the fit's attention coefficient `A` was wrong by **2.04×**
> (`3.09445e-07` → `1.51461e-07` ms per FMA-unit). **Scaling the old table would have been a guess**,
> so all six shapes were re-measured under `--attn avx4` **in one sweep** (`results/e4/shapes_avx4.json`)
> and both `f` and `r_w` are taken from it — E4 §2.4 forbids a rate from one sweep meeting an `f` from
> another.
>
> **Residuals first, because they are the licence** (`speed/e3_budget_by_shape.py --arms ...`):
> **0.3–0.8% at the 7–10 B shapes that decide**, −0.2% to +7.8% at 1.5–3 B, and **−23% at `S05`** —
> the two-term model no longer fits the small end, because with attention halved the per-call
> overheads dominate there. **No donor in this screen is below 1.5 B**, so that residual does not
> price anything; it is stated because the fit is now unfit for the shape it used to fit.
>
> | donor | head weights | **budget @300** | **head as % @300** | was (E3) | **budget @800** | **head as % @800** | was (E3) |
> |---|---|---|---|---|---|---|---|
> | Qwen3-8B | 622 M | **564 M** | **110%** | 152% | **369 M** | **169%** | 2763% |
> | OLMo-2-7B | 411 M | 578 M | 71% | 94% | 404 M | **102%** | 432% |
> | Mistral-7B-v0.3 | 134 M | 578 M | **23%** | 31% | 404 M | **33%** | **141%** |
> | Phi-3-mini | 99 M | 575 M | 17% | 21% | 452 M | 22% | 44% |
> | Qwen3-1.7B | 311 M | 592 M | 53% | 59% | 522 M | 60% | 81% |
> | Qwen2.5-1.5B | 233 M | 604 M | 39% | 42% | 552 M | 42% | 53% |
> | SmolLM2-1.7B | 101 M | 599 M | 17% | — | 539 M | 19% | 24% |
>
> **What changed.** E3's headline at 800 context was that the screen *stopped being about the head*:
> every 7–8 B dense donor was over the line regardless of tokenizer, and **Mistral-7B — the smallest
> vocabulary on disk — was at 141%**. It is now at **33%**. `f` had eaten the budget; it does not any
> more.
>
> **What did not change.** **Qwen3-8B is still the one that breaks**, at 110% @300 and 169% @800, and
> it breaks for the reason it always did: a 151,936-token vocabulary on a 4096-wide model is a 622 M
> head, and the head is charged in full every token. OLMo-2-7B is marginal at 800 (102%). **The head
> is set by the tokenizer, not the model** — the one claim that has survived every re-pricing.
>
> **The nine unpriceable donors are still unpriceable** (MoE and hybrid/SSM), for the same reason and
> with the same withdrawal.
>
> ### ⚠ SUPERSEDED BY E3 (`probes/E3_ENGINE_AT_TARGET_SCALE.md`)
>
> The table below screens twelve donors against **522 M**. That constant is wrong twice: it charges
> the fixed cost twice (§0), and — far larger — it uses a **1.17 ms** non-weight reservation that E3
> measured at **10.576 ms** at a 10 B shape. The screen is re-run below from measured `f`, per shape.
> `donor_speed_budget.py`, which produced the old table, says of itself that *"every attention row
> below is priced by ANALOGY to the proj-GEMV path, and that analogy is the single largest source of
> error here."* E3 measured it, so the analogy is retired.
>
> **The re-run.** `speed/e3_budget_by_shape.py` fits `f = A·L·NH·HD·pos + B·L·D` on E3's twelve
> measured points — worst residual **7.7%**, and **0.0–1.2% at the 7–10 B shapes that decide** — then
> applies it per donor. `budget = r_w × (20 ms − f)`, `r_w` measured per size class.
>
> | donor | D | V | head | fitted `f` @300 | **budget** | **head as % of budget** | was (§7) |
> |---|---|---|---|---|---|---|---|
> | Qwen3-8B | 4096 | 151,936 | 622 M | 7.928 | 409 M | **152%** | 112% |
> | OLMo-2-7B | 4096 | 100,352 | 411 M | 7.047 | 438 M | **94%** | — |
> | Mistral-7B-v0.3 | 4096 | 32,768 | 134 M | 7.047 | 438 M | **31%** | 24% |
> | Phi-3-mini | 3072 | 32,064 | 99 M | 5.286 | 468 M | **21%** | 18% |
> | Qwen3-1.7B | 2048 | 151,936 | 311 M | 3.083 | 528 M | **59%** | — |
> | Qwen2.5-1.5B | 1536 | 151,936 | 233 M | 2.312 | 552 M | **42%** | — |
> | SmolLM2-1.7B | 2048 | 49,152 | 101 M | 2.643 | 542 M | **19%** | — |
>
> **At 800 tokens of context the screen stops being about the head at all:**
>
> | donor | budget @800 | head as % |
> |---|---|---|
> | Qwen3-8B | 23 M | **2763%** |
> | OLMo-2-7B | 95 M | **432%** |
> | **Mistral-7B-v0.3** | 95 M | **141%** |
> | Phi-3-mini | 226 M | 44% |
>
> **Mistral-7B has the smallest vocabulary on the disk and still cannot reach 50 tok/s at 800
> context** — not because of its head, but because `f` has eaten the budget. Every 7–8 B dense donor
> is over the line at 800 context regardless of tokenizer.
>
> **Nine of the eighteen donors on disk are not priced by this fit at all** — MoE (`Qwen3-30B-A3B`,
> `Qwen3-Next-80B`, `OLMoE`, `DeepSeek-V2-Lite`, `Mixtral`, `gpt-oss-20b`, `granite-4.0-h`) and
> hybrid/SSM (`Zamba2`, `Nemotron-H`, `Falcon-H1`, `mamba2`) do not have uniform dense attention in
> every layer, so `f` at their shape has not been measured. **`gpt-oss-20b` at 105% and
> `Nemotron-H-8B` at 97% in the old table rested entirely on the analogy `donor_speed_budget.py`
> flagged as its own largest error, and are withdrawn rather than restated.**
>
> What survives from the old table unchanged: **the head's size is set by the tokenizer, not the
> model** — and E3 turned that from a weight count into an end-to-end measurement, `M7` vs `Q8`,
> 4.637× the vocabulary for 4.629× the head time (0.2%).

### 7.1 The original screen, kept for the record

`donor_speed_budget.py` prices the output head against the measured **27.7 G-weights/s** of the
packed kernel after the rope hoist (`SPEED_LEDGER.md` §12.2). The head is a dense GEMV of `D × V`
touched on **every** token, and **no** MoE, carve, sparsity or reconstruction result in this
programme touches it.

| donor | D | V | head | % of the 522 M budget for 10B @ 50 tok/s |
|---|---|---|---|---|
| Qwen3-8B | 4096 | 151,936 | 622 M | **112%** |
| openai/gpt-oss-20b | 2880 | 201,088 | 579 M | **105%** |
| Qwen2.5-Coder-7B | 3584 | 152,064 | 545 M | 98% |
| nvidia/Nemotron-H-8B | 4096 | 131,072 | 537 M | 97% |
| mistralai/Mistral-7B-v0.3 | 4096 | 32,768 | 134 M | **24%** |
| microsoft/Phi-3-mini | 3072 | 32,064 | 98 M | 18% |

**Two of twelve donors on this disk have an output head larger than the entire 50 tok/s budget,
and two more are within 3% of it.** Qwen3-8B cannot reach 50 tok/s on this machine even if every
other weight in it were free. It was four of twelve at the 495 M budget: **the entire 10.3% rope
win bought exactly two rows crossing back under the line.**

**Qwen3-8B and Mistral-7B are the same width.** Their heads differ by 4.6× entirely because of
vocabulary size. Every donor-adaptation probe this programme owns was measured on Qwen, which has
the worst head-to-body ratio on the disk. **Vocabulary is a speed variable, it is chosen rather
than earned, and it has never been treated as one.**

Two measured facts sharpen it: the head was 40.4% of every token in fp32 before R1 ternarized it
(`probes/R1_DONOR_RUNTIME.md` §3), and it is the one organ that pays **nothing** for int8
activations on the LUT path — 0.13997 → 0.14084 when excluded — because its input is the final
RMSNorm output (`donor_engine.c --lut-no-head`, commit `95b7fd3`).
