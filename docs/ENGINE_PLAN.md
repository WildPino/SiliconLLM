# C Engine Plan — Silicon Entropy Engine

**Status: 2026-07-04 — E1-E4 ALL CLOSED (176 → 848 tok/s dense / 701.7 MoE, parity-gated, ≤ +0.00004 BPB inference cost); engine consolidated (single core, 7/7 bit-identical), CI green. Phase 61 CLOSED double-fail: SSM projections STAY fp32 (+0.018-0.022 BPB measured cost); GEMV blocking lever tested and rejected (memory-bound, not FMA-bound) — the single-thread token-by-token engine is at its floor. Phase 62 (Cat-A) CLOSED: the foundation's architectural properties (sparsity ~92/79, in-place predictability 83-88%) transfer across prose→code→log; CODE validated (held-out modeling BPB 1.242, below the zstd-19/brotli-q11 floor — a modeling claim, model weights not amortized); LOG is a real boundary (overfits at 773K deduped tokens, ~10× above the LZ floor: template repetition is the compressor's home turf → logs are eval-only for now, routed to a future LZ/SSM hybrid product lane). The E5 design gate is CLOSED (2026-07-05, both pre-registered rules held): frozen-backbone MTP drafting reached 1.96 tokens/pass against the ≥2.0 rule → speculative-AR out; the carve/set-block co-training arm (E5.2) failed its gate decisively (BPB(B=2) 1.8106 vs ≤ 1.257) → **multi-token generation from state is closed at this scale; the model stays AR-lossless, and E5's engine-side scope is the block-verify chassis (zero-cost n-gram drafter, 1.75 tokens/pass floor, streamed-regime claims only) + threads. Phase 63 CLOSED (2026-07-05): threads ADOPTED (dense 827→2029 tok/s = 2.45×, MoE 635→1519 = 2.39×, bit-identical by construction, generic x86-64-v3 baseline per the portability law); block-verify chassis BUILT and lossless (Activation Replay state-commit, weights-once proven by counting) but its speed claim is NOT adopted at 8.3M (compute floor > weight traffic, C/T = 1.6 vs the ≲0.25-0.30 win-condition — pre-registered outcome) — `--block K` stays a correctness-proven, scale-up-conditional capability, default off. New law: block-verify amortizes SHARED streamed weights at tpp; ROUTED (i.i.d.) pools at break-even or worse. Execution-model section of SCALEUP_ARCHITECTURE.md rewritten from measured numbers.****
**Deliverable:** a single-binary C inference engine implementing the validated CPU-native architecture (blueprint: `SCALEUP_ARCHITECTURE.md`). This is the project's product; the probes exist to make this engine honest.

## Principles (project law, carried over)

- **Correctness before speed.** The fp32 C core built in E1 is the permanent regression harness: every later optimization lands only after parity against it is proven. An optimization without a reference is an unfalsifiable bug.
- **Portability law (owner's directive, 2026-07-05).** The reference machine (Ryzen 5 3600X) is an *instance* of the target class (commodity x86-64-v3 CPUs, old-gen, no VNNI) — never the target itself. Hardware numbers (LLC size, core topology, bandwidth) are runtime-discovered or configuration parameters, never compile-time assumptions; ISA-specific kernels live behind a kernel interface with the scalar reference as a working fallback; machine-specific tunings land only as dispatchable options with generic defaults. Where a reference-tuned choice conflicts with a parameterized design, the portable design wins by default — explicitly *including* at some speed cost on the reference machine. Build policy: the baseline is generic x86-64-v3 (`-O3 -mavx2 -mfma`, explicit `-ffp-contract` policy); `-march=znver2` is an optional tuning build. Corollary: the same binary produces bit-identical results on any CPU of the ISA class, so the golden dumps and the whole oracle chain are *binary-portable* — any rented machine verifies parity with one command before producing benchmark tables, and every new machine extends the measured set (claims stay per-machine-measured).
- **One variable per stage.** Each stage changes exactly one thing and has pre-registered gates. No gate is loosened after seeing results (anti-Goodhart; the only legitimate post-hoc move is tightening).
- **No `-ffast-math`** (Phase 35 determinism law). Toolchain: `clang -O3 -mavx2 -march=znver2`. Single-thread first; threads are a later, separate variable.
- **Decode hygiene locked:** greedy, repetition penalty 1.2 over a 128-token window, no top-p.
- **Honest scope:** at ~8.3M sandbox scale everything is cache-resident — E1-E3 validate *correctness* and *kernel-level* speed, not the streaming architecture. The two-pool layout (resident ≤16MB @ ~100 GB/s + DRAM-streamed experts @ ~28 GB/s, per probe-4) is exercised from E4 on and pays at scale-up.
- **Unit-exactness does not compose to system correctness** (E4 lesson, elevated to project law): a bit-exact kernel gate can pass while staging buffers / scale-apply corrupt the system (E4: a `g_xq[HID_E]` overflow cost +0.15 BPB while the kernel self-test stayed green; caught only by end-to-end parity + per-block diagnostics). Every rung keeps an end-to-end parity gate; kernel self-tests are necessary, never sufficient.

## Stages and pre-registered gates

### E1 — fp32 reference core (target model: `sp58_base.pt`, ~8.3M Arch-A: D256 N96 H8 L6, SWA@5, gated-dReLU ternary MLP)
1. **Exporter** (Python): checkpoint + tokenizer → versioned binary format (magic, arch header, fp32 tensors; the ternary MLP is exported *both* dequantized-fp32 (E1 path) and packed ternary+per-row scales (ready for E2)). Tokenizer (BPE-1024 vocab + merges) embedded or sidecar.
2. **C forward pass:** embedding → 6 blocks (Mamba-1 selective-scan semantics *exact*, SWA at its layer with its trained window, gated-dReLU MLP) → head → logits. Token-by-token AR, O(1) state.
3. **Decode:** the locked hygiene recipe.
4. **GATES:**
   - **G1 golden-trace:** PyTorch dumps per-layer activations for a fixed 64-token sequence; the C engine must match layer-by-layer (max rel. err < 1e-3 per layer; report the error profile). This is both the gate and the debugging instrument for scan-semantics drift.
   - **G2 logit parity:** top-1 agreement ≥ 99.9% over ≥ 10K val tokens.
   - **G3 BPB parity:** engine BPB on the val slice within ± 0.002 of the PyTorch eval (0.8799).
   - **G4 tokenizer parity:** C BPE encoder produces identical token ids vs the Python tokenizer over the val corpus.
   - **G5 generation parity:** greedy+rep streams identical on fixed prompts; divergences acceptable only if traced to fp reorder (report, Architect judges).
5. **Perf baseline:** single-thread tok/s on the 3600X, recorded. **No optimization allowed in E1** — this is the "before" number.

**E1 CLOSED (2026-07-02) — all gates green.** G1 l2-rel err ~4e-7 per layer (max_abs ~1e-5 = fp reorder); G2 top-1 100.0000% (20480/20480); G3 BPB 0.879949, delta +0.000000; G4 zero mismatches over 975,712 tokens; G5 zero divergences over 600 greedy tokens (3 seeds, bit-identical). Baseline: **176.1 tok/s / 5678 µs/tok** single-thread fp32 exact-exp. Apparatus: `benchmarks/phase60/` (exporter `e1_export.py`, reference `e1_reference.py`, engine `e1_engine.c`; rerun `bin/e1_engine.exe --all`). Adjudicated notes: (a) G1's original per-element rel-err metric was diagnostically broken near zero (false 0.25 readings on ~1e-5 activations); the l2-relative-per-layer replacement is a metric fix, corroborated by three orthogonal untouched gates — not gate-loosening. (b) G5 parity is bit-exact only while generation ≤ seq: beyond that the recompute-from-zero *reference* cannot represent the stateful stream — a streaming (stateful) PyTorch reference will be needed for E5/long-context. (c) G4 is self-consistent with the training-data codec lineage — which is the correct parity target (match what the model was trained on).

### E2 — ternary LUT MLP (probe-1 kernel lands) — ACTIVE
Pack ternary weights into the pshufb-LUT path (kernel from `benchmarks/phase57/phase57_lutbench.c`) for the three MLP matvecs (gate/up/down); packed ternary + per-row scales are already in the E1 export format. One variable: the MLP matvec path. Everything else untouched.

**Surfaced question (pre-registered before results):** the LUT requires **quantized activations** (int8, per-token absmax) to build the byte tables — but the model was trained with fp activations (only weights fake-quantized). Activation quantization is therefore a *new* inference-time error whose model-level cost has never been measured (probe-1 proved the kernel bit-exact vs an *integer* reference; quality was measured with fp activations). It is gated, not assumed.

**GATES:**
1. **Kernel bit-exactness:** LUT matvec bit-exact vs the scalar integer reference (probe-1 discipline).
2. **Quality:** BPB delta vs E1 ≤ **+0.005** (half the ternary cost itself). If it fails, the gate is NOT loosened; documented fallback = exact ternary add/sub path on fp32 activations (no multiplies, exact, slower than LUT), and/or escalation: per-group activation scales, int16 accumulation, activation-QAT in a future train.
3. **Agreement:** top-1 vs E1 ≥ 99% over ≥10K val tokens (greedy streams may legitimately diverge — logits change; report).
4. **Timing, Amdahl-explicit:** per-component time breakdown (scan / SWA / MLP / head / exp) **before and after** — the E1 baseline (~176 tok/s ≈ ~2 GFLOP/s effective) is very likely dominated by the exact-exp scan, not the MLP, so the honest E2 headline is the **MLP kernel time** (anchor 4.2-5.0×); the end-to-end tok/s gain is bounded by the MLP time share and is expected to be muted. Pre-registered deflation.

**E2 CLOSED (2026-07-02) — all four gates green.** `benchmarks/phase60/e2_engine.c` (reuses the E1M1 format; packed ternary already present). Activations quantized per-token absmax to **[-63,63]** (the widest range fitting int8 for ternary g=2 → best precision, kernel untouched); gate/up share one LUT build. Results: G1 kernel bit-exact, worst `|S_lut−S_ref|=0` over 2.76M dot-products; **G2 BPB delta +0.000037** (E1 fp32 0.841172 vs E2 LUT 0.841209, same 100K slice — the activation-quant concern resolved negative, 135× inside budget); G3 top-1 **99.7363%** (10213/10240); **G4** fp32 MLP = 811 µs/tok = **14.0%** share, scan-exp = **83.5%** (the surfaced hypothesis confirmed — the scan, not the MLP, is the bottleneck), LUT MLP = 378 µs/tok → **MLP kernel 2.14× faster** (incl. per-token quant+LUT-build+dequant), end-to-end 172.7→179.5 tok/s = **1.04×** (Amdahl-muted, exactly as pre-registered). Honest deflation: 2.14× < the 4.2-5× pure-kernel anchor because per-token LUT-build/quant/dequant overhead at ~8.3M-small dims is included — amortizable under block-decode (E5). **Cross-stage note:** the real bottleneck is the exact-exp selective scan (83.5%); a future scan-exp optimization (gated poly/LUT exp, parity-gated as an approximation) is the highest-leverage speed lever, orthogonal to the MLP path.

**Adjudication (Architect, 2026-07-02): E2 CLOSED, 4/4.** (a) The end-to-end +4% is within run-to-run noise (the scan itself varied +5% between the two runs) — the robust E2 claim is the component-level **2.14×**, and the absolute BPB shift vs E1's G3 (0.8412 vs 0.8799) is a slice change (100K vs 200K), not a regression: the valid comparison is the same-slice delta (+0.000037). (b) The **[-63,63]** activation range is endorsed: widest int8-safe range for ternary g=2 (2·63=126<127) = maximum free activation precision with the kernel bit-exactness untouched. (c) **Scale-up caveat, pre-registered now:** the ~zero activation-quant cost holds at ~8.3M/D256/TinyStories; per-token absmax int8 degrades when outlier channels emerge with model size — re-measure at scale-up, with per-group scales as the known escalation. (d) The scan-exp lever is approved as its own stage **E3.5** (below).

### E3 — activation-sparsity skip (predictor-free) — ACTIVE
Compute the gate matvec first; for gate≤0 units skip the up work, and skip down columns where h=0. The skip is **exact in the integer LUT domain** (skipped rows/columns contribute exactly zero), so the gate is stronger than "numerically identical". Same model (`sp58_base` — one variable = the skip mechanism). **Layout rule (ρ-law in the data layout):** store W_down transposed (per-hidden-unit columns contiguous) so column-skip is contiguous-chunk skip, not strided gather.

**GATES:**
1. **Bit-identical logits vs E2** on the val slice + golden trace (exactness of the skip).
2. **Measured sparsity per layer** (gate% and h% at inference) reported against the probe-2 anchors (79% gate / 92% hidden).
3. **MLP time before/after.** Anchor ~2.12× applies to the *matvec portion only* — the LUT-build/quant/dequant overhead does not shrink with sparsity, so measured MLP speedup < 2.12×. **Pre-registered deflation.** End-to-end will barely move (MLP is 6.8% post-E2): E3's value at sandbox is *correctness of the mechanism + measured kernel shrink* — thesis validation, not speed.
4. **Optional E3b (Architect decision after gates pass):** export the Phase 58 coherence-trained checkpoint (+reg, BPB 0.8801) and measure block-granular skip yield in-engine — the in-silico test of whether the block-structured sparsity recipe pays on real hardware.

**E3 CLOSED (2026-07-02) — gates green.** `benchmarks/phase60/e3_engine.c`. Gate = full LUT (predictor); **up row-skip** (row-major codes → scalar-LUT dot for gate>0 rows only; gate≤0 → h=0 exactly); **down tile-skip** (tile-major codes → iterate hq≠0 tiles only; a tile is a contiguous Mpad block, so it is chunk-skip not strided gather — layout satisfies point #4 without a separate transpose). Exact by construction (integer S associative). Results: **G1 bit-identity: 0 mismatches over 5,242,880 logit values, worst |diff|=0.0**; sparsity gate mean **78.5%** (anchor 79%), h mean **93.5%** (anchor 92%), down tile-skip **87.7%**; MLP time 370.8→319.4 µs = **1.16×**, end-to-end ≈ flat (188.7→192.3 tok/s). **Honest headline = weight-bytes touched, not compute-time:** gate 100% + up 21.5% + down 12.3% = **44.6% → 2.24× fewer MLP weights touched/token** (≈ the 2.12× anchor, slightly better via h 93.5%). Compute-time is only 1.16× because at cache-resident ~8.3M dims dense-pshufb beats scalar-sparse (up-skip), the gate is unskippable (1/3), and quant/LUT-build overhead is fixed — the skip's real value is **bandwidth (weights not streamed), invisible in a cache-resident microbench, paying at scale-up (E4 two-pool)**, exactly the pre-registered scope. E3b remains available (block-structured `sp58_reg.pt` → the up-row-skip pays at 32-block granularity).

**Adjudication (Architect, 2026-07-02): E3 CLOSED.** The bit-identical bar (0 mismatches / 5.24M logits, worst |diff| = 0.0) is the strongest gate in the plan and passed clean. The in-engine sparsity reproducing the training-measured anchors (78.5/93.5 vs 79/92) is itself a transfer validation: the probe-2 property is real on silicon. Endorsed reading: the *composing* number is **2.24× fewer MLP weight-bytes touched per token** — that is what multiplies with the two-pool streaming at scale-up; the 1.16× compute time is the honest cache-resident reality (SIMD-dense beats scalar-sparse at small dims; the gate matvec is the non-skippable predictor). Added nuance: the h zero-rate 93.5% (unit-level) dilutes to 87.7% at tile level — the unit→tile dilution now measured in-engine, consistent with Phase 58 ("free sparsity is fine-grained"; the coherence recipe closes this gap and belongs in the scale-up training recipe). **E3b DEFERRED, not dead:** the E4 target `moe_gran.pt` was trained with lam_coh=0 (no block structure in it to measure), and the coherence recipe is already adopted for scale-up training — E3b's payoff lands when a coherence-trained scale-up checkpoint exists. Next: E3.5, then E4.

### E3.5 — fast-exp scan path (deterministic approximation, parity-gated) — ACTIVE
The exact-exp selective scan is 83-91% of per-token time (E2 G4) — the highest speed lever in the engine. **Lawfulness:** the Phase 35 rule bans *nondeterministic compiler reordering* (`-ffast-math`), not explicit, versioned, deterministic approximations with pre-registered parity gates; a fixed poly/LUT exp is deterministic and reproducible. GATES: BPB delta vs E1 ≤ +0.001; top-1 vs E1 ≥ 99.9%; golden-trace error profile reported; scan time before/after + new end-to-end tok/s. Methodological payoff: shrinking the dominant scan share sharpens the end-to-end signal of every later stage (E4/E5).

**Implementation guidance (Architect):** (1) *Range-aware validation first:* dump the observed input domain of the exp arguments (dt·A) from the golden trace; the approximation's max rel. error must be documented over that *observed* domain, not a generic one. (2) One versioned function, fixed coefficients, deterministic — reproducible by construction. (3) Vectorizing the scan loop that the poly-exp enables counts as the *same* variable (the scan-exp path); every other component untouched. (4) Pre-registered expectation, honest: if exp dominates the ~4.3 ms scan, end-to-end lands around ~450-650 tok/s (~2.5-3.5×); report what remains after.

**E3.5 CLOSED (2026-07-02) — gates green.** `benchmarks/phase60/e35_engine.c`. libm expf → `exp256_ps` (Cephes fixed-coefficient poly, deterministic/versioned) + 8-wide vectorization of the N=96 inner loop (same variable). Variable isolated: gates compare **(fast-exp + fp32 MLP) vs E1 (exact-exp + fp32 MLP)**. **Range-aware:** observed domain `dt·A ∈ [−29.76, 0]` (always ≤0 → exp∈(0,1]); exp256 vs libm max rel-err **1.0e-6** on that domain. Results: golden-trace fast-vs-exact per-layer l2-rel ~2e-7 (fp-reorder level); **BPB delta −0.000000** (0.841172 both); **top-1 100.0000%** (10240/10240); **scan component 4158.8→160.4 µs = 25.9×**; end-to-end isolated (fp32 MLP) 172.9→**567.7 tok/s = 3.28×** (inside the 2.5-3.5× pre-registered band); **full engine (fast-exp + LUT+skip MLP) = 848.0 tok/s / 1179 µs = 4.8× vs the E1 baseline (176 tok/s).** **New profile for E4/E5 (full engine): scan-other 52.7% (now the bottleneck — the fp32 dense SSM projection GEMVs in_proj/x_proj/dt_proj/out_proj + softplus, kept fp32 as the "sensitive" parts), MLP 23%, scan-exp 13.7%, SWA 7%, head 3%.** Fast-exp moved the bottleneck off the scan-exp onto the fp32 SSM projections — the next speed candidate (a separate quality probe: can the SSM projections be quantized without hurting BPB?). Cumulative E1→E3.5: **176→848 tok/s = 4.8×**, every step parity-gated (bit-exact / bit-identical / ≤threshold).

**Adjudication (Architect, 2026-07-02): E3.5 CLOSED — the largest speed step so far, and the cross-stage numbers reconcile.** (a) *Bookkeeping check:* E2 reported "scan-exp 83.5%"; E3.5 splits it as exp-proper 71.9% + scan-other 11.5% = 83.4% — the E2 figure was the whole scan block's share, no contradiction, labels now correct. (b) *Variable isolation done right:* BPB/top-1 measured at fp32-MLP config (exact-exp vs fast-exp only; delta exactly 0, top-1 100%), and the isolated end-to-end 3.28× landed inside the pre-registered 2.5-3.5× band (predicted 450-650 tok/s; landed 567.7). (c) *Range-aware discipline:* dt·A ∈ [−29.76, 0] is structurally ≤0 (dt>0, A<0) → exp∈(0,1], no overflow path; rel-err 1e-6 characterized on the observed domain. (d) *Cumulative ledger:* **176 → 848 tok/s = 4.8×**, every step parity-gated; total inference-time quality cost **+0.000037 BPB** (the ternary cost itself, +0.028, was paid at training — separate ledger, probe-1). (e) *New bottleneck routed, not decided by the Builder (correctly):* scan-other 52.7% = dense fp32 GEMVs of the SSM projections (in/x/dt/out_proj), kept fp32 by the probe-1 mixed-precision recipe. Ternarizing them is a **model-side research probe** (training A/B), queued below — load-bearing at scale too (projection bytes are the same order as active-MLP bytes in Mamba blocks). A cheaper engine-side lever also exists (those GEMVs run at ~10 GFLOP/s effective → likely 2-4× vectorization/layout headroom, pure engineering, no quality question) — later polish stage. Neither blocks E4.

**Queued model-side probes (research, GPU training, not engine stages):** (1) **SSM-projection ternarization A/B** — probe-1 recipe applied to in/x/dt/out_proj, gate BPB ≤ fp32+0.01; doubly motivated (52.7% of engine time at sandbox; large resident-pool byte share at scale). (2) Head compression at real vocab (blueprint §3.7). (3) Activation-quant re-measurement at scale (E2 caveat). (4) E3b block-skip yield on a coherence-trained checkpoint (deferred from E3).

### E4 — MoE + two-pool layout (target model: `moe_gran.pt`, probe-4's promoted arm E32×h128 top-8) — ACTIVE
The engine's first exercise of the two-pool architecture. New surface: exporter v2 (fp32 router; per-expert packed-ternary **contiguous chunks** + per-row scales = the streamed pool, expert-id-major, tile-major within), top-8 dispatch, router-weighted expert sum — semantics identical to training.

**Gate ladder (E4 internally repeats the E1→E2→E3 discipline, one variable per rung):**
1. **E4-ref (fp32 MoE) vs PyTorch** (`moe_gran.pt` eval): golden-trace l2-rel < 1e-3 per layer; top-1 ≥ 99.9%; BPB within ±0.002 of the PyTorch eval (0.8589).
2. **Dispatch exactness:** engine top-8 expert ids + router weights vs PyTorch on the trace — fp32 router should match exactly; any divergence must be traced to tie-breaks and reported.
3. **E4-LUT+skip (ternary experts) vs E4-ref:** expert kernels bit-exact vs the integer reference (E2 discipline); BPB delta ≤ +0.005; top-1 ≥ 99%.
4. **Streamed-pool accounting (the composing number):** bytes/token touched in the expert pool, **counted** (top-8 × expert bytes, minus row/tile-skip within experts) vs theoretical; working-set-over-block reproduction vs the probe-4 anchors (ws@8 ≈ 84-89% of E) as transfer check.
5. **Perf, pre-registered expectation:** per-component breakdown + end-to-end. Honest expectation: active compute ≈ dense-1024 (same active hid) + router overhead → **similar tok/s to E3.5, on a better model (BPB 0.8589 vs 0.8799 = −0.021)** — the capacity tier demonstrated in the deliverable. Streaming *time* is NOT honestly measurable at ~8.3M (the whole expert pool fits in cache): the streamed-pool bytes are counted and priced by probe-3's 28 GB/s floor for scale-up. No cold-cache theater.

**E4 CLOSED (2026-07-02) — ladder green.** `benchmarks/phase60/e4_engine.c` (+ `e4_export.py` magic E4M1, `e4_reference.py`). MoE = fp32 resident router → top-8 → gated-dReLU-ternary experts; only the 8 selected are computed (non-selected are ×0 in PyTorch → exact). Results: **(1)** golden-trace MoE-fp32 vs PyTorch l2-rel ~4e-7; **(2 dispatch)** 0 set-mismatch, 0 id-order-mismatch, worst |Δweight| 5e-7 (fp32 router matches exactly, ordering included); **(1 cont.)** E4-ref BPB **0.858856** (Δ+0.000002), top-1 vs PyTorch **100.0000%** (20480/20480); **(3a)** expert-kernel bit-exact worst **0** / 98,304; **(3b)** E4-LUT+skip vs E4-ref BPB **Δ+0.000028** (≤+0.005, 178× inside), top-1 **99.7363%**; **(4 pool)** streamed bytes/token = top-8 × 51,200 B × 6 layers = **2400 KB/tok** (counted; priced @28 GB/s = 87.8 µs/tok at scale-up), **ws@8 mean 86.5%** (L0=89..L5=84 — probe-4's 84-89% anchor reproduced in-engine); **(5 perf)** E4-ref 173 tok/s; **E4 full (fast-exp + LUT experts) = 701.7 tok/s** (MoE 34.9%; ~E3.5's 848 minus ~17% for router + 8 small separate matvecs vs one large). **The E4 headline is the capacity tier, not speed: a −0.021-BPB-better model (0.8589 vs E1's 0.8799) at ≈ E3.5 speed, all parity gates green.** Packing note: base-3 g=2 codes are 4 bits/weight (~2.4× the 1.58-bit ideal) — a denser pack cuts the streamed-pool bytes ~2.4× in the bandwidth-bound scale-up regime. Two buffer bugs found+fixed (self-test `Sl[HID_E]` → segfault; engine `g_xq[HID_E]` → should be `[D]`, overflow corrupted the shared LUT, +0.15 BPB); lesson: the integer-kernel bit-exact gate (G5a) does NOT cover scale-apply or staging buffers — the end-to-end LUT-vs-fp32 quality gate (G5b) + per-block diag caught it (blk0 l2-rel 0.23 → 6e-3 after fix).

**Adjudication (Architect, 2026-07-02): E4 CLOSED — the capacity tier is in the deliverable.** Verified arithmetic: 2400 KB/tok ≈ 48 expert-visits × ~48 KB (98,304 ternary weights/expert at 4-bit g=2 codes + scales) ✓; 88 µs at probe-3's 28 GB/s floor ✓; ws@8 86.5% inside the probe-4 anchor 84-89% ✓ (another in-engine transfer check passing). The −17% vs E3.5 (router overhead + 8 small matvecs vs 1 big) is honest; the headline is the pre-registered one: **a −0.021 BPB model at ≈E3.5 speed, total inference cost vs PyTorch +0.000030 BPB**. The two buffer bugs are the strongest *process* validation so far — the kernel bit-exact gate stayed green while the system was broken (+0.15 BPB): the lesson is elevated to project law (see Principles). Light verification request: rung-3b's top-1 99.7363% (27/10240) is numerically identical to E2's G3 on a *different model* — confirm it is a fresh measurement (dump flipped positions or shift the slice); no gate depends on it, but coincidences get checked. **RESOLVED 2026-07-03: true coincidence, no harness bug — flip positions dumped (`--dumpflips`) are fully disjoint between E2 and E4 (zero overlap over 27+27 indices), and the shifted-slice counter-check moves the counts independently (E2 26/10240 at offset 512, E4 27/10240). ~0.26% is the stable int8-activation-quant flip rate at this scale; report in `results/phase60/p02/P02_REPORT.md`.** Queued engine optimization (bandwidth-bound regime only): **denser trit-pack** (5 trits/byte = 1.6 bits/weight) cuts streamed bytes ~2.5× vs the current 4-bit codes — worthless at sandbox, real at scale-up.

**Axis decision (Architect, 2026-07-02): E5 is design-blocked on model-side prerequisites.** Speculative-AR needs a drafter/MTP head (not trained); carve/SBD needs carve-capable training (not done); the recall tier lives in the phase-56 architecture line (recall slot), not yet unified with the phase-58/59 line the engine runs. Opening E5 now would idle on decisions that research must feed. → **Active track: Phase 61 (model-side) — SSM-projection ternarization A/B** (the 52.7%-of-engine-time + resident-pool-bytes question; arms and pre-registered gates in the Phase 61 brief/memory). **Builder side-task meanwhile: the streaming-stateful PyTorch reference harness** (the E1-annotated E5 prerequisite — unblocked, pure harness, needed regardless of the design-gate outcome).

**Phase 61 CLOSED (Architect, 2026-07-03): SSM-projection ternarization REJECTED — clean double-fail; the fp32 projections stay.** Arm A (all four projections ternary) BPB **0.8940**, arm B (in/out only) **0.8972**, against the pre-registered gate ≤ 0.8899 (fresh base 0.8757 + 0.010) — both fail, +0.018 / +0.022 vs the fresh base. Reading: the selective-SSM projections are **precision-hungry control organs** — now a measured number, not a caution; probe-1's mixed-precision recipe (ternary the MLP only, keep backbone/projections fp32) exits this phase *measured*, not assumed. The dt/x-sensitivity hypothesis is inverted but cautiously: A ≈ B within bf16 noise → "the cost does not depend on the split," nothing more. No 12k rerun (curves diverge past step 2000; prior against; GPU not spent on a hope). **Free-variance datum:** the refit base gives 0.8757 vs the 0.8799 anchor at identical recipe → **~0.004 cross-script reproducibility**; consequence — single-seed deltas below ~0.005 are not resolvable (probe-2's +0.0006 reads "zero within noise," as already labeled), valid comparisons remain within-script matched, and the 3-seed calibration (R1) is now a project need.

**GEMV vectorization lever REJECTED by measurement (Architect self-correction, 2026-07-03).** At E3.5 the 2–4× headroom hypothesized on the fp32 SSM-projection GEMVs assumed they were FMA-latency-bound; the Builder measured them **memory-bound** — a 2.6× microbench becomes **−4% in-engine** (probe-3's ρ-law, at home). Sister-lesson to the E4 law: *a compute-bound microbench speedup does not compose to a memory-bound system → measure end-to-end.* **Scope note on the floor:** it is the floor of *single-thread token-by-token* — exactly the current engine's pre-registered scope. The two levers against a memory-bound GEMV are **threads** (aggregate L3 bandwidth) and **block-decode** (amortize weight traffic over N positions) = **E5**. The floor does not close speed; it strengthens the case for the architectural change already queued. **Route:** R1 seed-variance calibration (no gate) → Cat-A corpus + unit cartography (Phase 62, product thesis) → E5 design-gate *after* Cat-A.

### E5 — execution model (block-decode main-loop) + recall tier
**Design gate OPEN (2026-07-04): prerequisites met** — Cat-A data (Phase 62: code validated, log = boundary) + three literature dossiers (`docs/research/Dossier_E5*`). Arms under adjudication: **speculative-AR** (drafter menu, cheapest first: zero-training n-gram drafter [Snakes & Ladders]; MTP-style heads, cheap to co-train at this scale; distilled small-SSM drafter [STree-style]) vs **carve/SBD** (unpublished on pure SSM; recompute dissolves the state-rollback crux — two published state mechanisms exist: Activation Replay, snapshot h_j + fused multi-step). **First measurement (E5.0): n-gram drafter acceptance on code-val against the Phase 62 code checkpoint — zero training, CPU-side.** Honest ceiling stays a≈2–4× (externally corroborated: published pure-algorithmic ceilings 2–3.3×; DeepSeek-V3's own MTP figure is 1.8× TPS). Caution (OpenInfer): speculative decoding pays only in a genuinely bandwidth-bound regime — at the cache-resident sandbox the win must be measured with streamed/emulated weights or claimed only at scale-up scope.

**Design gate DECIDED (2026-07-04) — rule held.** E5.0 (zero-training n-gram drafter, convention: tokens/pass = mean accepted + 1): best surface floor **1.75 tokens/pass** on code (order-5 peak; prose 2.32+, log inflated by the overfit checkpoint). E5.1 (Medusa-1-style MTP heads, 0.263M params, on the FROZEN code backbone, same acceptance apparatus): **1.96 tokens/pass @K=4 vs the pre-registered ≥2.0 rule → speculative-AR exits; carve/set-block decoding is E5's primary lane.** The learned drafter beats the surface floor (+0.21) — the deep-vs-surface thesis is confirmed in direction — but the frozen hidden state carries roughly one reliable step of look-ahead (t+1 matched 77%, t+2+ collapses): in-place active-set predictability does not imply multi-step output predictability from a frozen state. Co-training is NOT taken as an override; it re-enters only if carve fails its own pre-registered gate (the heads are banked at `results/phase62/mtp_code.pt`).
**E5.2 (pre-registered 2026-07-04, before any results): block-factored ("carve") co-training on code.** Foundation recipe with per-offset heads co-trained with the backbone; at generation the state advances by recomputing the scan over committed tokens (recompute dissolves rollback; weight-stream amortization = B, deterministic). Arms: B=1 (= the Phase 62 code checkpoint, anchor 1.242, no new GPU) / B=2 / B=4, matched 4k steps, same data order. Val BPB evaluated under each model's own block factorization, plus per-offset CE, transfer probes, block-committed greedy samples. **Gate: carve is a viable E5 lane iff BPB(B=2) ≤ 1.242 + 0.015 (≈3σ_seed).** B=4 reported as the curve. If B=2 also fails, multi-token generation is closed at this scale and E5 falls back to the block-verify chassis + threads.

**E5.2 executed (2026-07-05) — gate FAIL, clean: multi-token generation from state is CLOSED at this scale.** BPB(B=2) block-factorized = **1.8106 vs the 1.257 gate** (per-offset t+1 = 1.245 / t+2 = 2.376; B=4 = 2.373, monotone 1.264/2.431/2.829/2.980). All sanity checks pass (per-offset arithmetic consistent with the block BPB; sparsity/recall transfer within tolerance; B=1 anchor untouched) — the run is valid and the failure is informational, not a training artifact. **The load-bearing pair is (t+1 = 1.245 ≈ anchor, t+2 = 2.376): with full gradients into the backbone and an explicit P(t+2|state) objective, the optimizer moved nothing — it did not even trade t+1 quality for t+2 information.** This upgrades the E5.1 finding: the hidden state carries ~1 reliable step of look-ahead *even when co-trained to carry more*; predicting t+2 from h_t must marginalize over the uncommitted t+1, and that +1.13 bits/byte gap is genuine conditional entropy, not a frozen-regime defect (two independent measurements — frozen E5.1, co-trained E5.2 — same wall). **The residual co-train speculative-AR arm is closed without a run:** the carve heads *are* a Medusa-2-style co-trained drafter (same mechanism: per-offset heads off h_t, gradients into the backbone); its premise — "freezing was the limitation" — is directly falsified, and the bounded expectation (t+2 CE unchanged in regime → acceptance ~unchanged → ~1.9x tokens/pass) stays below the already-missed 2.0 rule. No gate was loosened; an arm whose premise a pre-registered intermediate measurement falsified is adjudicated closed (the P61 no-rerun precedent).
**E5 final scope:** the model stays **AR-lossless** (the code-domain quality edge, 1.242, is untouched); the engine-side levers are **(a) the block-verify chassis with the zero-cost n-gram drafter** (E5.0 floor: 1.75 tokens/pass, order-5) — layer-major block execution, linear verify, lossless output by construction, ~1.75× weight-stream amortization at the roofline minus verify overhead, claimed only in the streamed/scale-up regime (cache-resident sandbox is mute) — and **(b) threads** (the P61 floor is single-thread by scope; aggregate L3 bandwidth; OpenMP under the reproducibility gate). Negative-result checkpoints banked: `results/phase62/carve_b2.pt`, `carve_b4.pt` (alongside `mtp_code.pt`).

### Phase 63 — E5 implementation: threads + block-verify chassis (OPENED 2026-07-05, pre-registered before any results)

**Scope.** Land E5's decided scope in the consolidated engine. No training, no model change (the model stays AR-lossless). One variable per stage; every stage is adjudicated against the existing oracle chain. Sandbox-resident speed is *reported for context only*; the E5 claim is written exclusively from streamed-regime measurements. Corrected fact recorded at opening: the packed E4 expert pool is ~9.2 MB (32 experts × 6 layers × ~48 KB) — at the sandbox it fits in L3 too, so E4's "2400 KB/tok streamed @ 28 GB/s" was a *counted-and-priced* figure, not DRAM-served. Hence the speed truth below separates byte accounting (real, counted) from wall clock (emulated cold weights as the scale-up proxy).

**Portability directive (owner, 2026-07-05 — folded in before 63.T lands; see the Portability law in Principles).** Nothing in this phase hard-fits the 3600X: (1) the thread partition is computed from `--threads N` plus runtime-discovered topology — no hard-coded 3+3 CCX split; pinning is an optional flag, default off; the T-G2 curve {1,2,3,6} CCX-pinned is the *measurement protocol on this instance*, not code structure. (2) The baseline build drops `-march=znver2` for generic x86-64-v3 with an explicit `-ffp-contract` policy; pre-registered decision rule: if the generic build does not reproduce the P43 golden md5s, pin `-ffp-contract` explicitly and re-baseline the goldens ONCE with the reason documented (goldens are per-build-policy; build policy is project law). The znver2 build survives as an optional tuning target, measured separately. (3) The Phase 63 STOP tables come from a one-command bench harness, so a rented machine re-runs the identical protocol after verifying the binary-portable oracle chain. (4) Forwarded to the Phase 64 design gate: the two-pool budget is expressed as f(LLC size, measured bandwidth) — 16 MB / 28 GB/s are the 3600X's *values* of the law, not the law.

**S0 — oracle-chain re-verification (smoke; nothing lands before it is green).** `make selftest`; 7/7 stage-parity bit-identical at HEAD; the C engine matches the streaming-stateful reference (`e5_ref_stream.py` → `golden_stream_gen.bin`) for gen ≫ seq.
**S0 GREEN (Builder, 2026-07-05).** selftest 5/5; the 7 parity dumps regenerated at HEAD match the P43 md5 record exactly (zero compiler drift on clang 21); the stream-golden reproduced bit-for-bit once regenerated with its stored generation parameters — the initial mismatch was an args mismatch (stored seeds1/gen200 vs current defaults seeds3/gen800), not drift. Process lesson recorded: an untracked golden must be reproduced with its exact generation args before drift is declared; a naked byte-diff lies when parameters differ. Working golden re-baselined at seeds3/gen800 as the stronger long-context oracle for V-G1. 63.T/63.V unblocked.

**63.T — threads (OpenMP, repro-gated).** Design constraint fixed a priori: parallelism only across independent outputs (GEMV rows, experts, SWA lanes) — never inside a reduction; static partition, per-thread scratch, no atomics on accumulators. A parallelization that cannot preserve bit-identity does not land.
- **T-G1 (hard): raw logit streams bit-identical to the single-thread engine** on the golden configs (fp32 ref, dense-full, MoE-full).
- **T-G2 (pre-registered adoption rule):** tok/s at threads {1, 2, 3, 6}, CCX-pinned (Zen 2 3600X: 2 CCX × 3 cores, 16 MB victim L3 per CCX — measure within-CCX before crossing; SMT last, expected mute for a memory-bound workload). **Threads enter the deliverable iff ≥1.5× at some thread count with T-G1 green**; otherwise documented and reverted. Honest expectation: sublinear; cross-CCX may *hurt* the resident pool (L3 split) while *helping* a streamed pool (aggregate DRAM bandwidth) — that asymmetry is itself a scale-up datum to record.

**63.T CLOSED (Architect, 2026-07-05) — both gates green, threads ADOPTED.** P0: effective `-ffp-contract=on` in both builds (the engine uses explicit FMA intrinsics); the generic x86-64-v3 baseline reproduces selftest 5/5 + the 7/7 P43 md5s exactly → `-march=znver2` is pure scheduling tuning with zero float effect; no golden re-baseline (rule not triggered). Implementation: `OMP_PFOR` on output-independent loops only (matvec rows, per-channel scan exact+fast, dt, MLP-LUT 32-row tiles); reductions stay serial; `--threads N` (default 1); pinning external via `OMP_PROC_BIND`/`OMP_PLACES` — zero topology code (portability law honored). **T-G1 PASS:** threads=1 reproduces P43 7/7; bit-identical at {2,3,6} on fp32-ref / dense-full / MoE-full (by-construction: one output = one thread, serial reduction → invariant in N). **T-G2 PASS: dense-full 827 → 2029 tok/s at t6-spread = 2.45×; MoE-full 635 → 1519 at t6-close = 2.39×** — well above the 1.5× adoption bar. Banked findings: (1) **spread ≥ close at 2-3 threads — the pre-registered expectation "cross-CCX may hurt the resident pool" was WRONG on this instance**: the memory-bound workload prefers aggregate bandwidth (two L3s + DRAM channels) over L3 locality (instance datum, Zen2 2-CCX victim-L3; recorded, not generalized). (2) 2.4× on 6 cores = the memory-bound ceiling (bandwidth aggregation, not compute) — consistent with Phase 61. (3) SMT-12 regresses (1.41×/1.43×) — per-core bandwidth contention, as predicted. **Open bookkeeping (light verification, non-blocking, folded into 63.C): attribute the t1 deltas vs the historical singles (dense 827 vs 848, −2.5%; MoE 635 vs 701.7, −9.5%)** — separate generic-vs-znver2 build effect from OMP-at-N=1 overhead (two runs: znver2 build at t1, and/or OMP compiled out); the −9.5% on MoE is too large to leave unattributed. **Scale-up composition note (to be measured in 63.C):** the 2.45× is the *resident/L3* regime; on a DRAM-streamed pool the thread ceiling is ~40-44 GB/s aggregate vs probe-3's 28 GB/s single-core ≈ 1.4-1.5× — exactly the residual block-verify attacks; the two levers are complementary, not multiplicative. Extra deliverable landed: `benchmarks/phase63/bench_63t.sh` — the one-command protocol harness for rented machines (portability law).

**63.V — block-verify chassis (zero-cost n-gram drafter).** The R-F main-loop: draft K tokens with the order-5 train-only backoff n-gram (E5.0's best; 1.75 tokens/pass on code); run the block layer-major so each weight is touched once per block; the scan yields per-position states as a byproduct → checkpoint at the last accepted position, rollback = discard (R-H); verify = hygiene-locked greedy (rep 1.2 / win 128); accept the longest matching prefix; commit. Pre-listed traps: the rep-penalty window must contain committed tokens only (rejected drafts must not pollute it); the causal-conv ring and the SWA kv-ring roll back with the state (checkpoint ring indices, not just h); the n-gram table is an engine asset.
- **V-G1 (hard): block-verify greedy output token-identical to the AR engine** over the eval stream and the long-context stream-golden. Lossless by construction — any mismatch is an implementation bug, not a tuning matter.
- **V-G2 (transfer): in-engine tokens/pass within ±0.10 of the e5_0-apparatus figure measured on the *same model/domain pair*** (this phase runs on the TS foundation models the oracle chain covers; the apparatus re-run on that pair provides the reference). K is an engine knob; the K used for speed is the K reported for tokens/pass.
- **V-G3 (pre-registered speed rule):** sandbox-resident = report-only (expected neutral-to-slower: wasted verify compute at ~1.75 tokens/pass). Streamed regime, two measurements: **(a) accounting** — E4 expert pool: touched KB/token, block vs token-by-token (expert-union amortization; a counted quantity, independent of where the bytes are served); **(b) emulation** — weights forced DRAM-cold (rotating replicated buffer ≫ L3) as the scale-up wall-clock proxy. **The scale-up claim is adopted iff the emulated-streamed wall-clock gain is ≥1.4×** (honest floor under the 1.75× ideal; drafting and verify overhead priced in).
- **V-G4 (budget):** n-gram table bytes counted against the resident budget; lookup cost measured.
- Queued rung (separate variable, only after V-G1..G3 are green): export `catA_code.pt` to the engine format with its own parity golden and measure the code-domain 1.75 tokens/pass in-engine — the product-thesis pairing (code × engine), kept out of the gate path because it adds an export/vocab variable.

**63.V core adjudicated (Architect, 2026-07-05): V-G1/V-G2 PASS; V-G3b/c BLOCKED on a design-invariant violation, caught before the gate is read.** Results (TS pair, `--threads 1`, N5/K=8): **V-G1 PASS** — token-identical to AR over 3 seeds × 800 (lossless). **V-G2 PASS** — in-engine tokens/pass 2.623 (apparatus protocol, 300 pos/ctx128) vs reference 2.590, gap 0.033 < 0.10; honest reconciliation accepted: the self-generated *stream* tpp is 2.775 (> sampled 2.623 — auto-generated text is more n-gram-predictable than sampled val); the sampled figure is the gate number, the stream figure is the production number. **V-G3a (report-only): AR 844 → block-verify 248 tok/s = 0.29× in-cache — the "mute/worse at sandbox" prediction confirmed, BUT the attribution is impure**: the implemented commit mechanism is snapshot-at-block-start → speculative forward → restore + **replay of the committed prefix**, so 0.29× conflates speculation waste with replay double-compute; do not quote it as "the chassis cost" until re-measured on the compliant chassis. **V-G4:** order-5 table = 15.8 MB (1.08M contexts). **Architect correction to the "exceeds 16 MB L3" alarm — category error under the ρ-law: the table is not a stream, it is ~K random probes per pass (binary search ≈ 20 latency-bound touches ≈ µs-scale), so it needs RAM footprint, not L3 residency.** Pruning (count-threshold; singleton contexts have unreliable argmax anyway) stays queued as scale-up hygiene — per-domain tables add up and pruning may even raise tpp — but it is not a blocker; the lookup *cost* measurement (part of V-G4) is still owed.
**Blocking directive for V-G3b/c: the sealed chassis invariant is "each weight touched once per pass" — snapshot+replay violates it.** Quantified: a token-wise replay costs (1+tpp)/tpp weight-streams per token — *worse than plain AR* in the streamed regime; even a batched (layer-major) replay costs 2 streams/pass → gain = tpp/2 = 1.39× on TS (below the 1.4× gate) and **0.875× on code (tpp 1.75) — a loss on the product domain**. Reading V-G3c on this mechanism would falsify the implementation, not the design. Fix menu (the two published SSM state mechanisms from the E5 dossier): **(1) Activation Replay — store per-position post-GEMV scan inputs (~5 KB/layer/position ≈ ~240 KB scratch at K=8) during the speculative pass; on commit, replay only the elementwise recurrence (no GEMVs, no weights) from the block-start snapshot** — favored by the numbers; (2) per-position h snapshots (~1.2 MB/position → ~9.4 MB at K=8, competes with L3). Builder chooses and declares; V-G1 re-runs after the swap (cheap identity re-gate); only then V-G3b (expert-union accounting) + V-G3c (DRAM-cold emulation, ≥1.4× adoption rule). With weights-once the ideal streamed gain is tpp itself (2.775 TS / 1.75 code) before draft+verify overhead — comfortable headroom over the gate.

**Activation Replay landed + adjudicated (2026-07-05): chassis COMPLIANT; V-G3b/c GO under the protocol below.** Swap: per-position `ActPos` stash (raw pre-conv in_proj, dt, B·x for SSM layers; k/v for SWA ≈ 28 KB/pos, ~226 KB @K=8, L2-resident) captured during the speculative pass behind a `g_cap` guard (AR path unperturbed — P43 spot md5 intact); `replay_commit()` advances persistent state (scan h + conv ring + kv ring) over the matched prefix by re-running the **elementwise recurrence only** — zero GEMV, no streamed weight re-touched (reads resident A + tiny conv_w); the engine token at the divergence is processed as the first position of the next block. Re-gates: **V-G1 PASS dense + MoE** (3 seeds × 800), selftest 5/5, V-G2 unchanged (mechanism-independent; MoE-pair tpp 2.633 now measured). V-G3a re-measured on the compliant chassis: **0.30× dense / 0.33× MoE** — attribution now pure (speculation waste at hot weights; the sandbox "mute/worse" prediction stands with clean books). V-G4 closed: lookup 70.8 ns/draft; table is RAM-resident latency-class, not a stream.

**V-G3b/c protocol (pre-registered by the Architect, 2026-07-05, BEFORE any streamed measurement — refined because a naive emulation would measure model size, not the chassis):** the 8.3M sandbox model's streamed-class bytes are small relative to its per-position compute (fp32 scan recurrence, activations), so a streamed-class-only cold emulation is arithmetically unable to reach 1.4× at any K regardless of chassis quality. The honest structure:
- **V-G3b (accounting, E4 expert class, counted):** per-pass expert-union touched-KB per committed token vs token-by-token 2400 KB/tok (same run), union-size stats per layer, K ∈ {4, 8}. Pre-declared reading: the expert class amortizes at **union-rate, not tpp** (positions route to different experts; rejected positions' experts still count) — expected well below tpp; this is a granularity datum feeding Phase 64's two-pool sizing, distinct from the claim gate.
- **V-G3c (emulation, wall-clock):** rotating replica buffer ≥128 MB covering **all weight classes** (in the scale-up regime every weight class is bandwidth-bound — DRAM or L3 — so all weights go cold; the per-position elementwise compute stays, honestly, since it does not amortize at scale-up either). Rotation per token (AR) / per pass (block); threads = 1; **K sweep {2, 4, 8}** with tpp measured per K (the K of the speed is the K of the tpp); token-identity asserted in emulated mode (replicas are identical copies — any diff is a replication bug). Instrumentation: weight-bytes touched per token (counted) + wall time; a pure-stream GB/s microbench on the same buffer as *calibration only*.
- **Pre-declared diagnosis taxonomy (fixed before results):** (1) **Mechanism gate (binary): block-verify weight-bytes/token = AR weight-bytes/token ÷ tpp.** If this fails the chassis is broken, full stop (also catches a wrong loop order: rows must stream once and apply to all K positions). (2) **Claim gate (as sealed): wall-clock ≥1.4× at best K → the E5 speedup claim is adopted**, scoped to weight-traffic-dominated regimes. (3) If the mechanism gate passes but wall-clock fails on the per-position compute floor, **the claim is NOT adopted**: verdict = "mechanism verified, claim scoped out at this model size", and the measured (traffic, compute) components become the quantitative sizing input for the Phase 64 design gate — the honest possibility pre-registered at E5 opening (OpenInfer: the win exists only where streaming dominates). Arithmetic fixed now: at the banked tpp values the 1.4× gate is reachable iff the per-position compute floor is ≲ ~25-30% of the all-cold traffic time; the emulation measures exactly this ratio, nothing is assumed.
- Appendix (non-blocking): 63.T t1 attribution — t1 with the znver2 build; t1 with OMP compiled out (separates build effect from OMP@N=1 overhead on dense −2.5% / MoE −9.5%).

**V-G3b/c executed + adjudicated (Architect, 2026-07-05): pre-registered outcome (3) SEALED — mechanism VERIFIED, speed claim NOT adopted at 8.3M.** Arithmetic independently verified: block weight-bytes/token = AR ÷ tpp *exactly* at all three K (AR 15,370 KB/tok; 8538/6102/5214 KB at tpp 1.800/2.519/2.948) — the weights-once mechanism is proven by counting, three times over; per-position block cost (1.18 ms) ≈ token cost (1.14 ms) → no kernel artifact; token-identity IDENTICAL at every K in emulated mode (replica-bug guard). Claim gate: best 0.679× (K=2) < 1.4× — the per-position compute floor (1.14 ms) exceeds the all-cold traffic (0.71 ms = 15.4 MB/tok @ 22 GB/s calibrated), C/T = 1.60 vs the ≲0.25–0.30 needed; robust (flipping the verdict would need ≲4 GB/s bandwidth — no real machine). **The E5 speed claim is NOT adopted at this scale. Block-verify stays in the engine as a correctness-proven, scale-up-conditional capability behind `--block K`, default OFF — the default AR path keeps full bit-parity (P43 md5 intact).** Honest caveat registered: the K-batched kernels (`mv_K`, `matvec_lut_full_K`) introduce an FP reorder ≤ 5.7e-6 vs the token kernels; the sealed gate is *token*-identity (holds on eval, stream-golden, and emulated runs at every K); logit-bit-identity between block and AR paths is not claimed.
**V-G3b upgraded to a law.** The measured per-layer unions match the pure-i.i.d. prediction E·(1−(1−topk/E)^K) = 21.9 (K4) / 28.8 (K8) vs measured 21.8/28.5 — probe-4's "routing ≈ i.i.d." is confirmed in-engine, quantitatively, a third independent time. Derived law: **block amortization of a routed weight class = topk·tpp/union — under i.i.d. routing it is break-even at best (K2 ≈1.03, K4 0.96, K8 0.86) and *worse* for more granular MoE (union → topk·K, amortization → tpp/K); amortization at full tpp exists only for SHARED weights.** Consequence for the Phase 64 design gate: if the scale-up streamed pool is expert-dominated (the current two-pool blueprint), block-verify does not help it — it pays only where a large *shared* streamed class dominates; the expert pool's levers remain threads (~1.4–1.5× DRAM ceiling) and granularity/prediction.
**Banked sizing input for Phase 64:** gain(K) = (T+C) / (T/tpp + C·K/tpp); the lever pays iff C/T ≲ 0.25–0.30 at the banked tpp values. Measured components at 8.3M: T = 0.71 ms, C = 1.14 ms. A traffic-dominated scale-up point therefore requires roughly an order of magnitude more *shared* streamed bytes/token AND a resident core several times cheaper per position than today's fp32 scan (ternary resident + scan optimization) — a concrete, falsifiable requirement the Phase 64 gate must price.
**t1 attribution accepted (63.T bookkeeping closed):** the regression vs the historical singles is OMP-at-N=1 overhead (~6%; generic-no-OMP reproduces 876/704 ≈ historical 848/701.7), not the build switch; znver2+OMP is *slower* than generic — the baseline choice is reconfirmed. Residual 1–4% across runs = run variance, noted. **Queued rung re-scoped: the code-export rung is DEMOTED to optional** — its gate purpose (in-engine code tpp feeding the speed claim) died with the claim; its residual product-pairing value moves to Phase 64 prep. Not required for Phase 63 closure.

**63.C — closure (final stage, re-scoped after V-G3).** (i) Recover the N=1 OMP tax with the standard OpenMP `if` clause on the parallel regions (one line, portable, no dual code path); re-run t1 + spot T-G1; if it does not recover, accept and document (the portability/simplicity law beats 6%). (ii) Consolidated final phase table (threads curves, block emulated curve, expert unions, t1 attribution) folded into the one-command harness. (iii) STOP → the Architect rewrites the execution-model section of `SCALEUP_ARCHITECTURE.md` from measured numbers only and closes the phase in this plan.

**63.C executed + PHASE 63 CLOSED (Architect, 2026-07-05).** (i) The `if(threads>1)` clause landed (T-G1 intact at {1,2,6}; P43 3/3 at threads=1) but does NOT recover the ~6% N=1 tax — the cost is the pragma's loop-body outlining defeating serial vectorization, not the fork. Per the pre-registered rule: **tax accepted and documented** — one threaded binary, ~6% at N=1, repaid 2.4× at N≥2; a separate no-OMP binary would violate one-binary for a single-thread-only gain. (ii) `bench_63t.sh` now reproduces the whole phase in one command (build + selftest + 7/7 parity → T-G1/T-G2 → V-G1/V-G2/V-G3a → V-G3b unions → V-G3c emulation → t1 attribution) — the rented-machine protocol artifact. Measurement note, recorded honestly: absolute tok/s vary ~5-10% across sessions/protocols on the reference desktop; all gates and ratios were within-run, and the harness pins the protocol — reference numbers are quoted per-protocol. (iii) `SCALEUP_ARCHITECTURE.md` §3.8 + multiplier table + probe map rewritten from measured numbers only.
**Phase 63 deliverables:** consolidated engine with `--threads N` (adopted: 2.45×/2.39×, bit-identical) and `--block K` (correctness-proven, scale-up-conditional, default off; AR path keeps full bit-parity); generic x86-64-v3 baseline build (portability law); one-command phase harness; n-gram asset pipeline. **Banked for the Phase 64 design gate:** measured components (T ≈ 0.6-0.7 ms traffic, C ≈ 1.1-1.3 ms compute at 8.3M) + the win-condition C/T ≲ 0.25-0.30; the routed-vs-shared amortization law; thread ceilings (2.45× resident / ~1.4-1.5× DRAM-streamed); expert-union i.i.d. saturation curve. Optional carry-overs (not blockers, queued to Phase 64 prep): code-checkpoint engine export + parity golden; n-gram singleton pruning; record-only popularity-skew measurement at the next scale-up training.

**Post-closure audit (Architect, 2026-07-05, full-chain review requested by the owner) — four corrections/annotations, none reopening a gate:**
1. **Headline precision:** T-G2's 2.45×/2.39× is relative to the threaded binary's own t1 (which carries the ~6% OMP tax); relative to the best *serial* binary the same-protocol gain is ≈2.2-2.3× (dense) / ≈2.2× (MoE) — still far above the 1.5× adoption bar. External claims must state the definition, or quote both.
2. **The C/T win-condition is roofline-optimistic:** measured V-G3c gains ran 0.61× (K2) to 0.74× (K8) of the gain(K) formula — AR hides traffic under compute (AR-cold measured 1.305 ms vs T+C = 1.85 ms) while block pays stash/replay/rotation overheads growing with K (1.927 ms measured vs 1.661 ms formula at K2). **C/T ≲ 0.25-0.30 is necessary, not sufficient; working threshold ≈ 0.15-0.20**, to be re-measured at any Phase 64 candidate point. Annotated in `SCALEUP_ARCHITECTURE.md` §3.8.
3. **One banked input is derived, not measured:** the "~1.4-1.5× streamed thread ceiling" comes from probe-3 arithmetic (aggregate ~40-44 vs single-core 28 GB/s); the planned threads×cold-emulation measurement was silently dropped when 63.C was re-scoped. The machinery exists (`--g3c --threads N`); one cheap run closes it — queued as the natural *first measurement* of the Phase 64 design gate. Related re-pricing: E4's "priced @ 28 GB/s" figures should use the measured rotating-stream 22 GB/s (or per-machine calibration).
4. **Process slip (third of its kind): the V-G3b/c instrumentation and refined protocol were in the working tree, unpushed, when the gate-bearing run executed** — the push-before-run witness (GitHub received-time) is missing for this stage. The taxonomy demonstrably predates the results in the session record, but the falsifiable form of the discipline was not met. Rule restated for Phase 64: **no gate-bearing run before protocol + apparatus are pushed.**
Stale-doc fixes applied in `SCALEUP_ARCHITECTURE.md`: §6 niche-3 re-scoped to its measured (negative) answer; §7 roadmap updated; §2 gains the measured **second wall** (per-position compute floor — bytes are not the only budget). Clean on re-check: E5.0/1/2 arithmetic and gate seals; V-G3 mechanism counting (bytes = AR÷tpp at all K); the i.i.d. union law; the oracle chain (PyTorch golden ↔ C-AR ↔ C-block); the apparent spec-AR-exits-vs-n-gram-chassis tension (resolved: V-G3c adjudicated the surviving lane and adopted no speed claim); anti-Goodhart record (three gates held under temptation: E5.1 1.96, E5.2, V-G3c).

**Deliverable:** the consolidated engine with `--threads N` / `--block K`, the full oracle chain green, and a measured, honestly-scoped E5 claim. Standing rules: no `-ffast-math`; decode hygiene locked; end-to-end gates (the E4 law); apparatus committed before gate-bearing runs.

## Consolidation (P4.3, 2026-07-03)
The five stage engines are consolidated into **one core with feature flags**: `benchmarks/phase60/engine.c`
(`--mlp fp32|lut --skip on|off --exp exact|fast`; dense vs MoE selected by the weights magic E1M1/E4M1).
**Acceptance (pre-registered): bit-identical raw logit streams vs the archived stage engines, per
configuration — 7/7 IDENTICAL** (E1, E2, E3, E3.5-iso, E3.5-full, E4-ref, E4-full; md5 table in
`results/phase60/p43/P43_REPORT.md`). Per the E4 law the acceptance is end-to-end, not kernel-level.
The stage engines are archived (not deleted) in `archive/benchmarks/phase60_stage_engines/` and stay
buildable (`make stage-engines`) as the parity oracles. Consolidated tok/s are re-measured at training end.
CI (`make selftest`) runs the synthetic kernel self-tests — every LUT path bit-exact vs scalar-int +
poly-exp ≤2e-6 on the documented domain — with no weights required.

## Working protocol
Builder implements + runs gates + STOP with the full gate table; Architect reads the numbers and closes each stage. Known traps checklist for E1: scan discretization order (the #1 parity risk — golden-trace exists for this); exported weights must be the fp32 master copies (training ran bf16 autocast); BPE merge-order parity; rep-penalty window semantics (decode window, not attention window).
